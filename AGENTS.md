# typing-dongnanya

## 项目目标

- 本项目计划开发面向东南亚语言的「边写边译」输入法，底层输入法引擎拟基于 `librime`。
- 当前仓库于 2026-07-31 初始化；macOS arm64 为首个可运行客户端，2026-08-04 起并行开发 Android arm64 V1；用户已明确产品用于商用，最终开源/闭源方式仍未确定，但商业交付包按用户要求不得直接复制、打包或依赖 GPL/LGPL 的代码、schema、词典或其它数据，只允许使用项目自有内容、已购买商业授权内容或经核实可商用的 BSD/MIT/Apache-2.0 等宽松许可证内容；客户端统一只访问已部署的自有 HTTPS 翻译服务。
- 首发平台按当前开发机确定为 macOS arm64；应用以独立 `com.caizhichao.typing-dongnanya` 输入法包安装到 `/Library/Input Methods/TypingDongnanya.app`，不替换、删除或自动切换其它输入法。
- 工程目录固定为：`platforms/macos/` 放 Swift / Objective-C++、Info.plist、macOS 资源与冒烟测试，`platforms/android/` 放 Gradle、Manifest、Kotlin、JNI/CMake 与 Android 资源，`shared/RimeData/` 放两端共用的 Rime patch/OpenCC 运行数据，`vendor/` 锁定上游子模块，`scripts/macos/` 与 `scripts/android/` 放各平台 CLI，`server/typing-dongnanya-api/` 放 Go 翻译服务；不要把构建产物或密钥写入源码目录。
- 线上翻译服务固定使用 Go 实现，源码位于 `server/typing-dongnanya-api/`；禁止把 Node.js 或 Rust 作为线上翻译服务运行时。部署必须在本机使用 `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build` 生成 Linux x86_64 release 二进制后，通过 SSH 上传二进制和 systemd/Nginx 配置；腾讯服务器不安装 Go、Rust 或编译工具链。
- 2026-08-01 已核实同一腾讯服务器上的 `cosplay-go` 发布范式：本地生成 Linux x86_64 ELF 二进制，上传到 `/opt/cosplay-go/`，systemd 直接执行该二进制，服务监听 `127.0.0.1:13120`，Nginx 反向代理到本机端口；本项目 Go 翻译服务沿用该范式。
- macOS 输入源元数据必须使用 `APPL` 包类型、模块前缀的 InputMethodKit 类名、`TISInputSourceID`、`TISIntendedLanguage`、`ComponentInputModeDict`、脚本代码与输入法图标；Swift 构建固定传入 `-module-name TypingDongnanya`，避免包内类名与 Info.plist 不一致。
- 输入源的根 ID 与模式 ID 都必须在 `Resources/*lproj/InfoPlist.strings` 提供本地化显示名；macOS“添加输入法”窗口按 `TISIntendedLanguage` 的语言分类和检索，不按产品名检索：当前值为 `zh-Hans`，测试时必须搜索“中文”或“Chinese”，再选择 Typing 东南亚，不能搜索“东南亚”。
- 注册页与菜单栏必须使用两套不同图标：`CFBundleIconFile` 和根 `tsInputMethodIconFileKey` 使用本项目原创彩色应用图标，输入模式的 menu/palette/alternate-menu 键继续使用透明 16×16 单色模板；不得再把菜单模板放大用于系统设置注册页。根来源本地化名为产品名，模式来源本地化名为“简体拼音 · 边写边译”，避免两行重复。
- 根目录通过 `package.json` 统一暴露 `bun run build` / `bun run test`（默认 macOS）以及 `build:macos`、`test:macos`、`build:android`、`test:android`、`build:all`、`test:all`；原平台脚本仍是唯一真实执行入口。macOS 构建产物固定收口到 `platforms/macos/build/`，验证二进制与临时 Rime 数据位于 `platforms/macos/build/tests/`；Android 测试 APK 仍固定为 `platforms/android/app/build/outputs/apk/debug/app-debug.apk`，当前只打包 `arm64-v8a`。
- Android Debug APK 固定流程：每次交付测试包先提升 `platforms/android/app/build.gradle.kts` 的 `versionCode` 与 `versionName`，再执行 `bun run scripts/android/test.ts`，禁止只跑 `build.ts` 后直接发布；该验证入口会重新 assembleDebug、执行 Android Lint，并核对包名/版本、Launcher 与 IME 入口、单一 `arm64-v8a` ABI、Rime/OpenCC 数据、第三方许可证、HTTPS capability、上游凭据边界、APK v2 签名和本轮关键源码契约，任一步失败都不得上传。验证产物固定为 `platforms/android/app/build/outputs/apk/debug/app-debug.apk`；有且仅有一台已授权设备时可执行 `bun run scripts/android/install.ts` 覆盖安装并打开设置页，但脚本不得自动启用或切换输入法。服务器发布固定执行 `bun run scripts/android/deploy-apk.ts`：稳定下载相对路径从本机 600 权限的 `~/.config/typing-dongnanya/apk-download-path` 读取，完整随机路径不得写入仓库；APK 必须由 Bun `readFileSync` 后经 SSH stdin 传输，发布时只在 `/tmp` 暂存旧包用于失败回滚，校验新包远端 SHA 后原子覆盖原 URL，不因 APK 内容更新修改或 reload Nginx。发布脚本必须再从公网完整下载并核对 SHA-256、确认响应为 APK attachment、错误随机路径返回 404、翻译 API health 正常，成功后删除临时旧包和 releases 下其它 APK，最终服务器只能保留一份 APK；完成后更新本文件中的 APK 内版本、SHA-256、自动验证结论与真机未验收项，禁止把构建/发布通过表述为真机功能验收完成。
- 2026-08-03 本机 Bun 1.3.14 曾被 macOS `taskgated` 以无效签名终止；已对现有 Bun 可执行文件做本机 ad-hoc 重签后恢复。若再次出现命令立即以 137 退出，先查看 `~/Library/Logs/DiagnosticReports/bun*.ips` 是否为 `Taskgated Invalid Signature`，不要误判为项目构建 OOM。
- 首版 UI 使用 macOS InputMethodKit 原生 marked text 只显示拼音，候选和首选高亮由独立深色玻璃候选条显示，当前稳定句子的草稿译文由紧邻的第二层浮窗显示；没有复制 Squirrel / Weasel 代码。检索其公开实现未发现可复用的手写输入模块，因此 V1 明确不含手写输入。
- 首版设置使用输入法进程内的独立原生窗口，不额外安装设置应用：候选条尾部和输入法菜单都可打开；翻译页提供边写边译开关、目标语言、安全输入说明和“翻译当前剪贴板”快捷键，输入页提供中英、简繁、标点和半全角基础设置。设置只写本项目 `UserDefaults` 并回写当前 librime 会话，不切换 macOS 系统输入源。
- 2026-08-04 起客户端翻译链不再读取宿主编辑框正文，不使用 Accessibility、全局 Event Tap、全局键盘 monitor 或正文通知，也不声明 `NSAccessibilityUsageDescription`；Rime 拼音输入只累积本输入法明确提交的文本，宿主实际转发 Command-V 或多字符粘贴文本且与当前剪贴板一致时才翻译剪贴板内容。此模式不申请辅助功能或输入监控权限，但不能承诺飞书、浏览器等未把粘贴交给 InputMethodKit 的宿主自动触发，也不能安全点击替换宿主原文。
- 当前机器没有可用 Developer ID 签名身份，测试包仅做 ad-hoc 签名；macOS 26.5.1 的用户目录安装虽可让 `TISRegisterInputSource` 返回成功，却没有写入当前用户的 `AppleEnabledThirdPartyInputSources`，不能视为系统设置可用。安装脚本必须先清理本项目旧的用户级测试包，再经管理员授权复制到 `/Library/Input Methods/TypingDongnanya.app`，由系统安装阶段执行 `--register-input-source`；替换磁盘包不会替换已运行进程映射的旧二进制，安装脚本仅结束本项目旧进程，用户切换到其它输入法再切回后才启动新版本。用户在系统设置手动添加前，公开 `TISEnableInputSource` 不会写入第三方来源记录，因此不能拿它冒充自动安装成功。绝不调用 `TISSelectInputSource`，不自动切换当前输入法，也禁止改写 `AppleEnabledInputSources`。

## 已确认的技术与授权边界

- `librime` 核心采用 BSD-3-Clause；引入、分发时仍须逐项核对实际依赖、输入方案和词库的许可证。
- `rime/squirrel`（macOS）与 `rime/weasel`（Windows）采用 GPL-3.0。它们只能作为系统输入法前端实现和交互逻辑的参考；在未明确采用 GPL 开源发布策略前，不得将其代码、二进制或派生实现作为闭源商业产品的基础。
- 2026-08-01 重新核对 `librime` 与当前 Squirrel / Weasel 源码：`librime` 本身只提供输入引擎和上下文 API，不提供可直接使用的 UI 壳；完整前端至少要消费组合区光标与选中范围、候选标签/注释、高亮项、页码与末页状态，并把候选选择、高亮、翻页和组字光标移动回写引擎。当前 `RimeBridge.mm` 只透出了其中一部分，下一轮不能继续在不完整快照上堆 UI。
- 2026-08-01 补充核对 macOS 开源前端：`macSKK` 为 GPL-3.0-or-later，只能作行为参考；`Gureum` 核心为 2-Clause BSD，可用于核对 InputMethodKit 生命周期和原生候选事件转发；`Easydict` 为 GPL-3.0，只参考翻译交互；`Voxt` 为 Apache-2.0，可参考其“保存原始文本快照和替换范围、结果返回后再次验证文档未变化”的事务边界。当前仍采用独立实现，不直接复制这些项目代码或视觉资源；以后若实际复用宽松许可证代码，必须记录文件来源、修改内容和 NOTICE/版权声明。
- 2026-08-03 补充核对社区复制/粘贴实现：`azooKey-Desktop` 的 AI 选中文本改写先用 `IMKTextInput.insertText(...replacementRange:)` 直接替换，兼容兜底则暂存原剪贴板、写入结果、用 `CGEvent` 模拟 Command-V，再延迟恢复剪贴板；`WritingTools` 同样采用剪贴板快照、重新激活原应用、模拟粘贴和 `changeCount` 防覆盖，并明确跨进程没有粘贴完成通知，只能使用保守延迟。社区对用户主动粘贴的检测通常依赖 Event Tap/Input Monitoring、Accessibility 的值变化通知或宿主插件；单独观察 `NSPasteboard.changeCount` 只能发现剪贴板发生复制/写入，不能证明内容已粘贴进当前输入框。当前项目若继续禁止 Accessibility、全局事件和剪贴板观察，则只能依赖宿主转发的 IMK 事件或下一次输入事件补触发，不能承诺所有应用都能在粘贴后立即翻译。
- `vendor/` 仍保留 `rime-prelude`、`rime-luna-pinyin` 与 `rime-essay` 子模块供只读格式/行为核对，但 2026-08-05 起 macOS/Android 商业构建链不再复制或引用它们。共享拼音数据固定从 Apache-2.0 的 AOSP PinyinIME `49aebad1c1cfbbcaa9288ffed5161e79e57c3679` 原始 65,105 条词典经 `scripts/rime/generate-aosp-pinyin-dictionary.ts` 确定性生成；源文件、NOTICE 与生成词典 SHA-256 分别为 `408700f28a56091fa07f3b849a0f134fbfc71e6b2ae9b3f52973a5b076f599ff`、`b5830d96fb5a7e7e7ebcc295f352846b4b998e78fdc8f9aa68e134d2e4b39986`、`c13842b707ed764a5520fe158908f38ac0cf5ae1d84bf5e3b84a2972a39ec539`。两端测试必须同时检查所需资源、来源声明、文件名和文本引用，拒绝旧 LGPL Rime 数据重新进入交付包。
- 以后新增任何 Rime 前端、词库、输入方案或第三方代码前，必须先记录其许可证、修改方式和分发影响到本文件。GPL/LGPL 项目的公开代码允许只读参考功能边界、交互行为、系统生命周期、API 调用顺序和测试口径，但禁止复制、改写、逐句翻译、机械迁移其代码、schema、词典、配置、UI 资源或形成可识别的派生实现；正式实现必须基于 librime/系统官方 API 与项目自有设计独立编写，并能说明参考事实与本项目实现的边界。
- 2026-08-05 全拼、自然码双拼与小鹤双拼已统一改接项目 `typing_pinyin` 词典并同步到 Android 设置、持久化、JNI schema 选择和键盘生命周期；加入五笔后共享 Rime 数据版本为 `7`。macOS 冒烟验证 `nihao`、自然码 `nihk`、小鹤 `nihc` 均提交“你好”，不再引用 `luna_pinyin`。
- 2026-08-05 Android `0.2.0 (5)` 已增加项目独立实现的中文九键：3×4 数字/字母分组通过 `typing_pinyin_t9` 将 `64426` 解析为“你好”，候选与词频复用 Apache-2.0 拼音数据；切换方案只重建固定高度键区并安全重挂复用按键，中英切到西文时回到 QWERTY，避免九键直接输出数字。`bun run scripts/macos/test.ts` 与 `bun run scripts/android/test.ts` 均通过，最终 Debug APK SHA-256 为 `c89f4543859bc7bf6ab81678b2e40e8d4e8a857218b5f0caa70ed8982e7bf49a`；尚未在 Android 真机验收九键候选、方案切换与手感，不得表述为真机完成。
- 2026-08-05 五笔不使用官方 LGPL `rime-wubi` 或上游链不清的极点/搜狗码表，当前锁定 `tenfyzhong/wubimb` 的 MIT 数据：仓库提交 `afba8da77234920d10ccf0c94bfc7897861f3543`、数据提交 `198c11625482c5bab34fb19128a872851f9b86ed`，原始 `wubi86.txt` 共 53,727 条且 SHA-256 为 `9fca2e8eb1b19263a09dcf54bc94de128902b4d3596ed64a305178ae910c63c8`；该仓库先建立 MIT LICENSE、后由同作者加入码表且未发现相反上游归属说明，因此可用于当前实现，但生产商用前仍建议法务复核来源链。`scripts/rime/generate-wubi86-dictionary.ts` 负责哈希/数量/字段门禁，生成词典 SHA-256 为 `b8a00a0307c33432bf8b2e25075ff85f45dcd01281e64816bc58f2ac830878c2`，macOS smoke 已验证 `wqvb{space}` 提交“你好”。
- 2026-08-05 Android 手写不依赖 Google 动态模型下载或系统隐私权限，固定打包 Apache-2.0 的 PaddleOCR `PP-OCRv6_tiny_rec_onnx` 提交 `2612ab37152ae0a677521bae4e1e3d4fb4cf7c30` 与 MIT 的 ONNX Runtime Android `1.21.1`；模型 SHA-256 为 `9ef676d6ed3c88256a2d92c640c44f25b0c40947e111b14b8be8f594091563e6`，生成的 6,905 项字符表 SHA-256 为 `a295ab9d9a1e00aa86873a06230989f90c019291d1e7eaf026693ca136437abe`。手写是独立 `KeyboardInputMode`，不是 Rime schema；画布只在抬笔后触发 420ms 停顿识别，ONNX 推理使用独立串行线程和 generation 拒绝旧结果，候选确认后才并入同一原文草稿并继续 1 秒翻译链。该 OCR 模型对真实手写的准确率、首轮加载延迟、连续书写和候选手感必须以 Android 真机验收，自动构建通过不能替代质量结论。

## 实时翻译 API 结论（2026-08-01 更新）

- 首选候选：阿里云百炼 `qwen-mt-flash`。它面向文本机器翻译，支持 92 种语言、术语干预、翻译记忆、领域提示，以及真正的增量流式输出；适合中文与泰语、越南语、印尼语、马来语、老挝语、缅甸语等东南亚语言组合。
- `qwen-mt-flash` 于 2025-11-10 在中国内地发布，于 2026-01-12 在全球地域发布；截至 2026-07-31，它属于发布约八个半月的当前翻译模型，不是刚发布的实验模型。
- 2026-07-31 16:47（Asia/Shanghai）从本机查询 OpenRouter 公共 `GET /api/v1/models`：返回 200，但没有任何模型 ID 含 `qwen-mt`；在 OpenRouter 正式上架并由其目录查询到前，不得将它作为 `qwen-mt-flash` 的测试或生产渠道。
- 购买前先在百炼的模型体验页验证翻译质量；模型页标注 `qwen-mt-flash` 支持模型体验。新开通百炼账户在北京地域、服务部署范围为中国内地时可获得 90 天新人免费额度，但具体到 `qwen-mt-flash` 的可用 token 数必须以控制台当日显示为准，并在调用前开启“免费额度用完即停”。
- 阶段性流程 POC 可先使用通用模型 `deepseek-v4-flash`：它于 2026-04-24 发布，较 Qwen-MT 更新；但它不是专用机器翻译模型，测试结果只能验证“整段提交 → 服务端 → 返回译文”的产品流程，不能替代最终语种质量、术语一致性或成本选型。
- 2026-07-31 从本机查询 OpenRouter 公共模型目录，已存在模型 ID `deepseek/deepseek-v4-flash`。可以用现有 OpenRouter 额度做流程 POC；正式的中国大陆链路、数据边界、限流和成本必须另行以 DeepSeek 直连或最终供应商直连验证，不得以 OpenRouter 结果代替。
- 用 `deepseek-v4-flash` 做纯翻译时，关闭思考模式，并在固定系统提示中要求“仅输出译文、保留段落/占位符/专名、不解释”。首轮整段提交流程可先用非流式返回；需要验证译文逐步呈现时再单独开启 SSE，不能把流式 UI 调试与翻译质量评估混在同一轮。
- `qwen-mt-flash` 和 `qwen-mt-lite` 的流式响应块只包含新生成文本；`qwen-mt-plus`、`qwen-mt-turbo` 每块包含当前完整文本，不能作为实时预览默认模型。`qwen-mt-lite` 仅支持 31 种语言，适合低延迟简单预览；`qwen-mt-plus` 只在用户确认整句后的高质量复译使用。
- 百炼翻译模型的文本接口每次只接收一条待译文本，不支持前缀续写或上下文缓存。因此「边写边译」必须将当前稳定文本快照作为新请求发送，不能把不断增加的按键输入写入同一请求。
- 百炼北京地域当前默认限制：`qwen-mt-flash` 为 60 RPM / 35,000 TPM。实时策略必须做防抖、取消旧请求、版本号拒绝过期结果，并限制每个用户的发起频率；上线前必须通过控制台申请符合活跃用户数的额度，不得按每个按键发请求。
- 备选候选：腾讯混元 `hunyuan-translation`。接口支持 SSE，输入可带 `Field`、`References` 和术语库；默认接口频率为 20 次/秒、账号默认并发 5 路，标准版当前刊例价为输入 1.2 元 / 百万 Token、输出 3.6 元 / 百万 Token。它覆盖泰语、越南语、马来语、印尼语、高棉语、缅甸语等，但官方当前语言列表未列老挝语；若老挝语是首发范围，不能替代 Qwen-MT。
- 不要把阿里 `qwen3-livetranslate-flash` 用作文本输入法翻译：其公开 API 输入为音频或视频。不要接入腾讯云旧 TMT 文本 API；该产品的 `TextTranslate` 等文本接口已在 2026-07-08 删除。
- API 密钥不得嵌入 Rime 配置、Lua 脚本、桌面客户端或仓库。客户端只能访问自有服务；翻译需由用户明确开启，并设计安全输入框/敏感应用的禁用策略。
- 2026-08-02 起输入法客户端不读取或携带 DeepSeek/上游 AI Key，也不读取 Keychain、不发送 Authorization 头；客户端只请求带 48 位 capability 的自有 Go 翻译 API。按用户本轮明确要求，Go 服务直接访问 DeepSeek 官方接口，DeepSeek Key 仅写入服务器 root 所有、权限 600 的 env 文件，不进入仓库、客户端、Rime 配置或应用包；本项目不得启动、停止、改端口或改配置服务器现有的 8317/CLIProxyAPI 服务。
- 2026-08-02 已重新验证用户指定 DeepSeek Key 可直连 `https://api.deepseek.com/chat/completions`，`deepseek-v4-flash` 返回 HTTP 200；该凭据只供服务器 Go API 使用，本机仅保存在 `~/.config/typing-dongnanya/deepseek-api-key` 供部署脚本读取，客户端与构建包不得读取。

## 开发与验证约束

- 先确定首发平台和商业授权模式，再选择系统输入法前端实现；不能因参考 Squirrel/Weasel 而复制 GPL 代码。
- 首版显示的是「草稿译文」，新的稳定输入快照到来后应整体替换上一次草稿；所有自动翻译（包含句末标点）都必须从最后一次输入活动起稳定等待 1 秒后才允许发送，过期请求、主动取消不应显示为失败。
- API 选型不能只看是否支持 SSE：每次新输入都要重新发起完整文本翻译，所以必须同时测试首字节延迟、整句完成延迟、取消后的过期回写、限流、成本和目标语言质量。
- 2026-08-02 已通过 `Tests/RimeSmoke.cc` 验证打包数据可把 `nihao{space}` 提交为 `你好`、`nage{space}` 提交为 `那个`；服务器 Go API 直连 DeepSeek 官方的真实 Chat Completions 请求已返回英文译文，输入法内最终跟随效果仍需 HTTPS 公网入口和用户切换输入法后人工观察。
- 2026-08-01 候选窗定位统一改为优先读取 `IMKTextInput.attributes(forCharacterIndex:lineHeightRectangle:)` 的真实光标矩形，失败时才回退 `firstRect`；候选窗与译文窗共用同一个屏幕布局入口，优先显示在光标下方，空间不足时整体翻转到上方，不再使用固定偏移或屏幕中心兜底。
- 2026-07-31 默认简体中文通过 `Resources/RimeData/luna_pinyin.custom.yaml` 的 `"switches/@2/reset": 1` 固定，Rime 数据增加 `rime-data-version.txt` 版本标记，只有版本变化时重新部署，避免旧用户目录继续使用历史繁简状态。冒烟已验证 `nage -> 那个` 且结果不含繁体 `個`。
- 2026-07-31 输入法菜单图标改为透明背景的单色模板图形；旧版 64px 蓝底白圆在 macOS 菜单模板渲染中表现为白色方块，因此不能继续使用整块彩色背景。
- 2026-08-01 `0.4.0 (4)` 将草稿译文面板重构为“双层轻卡”的辅助层，直接显示本轮实际发送原文、目标语言、加载/结果/失效/具体错误状态；翻译链路按 480ms 防抖、35ms 句末延迟、600 字符上限、任务取消和请求代次拒绝迟到回写，DeepSeek 提示词仍固定为无历史单段翻译并执行最小输出清洗。
- 2026-08-01 已构建并安装测试版本 `0.4.0 (4)`；`bun run scripts/macos/test.ts` 通过，`nihao -> 你好`、`nage -> 那个`，系统包与本地构建可执行文件 SHA-256 一致，ad-hoc 签名验证通过，产物仍不依赖 `/opt/homebrew` 动态库。安装脚本已结束旧进程，用户必须切换到其它输入法再切回 Typing 东南亚启动新版本；真实候选鼠标交互、光标定位和输入法内翻译仍待用户运行截图验收。
- 2026-08-01 `0.4.0 (4)` 不再按每次提交后的 `selectedRange()` 连续位置决定翻译原文是否重置：Rime commit 始终先进入同会话稳定缓冲，客户端文档明确匹配“完整缓冲”时建立可信范围，明确只剩新提交文本时才开启新草稿；请求开始前再次验证完整源文本，并在译文卡显示实际发送内容。“你好看真的 -> Really.”是否已闭环必须以新版本截图中的原文行和译文共同验收。
- 2026-08-01 用户截图确认 `0.4.0 (4)` 在编辑框已有“你很棒 这个商品你多少个”时，译文卡实际只发送“多少个”；根因是请求开始前按当前 Rime 缓冲长度读取客户端文本。`0.4.1 (5)` 已改为向光标前最多读取 600 字符，并从最近的换行或 `。！？!?；;` 后截取完整当前句，句末标点属于本句；翻译和点击替换共用该句的精确文档范围。当前构建与自动测试已通过，系统级安装和新截图闭环仍待完成，不能提前表述为已验收。
- 2026-08-01 边写边译已按“翻译源文本快照”和“可点击替换范围”两个职责实现：客户端不支持读取时仍可用 Rime commit 缓冲翻译，但显示“仅预览”；只有客户端、光标、原文正文和范围均验证通过时才显示并执行“替换原文”，任何一项变化都保留译文并切换为“原文位置已变化”，不能冒险覆盖用户文本。
- 2026-08-01 `0.5.0 (6)` 将候选条与译文卡改为同一套光标跟随布局：候选更新时只重定位已显示译文卡，不重建浮层；整句翻译防抖调整为 820ms，句末快速延迟调整为 180ms。构建、Rime 冒烟、ad-hoc 签名和 `/opt/homebrew` 动态依赖检查通过；系统包版本与本地可执行文件 SHA-256 一致，安装到 `/Library/Input Methods/TypingDongnanya.app`，未自动切换当前输入法。
- 2026-08-02 线上翻译服务使用本地交叉编译的 Go 二进制：Nginx 对外路径为 `/typing-dongnanya-api/`，Go 监听 `127.0.0.1:18127` 并直接访问 DeepSeek 官方；服务器现有 8317/CLIProxyAPI 属于其它用途，本项目不得依赖或修改。访问 capability 由部署脚本自动生成并注入客户端 URL，DeepSeek Key 只保存在服务器 600 权限 env。
- 2026-08-01 `0.4.0 (4)` 已补齐候选壳所需的组合选中范围与光标、候选标签/注释、高亮项、页码和末页状态，并实现鼠标 hover 高亮、点击选词、翻页和点击组合码移动光标；所有动作均回写 librime 后使用完整快照刷新，没有复制 Squirrel / Weasel 代码或视觉资源。
- 2026-08-02 `0.6.1 (9)` 完成候选与译文浮层第一轮手感收敛：候选层不再重复绘制 preedit，按 comment 使用 40px/48px 紧凑高度，单页不预留分页空白，最小宽度为 176px；hover 只作本地视觉反馈，不再回写 librime 高亮。译文层不显示原文、草稿或仅预览按钮，新请求开始立即隐藏旧译文；不可替换状态鼠标穿透，可替换状态整卡点击后仍按客户端/光标/原文范围重新验证。InputMethodKit 控制器使用进程内单活动浮层会话，激活、停用、`hidePalettes` 和跨编辑器接管均清空组合、取消翻译、隐藏旧浮层；并使用 `IsSecureEventInputEnabled()` 在安全输入期间禁用远程翻译。`bun run scripts/macos/test.ts` 通过，`0.6.1 (9)` 已安装到系统目录，本地与系统可执行文件 SHA-256 一致；真实切换、多屏、密码框、浏览器和鼠标截图仍待人工验收，不能提前表述为完成。
- 2026-08-02 Goal 模式完成 100 项候选/译文/生命周期设计逐项审查：其中 28 并入 21，63 与 92 删除；16、17、18、24、31、35、37、38、42、47、59、62、64、65、68、71、77、78、83、93、96、98、100 收敛为以 InputMethodKit、librime 快照/keymap、轻量辅助层和安全替换事务为准的最终口径。`0.6.2 (10)` 修复 Emoji surrogate 的强制解包风险：句子边界改用 composed character sequence，并新增可执行 smoke 覆盖 Emoji、标点、空白、换行；超长且无法确认句首的当前句不再发送截断残句。候选与译文先按可见屏幕宽度约束，垂直空间不足时选择较大一侧；正常译文改为约 36pt 单行内容卡、有限宽度档位，错误/失效才显示状态行。401/402/429/5xx 用户文案不再要求终端用户检查服务器配置。三名只读 Agent 分段审查均未改代码；真实宿主视觉与多应用验收仍待执行。
- 2026-07-31 已完成本机系统级注册验证：`/Library/Input Methods/TypingDongnanya.app` 存在，TIS 返回“已启用、可选择、当前选中”，`com.apple.inputsources` 已记录本项目的 Input Mode 与 Keyboard Input Method，输入法进程已由 `imklaunchagent` 启动。此事实证明必须按“中文/Chinese”手动添加，不应再以搜索产品名的空结果判断安装失败。
- 2026-07-31 已以只读方式核验 Squirrel 当前安装流程与本机 WeType：两者均使用 `/Library/Input Methods`；Squirrel 将系统安装阶段的注册、当前用户的启用和显式选择拆开。项目仅采用该公开的系统生命周期事实，不复制 GPL 源码；本项目测试安装不执行选择。
- `TISInputSource` 的进程内“已启用”状态不能单独作为安装成功依据；安装后先核对系统级包路径与 `--input-source-status`，再由用户在系统设置按“中文/Chinese”手动添加并核对 `com.apple.inputsources` 的第三方来源记录。公开 SDK 未提供 `TISUnregisterInputSource`，迁移旧用户级测试包时只能移除本项目自己的旧包并重新注册系统级包。
- 后续每次确认平台、许可证、模型版本、限流/价格、实测延迟、词库来源、隐私策略、输入文本提取方式或发布策略时，必须在本文件追加或更新结论，并写明核实日期；未验证的假设必须明确标注为未验证。
- 2026-08-02 `0.6.3 (11)` 补上基于 InputMethodKit `menu()` 与 librime runtime API 的最小输入法壳：菜单动态显示当前方案和中/英状态，可切换当前会话的中英、中文/西文标点、半/全角、简/繁字形，并从 librime `default.yaml` 动态读取并选择已打包方案；边写边译可开关，首版目标语言可选英语、泰语、越南语、印尼语、马来语。翻译偏好、已选方案及持久化的 Rime 视觉/标点字形选项仅写本项目 `UserDefaults`；`ascii_mode` 保持会话级，不会自动选择 macOS 系统输入源。已从本机 SDK 头文件和当前 librime 源码确认 `IMKInputController.menu()`、`get_schema_list`、`select_schema`、`set_option`、`get_option` 的职责，未复制 Squirrel/Weasel 代码。Rime 冒烟新增输入方案枚举及运行时 option 断言，用户级设置冒烟新增翻译开关/目标语言/方案/持久化边界；`bun run scripts/macos/test.ts` 通过。系统安装与菜单真实点击验收仍待完成。
- 2026-08-02 尝试通过既有 `bun run scripts/macos/install.ts` 安装 `0.6.3 (11)` 时，非交互终端在 `osascript` 管理员授权阶段无输出等待；已取消，未写入 `/Library/Input Methods/TypingDongnanya.app`。核对后系统包仍为 `0.6.2 (10)`、SHA-256 为 `39d6337c9f051f83c99385396593fd73604c4d20956e26b5d192f2917fd9f6e3`，新构建 `0.6.3 (11)` 的 SHA-256 为 `c4a422cccf6edcac23e8811083a8702cd21c2a8e6b725768b1afc2a1588e7680`；必须在可输入管理员凭据的终端重新运行安装脚本，成功后切到其它输入法再切回，才能实际验收菜单。
- 2026-08-02 本地构建完成 `0.6.4 (12)`：Rime 的 `currentSnapshot` 及 schema/option/翻页/高亮/光标等状态动作不再读取会消费 commit 的 `get_commit`，只有按键、选词与显式 `commitComposition` 读取 commit；两个 `inputText` 未接管入口均收口旧草稿；显式提交同步更新本地 Rime 快照。稳定草稿防抖调整为 1000ms，句末仍为 180ms。翻译地址只从已打包 `Info.plist` 的无 Token 公开路径读取，不再接受隐藏 `UserDefaults` endpoint/model/source 覆盖；无效配置、网络和解码错误均有可行动提示，固定提示明确原文内指令只作为待翻译数据。
- 2026-08-02 `bun run scripts/macos/test.ts` 已通过：无 Token 包端点契约、Rime `nihao -> 你好` / `nage -> 那个`、候选交互与 option、Emoji/句界/安全输入资格、设置持久化边界、Rime bridge 非消费快照与显式提交。`Info.plist` lint、ad-hoc 签名和动态依赖核对通过；`0.6.4 (12)` 本地可执行文件 SHA-256 为 `7c627aea7f663750ef2fa418e9576f8120753368fbe5aa71f89b05d23e915f27`，无 `/opt/homebrew` 动态依赖。系统包仍为 `0.6.2 (10)`，未修改；在线翻译服务和真实宿主 UI 均未因此验收。
- 2026-08-02 完成 `0.6.4 (12)` 的逐文件基础功能复审并再次构建验证：Rime bridge 仅接收 ASCII 可打印键或命名功能键，直接非 ASCII 文本和多字符 `inputText` 不再按 UTF-8 首字节伪造按键，bridge smoke 已覆盖该边界；未调用的 bridge 高亮 API 已删除，候选 hover 保持本地视觉行为。`InputOverlayAnchor` 在没有可信 marked/selected range 时直接隐藏，不会把 `NSNotFound` 交给 `firstRect`；可替换译文卡的整卡 hover 与实际点击范围一致，正常译文的绘制高度可显示最多两行；两个玻璃面板按圆角裁切，单候选面板由 176pt 收敛为 84pt，短译文卡增加 160pt / 220pt 宽度档位，减少无效留白。`bun run scripts/macos/test.ts`、Swift typecheck、Objective-C++ syntax check、`Info.plist` lint 与 ad-hoc 签名均通过；新本地 SHA-256 为 `ea25934cc672b0f7fcf916be3340b540af3ccf914db6a0885710e93cb5bbee37`，仍无 `/opt/homebrew` 动态依赖。系统包仍为未替换的 `0.6.2 (10)`（SHA-256 `39d6337c9f051f83c99385396593fd73604c4d20956e26b5d192f2917fd9f6e3`），本轮未执行管理员安装、未改服务器或远端资源；新包真实 UI、翻译接口和安全输入仍待人工验收。
- 2026-08-02 再次从 Agent TTY 发起 `bun run scripts/macos/install.ts`：macOS 管理员授权阶段在 90 秒内没有可交互输出，已主动取消；随后只读核对系统包仍为 `0.6.2 (10)`、SHA-256 `39d6337c9f051f83c99385396593fd73604c4d20956e26b5d192f2917fd9f6e3`，无残留安装进程。该执行环境无法代替用户完成系统管理员认证；必须从可见的本机 Terminal 运行安装脚本并完成授权，才可以同步新包，不能把本地 `0.6.4 (12)` 的构建通过表述为系统包已升级。
- 2026-08-02 继续收口浮层定位：部分 `IMKTextInput` 在 `attributes(forCharacterIndex:lineHeightRectangle:)` 无定位信息时会返回完整 `.zero` 矩形，不能当成真实屏幕左下角光标；`InputOverlayAnchor` 现在把该哨兵改走 `firstRect` fallback，仍然保留单一维度为零但其余坐标有效的真实 line rect。新增 `Tests/OverlayLayoutSmoke.swift` 的完整 `IMKTextInput` 替身，覆盖 attributes 优先、`.zero` fallback、缺少 marked/selected range 时不调用 `firstRect`、候选/译文 panel 屏幕内夹取；并把 smoke 接入 `bun run scripts/macos/test.ts`。完整测试、Swift typecheck、`Info.plist` lint、ad-hoc 签名和动态依赖核对均通过；本地 `0.6.4 (12)` 新 SHA-256 为 `9ba2e29ac395a5f1bb0248caa1268f914278b4628a4233a5011c6b61f9c7cfd1`。系统包仍为未替换的 `0.6.2 (10)`，真实宿主视觉与翻译接口没有据此验收。

- 2026-08-02 根据用户图 1–6 修复四项新包问题并构建 `0.6.5 (13)`：候选窗曾把宿主全文 `selectedRange.location` 作为 `attributes(forCharacterIndex:lineHeightRectangle:)` 的 index，导致右侧聊天框等宿主可能返回无效矩形并把候选条落到屏幕左下角；现在组合态只传 librime preedit 内的 `caretOffset`，`.zero` 时用 `markedRange.location + caretOffset` 调 `firstRect`，非组合态才用 selectedRange。`Tests/OverlayLayoutSmoke.swift` 覆盖全文 selectedRange、组合 caret、fallback 范围和缺失范围。现有部署端点是明文 `http://114.132.185.123/typing-dongnanya-api/v1/chat/completions`，图 1/3 的 ATS 错误已用仅该 IP 的 `NSExceptionAllowsInsecureHTTPLoads` 放通；测试同时强制 HTTP 时 `NSExceptionDomains` 只能保留该一个 IP，且严禁加入 `NSAllowsArbitraryLoads`；在服务端提供可核验 HTTPS 前不得称为安全链路。旧 `TypingDongnanya.tiff` 已移除，替换为本项目自创透明单色 `TypingDongnanyaMenuIcon.pdf`，根、menu、palette、alternate-menu 图标键统一引用，不能复制第三方输入法视觉资源。`bun run scripts/macos/test.ts`、签名、Info/PDF 与动态依赖核对通过；本地 SHA-256 为 `8c554547ac5efb19cf358003c7feeadbcfa96bd0750f169c05f67f5cea2830b3`。系统包仍需用户在可见 Terminal 完成管理员安装、切换到其它输入法再切回后，以图 6 的紧贴组字光标单层候选条为基准进行真实截图验收。

- 2026-08-02 根据用户图 1–4 复测，系统包实际为 `0.6.5 (13)`、SHA-256 `8c554547ac5efb19cf358003c7feeadbcfa96bd0750f169c05f67f5cea2830b3`，且输入源当前选中，问题不是旧包未安装。`0.6.6 (14)` 本地修复：候选定位改为优先 `firstRect(forCharacterRange:actualRange:)` 的屏幕坐标，只有它无效时才回退 Electron/宿主的 attributes；未换行的非零 attributes 不能视为可信坐标。输入源图标替换为自创的高对比 `TypingDongnanyaMenuIconV3.pdf` 并更换四个图标键文件名，以避开 TIS 缓存和深色菜单下的不可辨细黑轮廓。部署脚本生成的 48 位 `access-token` 过去从未被构建脚本消费，导致客户端请求无 capability 的路径；现在源 `Info.plist` 只保留基础地址，构建期仅从 `~/.config/typing-dongnanya/access-token` 读取并写入包内完整路径，禁止使用环境变量、禁止写入上游 `CLIPROXY_API_KEY` 或源码。该 capability 不是上游 AI Key，但可从桌面包中提取，必须继续保持 Go 代理的常量时间校验、30 RPM 每 IP 限流与及时轮换。
- 2026-08-02 腾讯服务器已签发 Let’s Encrypt 公网 IP 证书，SAN 为 `114.132.185.123`，有效期至 2026-08-09，并启用每 6 小时检查一次的 `certbot-renew.timer`；续期 dry-run 已成功。公网 TCP 443 放行后，Mac 访问 HTTPS health 返回 200，HTTP 入口 308 跳转 HTTPS，缺失或错误 capability 均返回 404，真实 completion 返回 HTTP 200 和有效译文。

- 2026-08-02 按用户明确要求已从腾讯服务器卸载 CLIProxyAPI：删除 `cliproxyapi.service`、override、二进制、配置目录、工作目录、专用用户组及本项目备份中的 CLIProxy 配置；8317、18317、18318 均无监听。`mihomo.service`、Nginx 和 `typing-dongnanya-api.service` 保持 active，本项目后续不得重新引入 CLIProxyAPI。
- 2026-08-02 已构建并系统安装 `0.6.7 (15)`：客户端端点为 `https://114.132.185.123/typing-dongnanya-api/v1/chat/completions/<capability>`，源代码、构建包和系统包均不含 DeepSeek Key；`bun run scripts/macos/test.ts` 全部通过，系统包与构建包可执行文件 SHA-256 均为 `f65871a1da0edd1a9f180965da8f56aed5ef21b360de9d73a80b79eef70b4143`，ad-hoc 签名有效且无 Homebrew 动态依赖。安装脚本未自动切换输入法，用户必须切换到其它输入法再切回 Typing 东南亚，真实候选定位、图标和边写边译仍以用户截图为最终验收。

- 2026-08-02 用户截图再次确认 0.6.7 (15) 的候选条与译文卡分别被夹到屏幕左下/左上角。根因不是间距参数，而是定位主链把“有限、非零”误当可信坐标，并在矩形不属于任何屏幕时回退 NSScreen.main 后夹取；异步翻译又在不同阶段重复查询，导致两层使用不同错误锚点。0.6.8 (16) 将浮层定位收口为 IMKTextInput.attributes(forCharacterIndex: 0, lineHeightRectangle:) 单一入口，完全移除 selectedRange、markedRange、librime caret 和 firstRect 对 UI 坐标的参与；无效或离屏矩形直接隐藏，不再回退主屏角落。翻译请求与 UI 锚点解耦：调度时捕获同客户端锚点，网络照常执行，返回时优先刷新当前锚点、失败则使用提交时锚点，两者都无效才记录受控日志。OverlayLayout smoke 已覆盖固定 index 0、firstRect 零调用、范围缺失、零值/NaN/Infinity/负尺寸/离屏拒绝和候选/译文屏幕内夹取；真实 ChatGPT/TextEdit/浏览器/副屏定位仍必须以安装后截图验收。
- 2026-08-02 已完成 0.6.8 (16) 最终构建与系统安装：bun run scripts/macos/test.ts 全部通过，构建包与 /Library/Input Methods/TypingDongnanya.app 可执行文件 SHA-256 均为 `931e553ac6351d1df708e9454f33f1940aa62bc4d206ca95e13f984d1de8b912`，两者 ad-hoc 签名有效，不依赖 /opt/homebrew 动态库；系统包端点仍是 HTTPS 固定代理路径并带 48 位 capability，源码、构建包与系统包的上游 Key 模式扫描结果为 0，公网 health 返回 HTTP 200。安装脚本只注册并退出旧进程，状态为已启用、可选择、当前未选中，未自动切换其它输入法；用户必须切到 Typing 东南亚后在真实输入框重新截图，才能判断定位是否闭环。
- 2026-08-02 用户截图确认 0.6.8 系统包的输入源菜单图标仍显示为白色圆角方块。已核实 V3 PDF 虽标称模板图形，但实际包含覆盖 1–15pt 范围的不透明青绿色圆角背景；macOS 在选中菜单项时按 alpha 蒙版统一染白，背景与内部箭头一起形成实心白块，问题不是单纯的 TIS 缓存。0.6.9 (17) 删除 V3 源资源并改用 TypingDongnanyaMenuIconV4.pdf：透明 16×16 页面只绘制单色双向翻译箭头，根、menu、palette、alternate-menu 四个图标键统一更名引用；新增 scripts/macos/generate-menu-icon.ts 作为确定性生成入口和 Tests/MenuIconSmoke.swift 像素级门禁，强制四角透明、稀疏 alpha mask、单色与有效包围盒。bun run scripts/macos/test.ts 全部通过，本地构建主程序 SHA-256 为 `6e32cf79d375f0f7be190180fd0a67479ea0aa43657dc543f3753a2d13f16689`；系统安装因管理员授权窗口未完成而已取消，系统包仍为 0.6.8 (16)，不能称为真实菜单验收通过。
- 2026-08-02 继续核对 0.6.9 图标未变化问题：系统包与构建包均为 0.6.9 (17)，主程序和 `TypingDongnanyaMenuIconV4.pdf` 的 SHA-256 分别一致，系统包四个图标键均引用 V4，当前输入法进程也从系统 V4 包重新启动；TIS 当前的 `kTISPropertyIconImageURL` 已明确指向系统包内 V4，因此已排除旧包未安装、旧输入法进程和旧 Info.plist。剩余最可能是负责绘制输入源菜单的 `TextInputMenuAgent` 持有按输入源 ID 缓存的旧图像，已只重启该用户级进程并确认 PID 更新、TIS 仍返回 V4 URL；真实菜单是否刷新必须以用户重新展开后的截图确认，不能提前称为闭环。`scripts/macos/install.ts` 已补充安装后重启 `TextInputMenuAgent`，不重启 SystemUIServer、不切换输入法。
- 2026-08-03 用户截图确认正常译文辅助层采用「只显示译文正文 + 右侧轻量替换入口」的紧凑单层方向可继续使用。`0.6.10 (18)` 将所有自动翻译统一为最后一次输入活动后稳定等待 1000ms，句末标点不再走 180ms 快速请求；Rime 组合码仍在变化时只取消旧任务和旧译文、不清空已提交草稿，组合结束后重新计时。请求开始前若当前文档原文与调度快照不同，会按新原文重新计满 1 秒；返回后再同时校验 generation、当前客户端、组合态和当前句正文，旧回复不得覆盖或展示为最终文本。译文卡宽度改为 160/220/320/420/560/720/960pt 有限档位并封顶 960pt，避免长句直接扩到整屏；整卡替换改在鼠标抬起时提交并支持拖出取消。`bun run scripts/macos/test.ts`、Go 测试、Swift 语法、plist、签名和动态依赖核对均通过，最终构建可执行文件 SHA-256 为 `9dee7c06ee0638dafb92ec0fd542236703c1312a46f0e34439ef5c3384b7312e`；最终系统包覆盖仍在等待 macOS 管理员授权，不能把当前系统包的同版本旧二进制写成已同步。
- 2026-08-03 `0.6.11 (19)` 针对用户新截图收口四项问题：注册页改用独立彩色应用图标并将模式副标题改为“简体拼音 · 边写边译”；候选条尾部与输入法菜单新增设置入口，原生设置窗口可直接修改目标语言及首版基础输入选项；无组合态空格由宿主直接插入时只重算 1000ms 稳定期，不再清空已经提交的整句草稿；长译文由最多约两行放宽到最多约四行后再缩略。完整 macOS smoke 与 Go 测试已通过，系统包已安装为 `0.6.11 (19)`，构建包和系统包主程序 SHA-256 均为 `ebe8b4d2f6a15a37dc08b1485c72a0d98098591abfe2c1f3c054601673e15bd4`，两类图标资源哈希一致、ad-hoc 签名有效、无 Homebrew 动态依赖和上游 Key；安装未自动切换输入法，当前 TIS 状态为已启用、可选择、当前未选中。真实系统注册页、候选设置按钮、设置窗口交互与空格后网络请求仍需用户切回输入法后人工验收。
- 2026-08-03 `0.6.12 (20)` 重构宿主文本变更触发链：旧实现遇到 Command 快捷键、多字符 `inputText`、未被 Rime 接管的删除/直输时会直接清空草稿，且宿主完成粘贴、剪切、撤销、重做后没有回读入口，因此文本已变更也不会翻译。新实现保留 Command 事件给宿主，只对 `V/X/Z/K`、删除键、未处理可打印字符/空格/换行建立编辑前局部文档探针；宿主落字后以文档长度与重叠正文确认真实变化，再从当前客户端重建整句与替换范围，并从该次最终变化重新等待 1000ms。`recognizedEvents` 同时接收左右鼠标按下/松开，参考 Gureum 公开的 InputMethodKit 事件范围但未复制其代码，使右键菜单粘贴可在鼠标松开后短时观察；单纯复制、移动光标或选区不发请求。请求开始与返回仍校验 generation、当前客户端、组合态和最终句正文。新增 smoke 覆盖键盘粘贴、等长替换、撤销后重建、光标移动不误触发和命令分类；完整 `bun run scripts/macos/test.ts` 已通过。构建包版本为 `0.6.12 (20)`，主程序 SHA-256 为 `4c687ef2e82c9d5346696d7ddc8580f0fe38db8529258a633076dc29e50c2127`；系统包覆盖仍等待管理员授权完成，未完成前 `/Library/Input Methods` 仍是 `0.6.11 (19)`，不能称为已安装验收。
- 2026-08-03 若 Swift/Clang 报告 SDK 头文件在 PCH 建立后尺寸变化，应使用本轮独立的 `CLANG_MODULE_CACHE_PATH` / `SWIFT_MODULE_CACHE_PATH` 重跑现有构建测试脚本；该错误来自本机旧模块缓存，不应删除 SDK 文件或误判为业务代码编译失败。
- 2026-08-03 用户明确调整复制语义后发布 `0.6.13 (21)`：所有已识别的宿主正文变化仍按最终文档快照重新等待 1000ms；`Command-C` 另外视为明确翻译动作，有选区时翻译选中文字且不开放点击替换，无选区时重新读取光标前当前整句。这样复制会触发翻译，而粘贴、剪切、撤销、删除仍必须等宿主真正改完正文后再发送，不能把操作名代替最终文本校验。`bun run scripts/macos/test.ts` 全部通过，系统包已安装为 `0.6.13 (21)`；构建包与 `/Library/Input Methods/TypingDongnanya.app` 主程序 SHA-256 均为 `ce050069a23a8a323b6c3786f83ee2d5c9d24ee96d381ec63cd2a84d41470a7d`，两者签名验证通过且无 `/opt/homebrew` 动态依赖。安装没有自动切换输入法，TIS 状态为已启用、可选择、当前未选中；真实宿主中的复制、粘贴及连续编辑仍需用户切回 Typing 东南亚后人工验收。
- 2026-08-03 用户在 Codex/ChatGPT 类宿主中连续复制同一文本仍无翻译 UI，确认 `0.6.13 (21)` 的缺陷是把复制触发绑定在 `IMKInputController.handle` 的 `Command-C` 事件；宿主应用会直接消费复制命令，输入法未必收到该按键，因此不是 UI 缓存问题。`0.6.14 (22)` 改为只在 Typing 东南亚输入源激活期间每 120ms 检查 `NSPasteboard.changeCount`，代次未变化时不读取内容；代次变化且当前不是安全输入时才读取最终字符串，并以只读翻译源重新等待 1000ms。相同字符串连续复制会因新的 changeCount 各触发一次，复制文本不开放点击替换；输入源切出或控制器销毁时立即停止监听。独立 NSPasteboard 回归已验证同一文本连续写入两次产生两个可消费代次，完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.14 (22)`，构建包和系统包主程序 SHA-256 均为 `d335b613456d50adf658c4756acba39ba272245d98397c090d11d7c1a9a71d4a`，签名有效且无 `/opt/homebrew` 动态依赖；安装后当前输入源仍是未选中，必须人工切回 Typing 东南亚启动新进程后再做真实宿主验收。
- 2026-08-03 用户截图确认 `0.6.14 (22)` 已显示译文 UI，但复制长句后仍只得到手动输入末尾“你好”的译文 `Hello.`。根因是剪贴板监听已经触发成功，`synchronizeCopiedText` 也保存了复制正文，但请求开始前和返回后共用的 `resolvedSnapshot` 无条件重新读取当前 IMK 文档，把复制源覆盖成输入框当前句。`0.6.15 (23)` 新增明确的 `TranslationSourceKind`：文档源继续在发送前重建最终整句，复制源固定使用对应剪贴板快照且保持只读；复制后继续手动输入时先清空复制草稿并切回文档源，避免两类正文串接。新增 smoke 直接以“输入框为你好、剪贴板为今天天气挺好”验证两次解析仍保持复制文本，并验证后续手动输入恢复文档源；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.15 (23)`，构建包与系统包主程序 SHA-256 均为 `5d99253aed5b1beb5c3d23c94c2012a01f8bc960a11aaa385db3478558b31d8a`，签名有效且无 `/opt/homebrew` 动态依赖；安装后当前输入源仍未选中，必须人工切回后再做真实复制验收。
- 2026-08-03 用户以空输入框直接粘贴“你好啊。 你好啊。 你好啊。”仍无 UI，确认前五轮回归只验证了“IMK 已收到快捷键/剪贴板回调后的状态”，没有覆盖宿主完全吞掉 `Command-V`、输入法尚未收到任何键盘事件的真实路径；旧 `activateServer` 又主动清空 `lastClient`，因此文档观察器没有客户端可读，现有 smoke 会误报通过。`0.6.16 (24)` 改为从 `IMKInputController` 初始化参数及 `client()` 立即绑定该输入会话，激活期间每 120ms 同时观察剪贴板代次和有限文档快照；即使没有任何 IMK 按键回调，只要宿主正文真实变化就重新等待 1000ms。若变化严格匹配当前剪贴板插入结果，则固定实际插入范围并翻译完整粘贴文本，多句内容不再缩成最后一句；连续粘贴相同剪贴板也按每次文档变化逐次触发。新增失败优先回归直接模拟“无 IMK 回调 → 粘贴三段你好啊 → 完整源文本 → 同剪贴板再次粘贴 → 不重复消费静止文档”，并保留等长替换、撤销、光标移动、复制源隔离等既有用例；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.16 (24)`，构建包与系统包主程序 SHA-256 均为 `6f4166547fadaa5fa153ed10187d5b3c01295ad88f28fb4efbac3751e05f8143`，签名有效且无 `/opt/homebrew` 动态依赖；安装未自动切换输入法，TIS 为已启用、可选择、当前未选中，仍须用户切回后做真实宿主验收。
- 2026-08-03 用户在没有候选 UI 时复制/粘贴仍无译文，统一日志已确认 `0.6.16 (24)` 的会话内 Timer 实际仍每 120ms 调用 IMK 文档读取，因此“UI 未显示就完全没有监听”不是完整根因。进一步对照 Apple InputMethodKit 生命周期、Gureum 的初始化客户端持有方式、Squirrel 的进程级 ApplicationDelegate 状态管理及 Easydict 的全局事件/AX 方案后，确认剪贴板生命周期不能继续归属按输入会话创建的 `IMKInputController`；本轮未引入 Easydict 式全局键盘事件或 Accessibility 权限，只把无需权限的 `NSPasteboard.changeCount` 监听上移到输入法主进程。`0.6.17 (25)` 从 `main.swift` 启动唯一进程级 `TranslationClipboardMonitor`，复制发生时即使没有活跃控制器也保留 5 秒未消费快照；随后有效文本会话建立且存在真实光标锚点时再交付，已交付代次只消费一次，相同文本下一次复制仍按新代次触发。会话内 Timer 现在只观察宿主文档变化，不再重复读取剪贴板；安全输入、翻译关闭或其它输入源当前激活时不读取正文。
- 2026-08-03 同轮发现旧 `--input-source-status` 的“当前选中”结论不可信：从 TIS 来源列表读取 Typing 模式的 `kTISPropertyInputSourceIsSelected` 曾返回 `true`，但 `TISCopyCurrentKeyboardInputSource()` 实际为微信输入法 `com.tencent.inputmethod.wetype.pinyin`。状态与进程级监听资格现统一改用真实当前键盘输入源 ID 精确比较，并新增 `InputSourceRegistrationSmoke` 防止再次用列表缓存冒充当前来源。完整 `bun run scripts/macos/test.ts` 通过；系统包已安装为 `0.6.17 (25)`，构建包与系统包主程序 SHA-256 均为 `619c1b9ce7e5855ae18193e0b24ebc6886a9004c0b2c8f50d728470a86db1639`，签名有效且无 `/opt/homebrew` 动态依赖。安装后真实当前键盘输入源仍是微信输入法，Typing 东南亚明确为当前未选中且进程未运行，必须由用户手动切换到 Typing 东南亚后再验收，禁止自动选择。
- 2026-08-03 用户截图确认 `0.6.17 (25)` 的正常译文卡会被长英文撑到约 `720px`，呈现为横跨编辑区的单行灰色工具条；根因是布局允许 `960px` 最大宽度并包含 `560/720/960` 宽度档位，同时输入法独立进程没有固定深色外观。`0.6.18 (26)` 将译文卡最大宽度收至 `420px`，仅保留 `160/200/280/360/420` 紧凑档位，长译文在最多约三行内换行，正文降为 13pt 常规字重，并固定深色 HUD 外观、轻边框和紧凑替换入口；翻译触发、文档观察和服务链路未改。新增 `TranslationOverlaySmoke` 验证长句宽度上限、多行高度、短句紧凑尺寸和窄屏约束，完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.18 (26)`，构建包与系统包主程序 SHA-256 均为 `2efbd6509e6c445e05dd52c94ab2562e3087772fe2fad9264f3f4c3c66f81eda`，签名有效且无 `/opt/homebrew` 动态依赖；安装未自动切换输入法，真实宿主视觉仍须用户切回 Typing 东南亚后截图验收。
- 2026-08-03 用户截图确认 `0.6.18 (26)` 在宿主已回车提交并清空输入框后仍显示旧译文卡。失败优先回归确认两个根因：已验证的文档源在当前句读取失败时仍会退回旧快照，使迟到响应重新显示已删除原文；鼠标发送路径在 mouse-up 后才建立正文基线，可能错过点击发送同步清空。`0.6.19 (27)` 统一收口提交生命周期：无组合态 Return、数字键盘 Enter 与 Command-Return 在交还宿主前立即撤销旧译文；鼠标按下前保存文档基线并覆盖点击发送、右键粘贴等后续编辑；120ms 文档轮询持续验证已显示译文的客户端、原文范围和光标事务；验证过的原文一旦从宿主消失，发送前和返回后都禁止退回旧快照。新增“提交清空 → 文档变化 → 旧快照必须失效”的失败回归，并补回车有无组合态策略断言，完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.19 (27)`，构建包与系统包主程序 SHA-256 均为 `c7d8eb31dbb5675ec8d772b6d9eb8ff36c7e3724def0b321acf561b86533e7b4`，签名有效且无 `/opt/homebrew` 动态依赖；安装后真实当前键盘输入源为 Typing 东南亚且新进程已启动，回车提交、点击发送和迟到响应仍须用户在真实宿主中最终验收。
- 2026-08-03 用户再次反馈 `0.6.19 (27)` 复制粘贴不触发 UI；本机统一日志核实该轮粘贴并非未请求：21:08:05 发起 HTTPS 请求，21:08:06 返回 HTTP 200。代码复查确认真正竞态有两处：按键短时观察通常先于 120ms 文档轮询消费同一次粘贴，却只调用当前句同步，导致多句粘贴缩成最后一句；文档轮询先发现旧译文范围失效时会 `reset` 后直接返回，已经消费到的粘贴变化不会再次出现。此前进程级 `TranslationClipboardMonitor` 只观察“复制”而不是宿主实际“粘贴”，还会提前创建独立翻译状态，违背“目标输入法激活后仅按正文变化工作”的当前边界。`0.6.20 (28)` 已删除该额外监听及其 pasteboard 代次/待消费状态，`Command-V` 短时观察和无 IMK 回调的定时轮询统一构造 `TranslationDocumentChange` 并进入 `synchronizeDocumentChange`；匹配当前剪贴板插入时固定完整 `documentRange`，否则再读取当前句；旧译文失效与当前文档变化同时发生时先处理变化，不再丢事件。新增统一多句静默/重复粘贴回归，并增加不记录正文的“文档变化来源与字符数、请求开始、响应收到、UI 显示”阶段日志；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.20 (28)`，构建包与系统包主程序 SHA-256 均为 `6e9aca189df15527c2d5e5d7ee0b69a521cec1c107445c76a055ca90b4ca3c1a`，签名有效且无 `/opt/homebrew` 动态依赖，当前新进程已启动；真实复制粘贴四阶段日志仍待用户本轮重试后核对。
- 2026-08-03 用户在 `0.6.20 (28)` 当前输入源明确为 Typing 东南亚时完成真实多句粘贴，但统一日志没有文档变化、请求开始或网络请求，证明本轮断点发生在文档监控资格而非 API/UI。代码复查确认 `hidePalettes()` 仅表示系统隐藏输入法面板，却错误清空了 `activeOverlayController`；粘贴被宿主直接消费、没有后续 IMK 按键回调时，120ms 文档监控会持续被资格 guard 拦截。`0.6.21 (29)` 保留隐藏面板时的 UI/异步状态收口，但只在真正 `deactivateServer` 时释放输入会话所有权；新增生命周期回归，禁止 `hidePalettes` 再清空当前控制器。完整 `bun run scripts/macos/test.ts` 通过，系统包已安装为 `0.6.21 (29)`，构建包与系统包主程序 SHA-256 均为 `37824f25254b21e399647561451d09394fec73c9520355e5ac4c1ac99219d3b3`，签名有效且无 `/opt/homebrew` 动态依赖，当前新进程 PID `18205` 已启动；真实静默粘贴仍须用户本轮最终验收。
- 2026-08-03 `0.7.0 (30)` 将宿主直接粘贴补齐为同一输入法进程内的 Accessibility 观察链：仅在 Typing 东南亚为当前输入源时，对当前前台应用注册 `AXFocusedUIElementChanged`、`AXValueChanged`、`AXSelectedTextChanged` 与元素销毁通知，并以 180ms 有限探针兼容通知不可靠的宿主；明确排除 `AXSecureTextField`，只读取光标前有限正文且日志只记录来源和字符数。粘贴必须同时满足“剪贴板正文出现在旧选区、最终光标位置匹配、文档长度变化匹配、局部正文一致”，并容忍宿主把选区与正文变化拆成两次 AX 通知；请求开始、响应返回、浮层持续显示和点击替换前均重新核对同一元素、原文范围与最终光标，继续复用 1 秒防抖、请求代次和迟到响应拒绝。新增辅助功能权限状态/请求 CLI、安装期权限引导、Info.plist 用途说明及静默粘贴、重复粘贴、覆盖选区、首尾空白、分段通知和非粘贴回归；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.7.0 (30)`，构建包与系统包主程序 SHA-256 均为 `a04d65540eed89ad6f219dc0764c65c21a97a0d17e0b34259fa06b10cf196124`，签名有效、无 `/opt/homebrew` 动态依赖，辅助功能权限为 `true`，当前新进程 PID `55137` 已启动；真实宿主中“不补键粘贴后等待约 2 秒”的 UI 与四阶段日志仍须用户本轮最终验收。
- 2026-08-04 为排除继续按现象猜测，已固定只读对照的输入法前端版本：Gureum `501693a59e8d8d49b92e443c9d9e0bd9adcea26e`（BSD-2-Clause）在控制器初始化时保存 `inputClient`；Squirrel `8418c95c3245e1e0bda578ac054025c5bfaccb7f`（GPL-3.0）在初始化、激活和按键入口持续补全弱引用客户端；azooKey Desktop `3ae5a4651c329d48fee9b9ec7ac1bcd60b940a12`（MIT）长期使用初始化客户端，并在宿主无法直接回写时使用剪贴板与 CGEvent；macSKK `398c1c763e9e8d27b6042022495013a5a8bafc25`（GPL-3.0-or-later）同样在初始化阶段捕获客户端。当前项目只采用“客户端生命周期不能依赖后续按键回调”的行为结论，未复制 GPL 代码、视觉资源或二进制。
- 2026-08-04 对 `0.8.0 (35)` 的真实 InputMethodKit 进程日志核实：`sh.paseo.desktop` 粘贴时，TCC 对 `kTCCServiceListenEvent` 和 `kTCCServiceAccessibility` 均返回拒绝，因此 Command-V 未进入进程级监听，API 与翻译 UI 均不会启动；安装脚本从终端运行 `--request-accessibility` 得到的结果属于错误的进程归属，不能代表输入法宿主进程。`0.8.1 (36)` 改为输入监控授权成功后才注册全局键盘监听，启动、切换输入源和安装阶段均不主动打开系统隐私页；首次启用只展示输入法内“权限”引导，逐项说明输入监控与辅助功能用途、未授权影响和真实状态，只有用户点击“去开启”后才请求权限并进入对应系统设置。完整 `bun run scripts/macos/test.ts` 已通过；当前只完成本地构建，系统级安装与真实 Command-V 验收仍待后续执行。
- 2026-08-04 候选条设置入口与半/全角交互按成熟前端重新收口：azooKey Desktop 当前版本由 `AppDelegate` 持有并复用唯一 `ConfigWindow`，macSKK 通过统一 AppKit/SwiftUI Settings 生命周期打开唯一设置窗口；本项目采用同样的“应用级窗口所有权”行为结论，新增 `TypingDongnanyaApplicationDelegate` 管理唯一设置窗口，不复制其代码或视觉资源。候选条尾部删除手绘“⌄ + ⚙︎”双字符，改为一个真实 `NSButton` 与 SF Symbols `gearshape`，具备独立点击区、悬停、工具提示和辅助功能描述；点击先隐藏候选层，再由应用级入口打开或前置 700×500 的翻译/输入/权限三页设置窗口。Rime Prelude 默认 `default.yaml` 只加载 `numbered_mode_switch`，未默认启用 `Shift+Space`；本项目 `default.custom.yaml` 明确追加 `Shift+Space -> full_shape` 与 `Control+Period -> ascii_punct`，并把空格键作为 Rime 命名键 `space` 传递，修复此前 Shift+Space 被普通可打印空格分支绕过的问题。半/全角和中/西文标点继续作为两个独立 Rime option，设置页分卡说明，切换后用独立短时状态层显示“半角字符/全角字符/中文标点/西文标点”；Rime 数据版本提升到 `3`。`0.9.0 (37)` 完整 `bun run scripts/macos/test.ts` 已通过，包括真实 Shift-Space option 切换、状态文案、单齿轮与唯一设置窗口源码契约；系统包与构建包主程序 SHA-256 均为 `b04346e5a0774afa95e3ebc8f9160da1d5b4e398db1d729cfde9d6cf340ab45f`，签名有效、无 `/opt/homebrew` 动态依赖，系统级安装已完成但真实设置按钮点击和视觉仍待用户切回输入法验收。
- 2026-08-04 `0.9.1 (38)` 对 `0.9.0` 的候选条、设置、权限、模式提示和粘贴链做了全功能 P0/P1 收口：候选尾部用独立几何布局为页码、上一页、下一页、`8pt + 1pt 分隔线 + 7pt` 分组间距和单个 28×28 齿轮按钮分别预留宽度，新增 `CandidateBarLayoutSmoke` 防止页码侵入候选或分页与设置再次合并；设置窗口改用标准 `.normal` 层级，失去当前控制器时仍保存 Rime 选项，已授权权限隐藏无效“去开启”按钮，首次引导只在窗口即将真实展示时标记为已读。Command-V 兜底只在 IMK 正文已验证实际插入剪贴板内容时发送，验证失败不再降级为只读剪贴板翻译；Accessibility 请求开始、响应回写、点击替换前的精确正文探针和替换后的 AX 基线刷新均改为后台串行队列异步执行，避免 `0.18s` 跨进程超时阻塞输入法主线程。Shift-Space 与 Control-Period 产生的半/全角和中西标点变化会同步持久化，真实 Rime Bridge 冒烟已覆盖两组快捷键；非组字模式提示同时避让已有译文卡。完整 `bun run scripts/macos/test.ts` 无 Swift 警告并通过，系统包与构建包主程序 SHA-256 均为 `2d77c940c72ec476c10c31515c5a0125c856d26446774ca53c443f0a9784fb0d`，签名有效、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；系统级安装已完成，当前系统输入源仍为微信输入法且本项目进程未启动，必须由用户切回 Typing 东南亚后验收真实候选视觉、齿轮点击、设置窗口再次打开、TCC 授权返回刷新和真实宿主粘贴，不能以几何/源码测试表述为视觉已通过。
- 2026-08-04 `0.9.2 (39)` 纠正 `0.8.1` 把“输入监控 + 辅助功能”同时当成必需条件的权限模型：当前 Command-V 观察使用 `NSEvent.addGlobalMonitorForEvents`，而粘贴正文核对、返回前复验和点击替换本身都依赖 Accessibility，因此统一以当前输入法进程的 `AXIsProcessTrusted()` 作为单一授权状态，不再用 `CGPreflightListenEventAccess()` 产生“系统已授权但设置页仍显示缺失”的假状态。设置页会在初始化、每次显示、窗口重新获得焦点及可见期间每秒刷新，授权后隐藏“去开启”；进程级 Command-V 监听和 AX 文档观察分别在 1 秒、2 秒内重新检测授权，事件监听创建失败会明确记录 `available: false`。粘贴链自检确认三条入口均只在宿主正文真实变化后调度：IMK/文档轮询核对前后光标与实际插入范围，AX 核对同一进程、同一焦点元素、文档长度、最终光标和实际正文，Command-V 兜底在 450ms 后核对光标前完整剪贴板文本；多句粘贴、相同剪贴板连续粘贴、覆盖选区、首尾空白、AX 分段通知、单纯复制/移动光标不误判均有 smoke 覆盖。所有自动路径继续统一等待正文稳定 1000ms，请求开始和返回都按 generation、当前客户端、最终原文与替换范围拒绝迟到结果。完整 `bun run scripts/macos/test.ts` 已通过，系统包与构建包主程序 SHA-256 均为 `93c08a5260293d70dc578b178303f75db0f02cbcdd652585a53799194727cb10`，签名有效、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；系统包已安装，安装包 CLI 当前返回辅助功能 `true`，但当前输入源仍为微信输入法，真实 InputMethodKit 进程权限、设置页状态和宿主粘贴 UI 必须由用户切回 Typing 东南亚后最终验收，不能把 CLI 结果单独当成真实进程验收。
- 2026-08-04 用户截图最终确认系统“辅助功能”列表中 `TypingDongnanya` 开关已开启，但 `0.9.2 (39)` 设置页仍显示“未开启”；当时 TIS 已明确选中本输入法，实际进程 PID `33086` 也从 `/Library/Input Methods/TypingDongnanya.app` 启动，因此排除未初始化、旧进程映射和单纯窗口刷新问题。根因是测试包长期使用默认 ad-hoc 签名，其 designated requirement 为每次构建变化的 `cdhash`，系统设置保留的同名旧授权记录不能代表新二进制身份。`0.9.3 (40)` 的本地开发构建改为显式稳定要求 `designated => identifier "com.caizhichao.typing-dongnanya.inputmethod.TypingDongnanya"`，并在 `scripts/macos/test.ts` 强制校验不得退回 `cdhash`；该弱要求只允许用于当前机器迭代测试，正式分发必须改用真实 Developer ID/受信代码签名，不得把 identifier-only 要求作为生产安全方案。已重新安装系统包，并用 `tccutil reset Accessibility com.caizhichao.typing-dongnanya.inputmethod.TypingDongnanya` 仅清理本项目旧辅助功能记录，用户需要在当前稳定身份下重新授权一次，后续同一开发要求的代码更新不应再因 CDHash 改变重复丢权。完整 `bun run scripts/macos/test.ts` 通过，系统包与构建包主程序 SHA-256 均为 `d6b5e18d4b65c4ccbbc853f65a47c14f738ff2d4a9afe37582f622573dad5d19`，签名与 designated requirement 验证通过、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；当前系统输入源为微信输入法且本项目进程未运行，必须切回 Typing 东南亚、点击一次“去开启”并完成真实进程授权后再验收权限状态和粘贴翻译。
- 2026-08-04 用户在权限重新授权后截图确认复制粘贴已经触发翻译，但粘贴 `你好。你好。你好。` 最终只显示 `Hello.`。`0.9.4 (41)` 定位为客户端粘贴竞态：旧 Command-V 兜底固定等待 450ms，350ms 文档轮询可能先按当前末句调度并占用请求代次，完整粘贴兜底随后又因已有任务提前退出，导致完整三句被末句快照永久覆盖；这不是 Go 代理截断。现改为每 45ms 核对一次真实文档、最多 10 次的有限探针，一旦锁定光标前完整剪贴板正文，就取消先到的末句任务、同步文档基线并按完整文本重新进入 1000ms 稳定期；只有已调度同一完整范围时才去重。DeepSeek 提示词同时删除“不要重复任何句子”的歧义，明确保持句子数量、顺序、重复次数、标点与换行，原文重复几次就翻译几次；相同公网 API 连续三次实测均返回 `Hello. Hello. Hello.`，耗时分别约 608ms、659ms、819ms，因此正常体感仍包含用户要求的 1000ms 防抖与约 0.6–0.8 秒网络模型时间。服务端只做鉴权、限流和透明转发，本轮无需部署；完整 `bun run scripts/macos/test.ts` 已通过，系统包与构建包主程序 SHA-256 均为 `d5054f7b5ea22fe6c3ae648424ddfeff3cba2d0274ca72f0f90bc27673737afe`，版本、稳定 designated requirement、签名及动态依赖核对通过。系统级 `0.9.4 (41)` 已安装，但真实宿主中的完整多句粘贴和最终 UI 仍须用户切回 Typing 东南亚后验收，不能提前表述为最终闭环。
- 2026-08-04 用户反馈 `0.9.4 (41)` 启用后连普通打字也明显卡顿。逐项审计确认客户端同时存在五条过重路径：主线程每 350ms 读取 IMK 文档、后台每 400ms 读取 AX 文档、每次按键的 `prepareClient` 都重复执行 Accessibility 启动/前台焦点刷新、未接管宿主按键最多执行 13 次主线程正文探针、鼠标操作最多执行 50 次主线程正文探针；IMK/AX 都是跨进程读取，AX 单次消息超时上限为 180ms，这些任务叠加会周期性挤占输入法事件循环。重新核对当前 Squirrel 与 Gureum 源码后，两者的核心按键链均以 IMK 事件驱动为主，只保留和具体动作绑定的一次性 Timer，不做常驻宿主正文轮询；项目 `0.9.5 (42)` 因此删除两套常驻正文 Timer，`prepareClient` 不再重启 AX 监听，鼠标只收口旧 UI，宿主直输改为连续输入可取消的 120ms 后单次读取。AX 粘贴监听只响应 `AXObserver` 的焦点、正文和选区通知，所有正文读取继续在 utility 串行队列执行，并把通知合并为最多一个正在执行和一个待执行探针；2 秒权限检查命中同一前台应用时直接返回，不再重复读取 AX 焦点。`scripts/macos/test.ts` 已增加输入延迟契约，禁止恢复按键级监听启动、IMK/AX 正文 Timer 或循环主线程探针；完整测试通过。系统级 `0.9.5 (42)` 已安装，构建包与系统包主程序 SHA-256 均为 `92759bf3042baff463710db52aabe1abf3a2285e6fd68d434ecef6d31a4e18fd`，稳定 designated requirement、签名和动态依赖核对通过；当前系统仍选中微信输入法，真实连续打字手感及键盘/右键粘贴仍须用户切回 Typing 东南亚后验收。
- 2026-08-04 用户确认 `0.9.5 (42)` 普通打字卡顿明显改善，但连续多次分别粘贴时译文只覆盖最后一次。根因是请求代次取消本身正确，错误在于每次 Command-V 或 AX 粘贴确认都会把草稿范围重置成最新 `insertedRange`，因此最终代次虽然最新，却只携带最后一段正文。`0.9.6 (43)` 将同一客户端、同一焦点元素、正文真实相邻且合并后不超过 600 字符的连续粘贴合并为一个完整可验证范围；中间出现手动编辑、范围不相邻、焦点变化或正文验证失败时立即开启新范围，不跨上下文误合并。AX 与 Command-V 可能乱序确认同一次粘贴，因此迟到入口若发现其插入范围已经包含在当前完整范围中，只复用当前快照，不得把 AX 已合并的正文缩回最后一段。新增连续三次粘贴累积、非相邻编辑断开、AX 三次累积和 AX/快捷键乱序四类失败回归，完整 `bun run scripts/macos/test.ts` 通过。系统级 `0.9.6 (43)` 已安装，构建包与系统包主程序 SHA-256 均为 `77e581679a06be2963852288211b41126d29438cea6b7c24aac9a73e7e4109a9`，稳定 designated requirement、签名与动态依赖核对通过；当前系统仍选中微信输入法，真实连续三次粘贴必须由用户切回 Typing 东南亚后最终验收。
- 2026-08-04 用户截图确认连续粘贴三次“你好。”后删除一次，编辑框实际剩余“你好。你好。”，但 `0.9.6 (43)` 译文仍只有 `Hello.`。根因不是请求取消，而是自动翻译仍可能沿用 Command-V/IMK 触发时的最后片段或最后一句，发送前没有统一优先读取 Accessibility 当前焦点编辑框完整正文。`0.9.7 (44)` 将编辑事件收缩为纯触发：宿主粘贴、删除、撤销和直输统一做一次可取消延迟读取，删除 Command-V 的 10 次主线程循环探测；停止编辑 1000ms 后以及响应回写前都优先在 utility 串行队列重新读取当前编辑框完整正文，Accessibility 不可用时才回退 IMK，超过 600 字符则不做部分翻译。点击替换只验证客户端与完整原文范围，单纯移动光标不再使替换失效；设置说明同步改为“翻译当前编辑框完整内容”。新增 IMK 与 AX 两组“粘贴三次再删除一次”失败回归，完整 `bun run scripts/macos/test.ts` 已通过。系统级 `0.9.7 (44)` 已安装，构建包与系统包主程序 SHA-256 均为 `93e30dc6d0b7391526f34550d454d56515ad3ec47a2805d69f37eb870cf5bec1`，稳定 designated requirement、签名与动态依赖核对通过；安装脚本在本项目现有系统包归当前用户且可写时直接原位更新，首次安装或受保护包才请求管理员授权。当前键盘输入源为 ABC，输入法进程已退出，必须由用户切回 Typing 东南亚后做真实宿主最终复测。
- 2026-08-04 用户真实复测确认 `0.9.7 (44)` 粘贴后完全不显示译文。系统现场已核实版本、当前输入源和 Accessibility 授权均正常，公网翻译接口以“你好。你好。”返回 HTTP 200 与 `Hello. Hello.`；统一日志显示粘贴后只发生 IMK `Get length`，没有进入翻译请求。根因是宿主编辑链虽然标称“停止编辑一秒后读取”，实际仍在事件后 120ms 先读取一次正文，宿主尚未暴露粘贴结果时读取为空并直接终止，后续 1000ms 防抖根本不会启动。`0.9.8 (45)` 删除这次前置读取：粘贴、删除、撤销和宿主直输只重置同一个 1000ms 稳定任务，满一秒后才首次读取当前编辑框完整正文，读取成功后不再追加第二段防抖而是立即请求；显式翻译快捷键同样使用已解析快照立即请求。完整 `bun run scripts/macos/test.ts` 通过，系统包与构建包主程序 SHA-256 均为 `6b9b509c6921feed854716d50bec882a95afaa4d0f1051674917d49641dee3d8`，签名、稳定 designated requirement 和动态依赖核对通过；系统级安装已完成，当前键盘输入源为微信输入法，真实粘贴仍须用户切回 Typing 东南亚最终验收。
- 2026-08-04 用户在 Electron 宿主 Paseo（`sh.paseo.desktop`）复测 `0.9.8 (45)` 仍无翻译；真实日志显示粘贴事件和稳定期后的 IMK `Get length` 已发生，但没有发出 CFNetwork 翻译请求。现场核实标准 `kAXFocusedUIElementAttribute` 返回 `-25212`，不能取得正文；按 Electron 官方辅助功能入口对当前宿主进程设置 `AXManualAccessibility = true` 后，Accessibility 树可遍历到唯一且 `AXFocused = true` 的深层 `AXTextArea`，其 `AXValue`、`AXSelectedTextRange` 和字符数均可读取，根因确定为旧代码只查顶层焦点而漏掉真实编辑框。`0.9.9 (46)` 在稳定请求阶段的 utility 串行队列中复用已观察焦点元素，失效时才限深 80、限 5000 元素遍历一次宿主 AX 树；禁止在按键主线程遍历、禁止恢复常驻正文轮询。完整 `bun run scripts/macos/test.ts` 通过，Electron 焦点兜底 smoke 覆盖 `AXManualAccessibility`、focused 元素和树遍历契约；系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `ac8d76a438cdbb6f71e9a598ed2300cb0f311c41084c124cb7362a75f74fe764`，签名、稳定 designated requirement 和动态依赖核对通过。安装后当前仍为微信输入法且旧进程已退出，必须由用户切回 Typing 东南亚并在 Paseo 粘贴后静置 1 秒做真实宿主验收；若仍失败，只读检查新 PID 日志、AX 搜索结果和是否产生 CFNetwork 请求，不再修改防抖时序。
- 2026-08-04 用户真实确认 `0.9.9 (46)` 在 Paseo 的粘贴整段翻译已经生效，但飞书输入框仍未显示译文，并反馈 Control 类宿主快捷键出现卡顿。现场核实飞书主进程为 `com.electron.lark`，顶层 AX 焦点同样返回 `-25212`，同时存在空的 `WatermarkWidget` 覆盖窗和名为“飞书”的 `AXStandardWindow`；飞书测试期间客户端翻译请求已返回 HTTP 200，响应后再次执行 IMK 锚点查询，因此本轮同时处理编辑窗搜索、无组合态锚点和快捷键误探针。`0.9.10 (47)` 将 AX 深层搜索改为主窗口/标准窗口优先、可见子树优先，全部候选根共享最多 5000 元素与 80 层深度预算，不因窗口数量扩大扫描；候选层仍只接受实时坐标，译文层仅在同一 IMK 客户端内最多 6 秒复用最近可信锚点，使宿主提交后暂时不返回组合区矩形时仍能显示结果。普通 Control 快捷键在进入 librime 前直接交还宿主，只有既有 `Control+Period` 标点切换和 `Control+Shift+T` 手动翻译保留；进程级 Command-V 回调仍在启动期配置，但全局 keyDown monitor 只在本输入源实际激活期间注册，停用立即移除，并删除原 1 秒授权轮询。完整 `bun run scripts/macos/test.ts` 通过，新增 Control 快捷键策略、同客户端短时锚点复用、跨客户端/过期拒绝及标准窗口共享搜索预算契约；系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `96826c8ca300ebce81c13c01ed8fbbddc846ce316daa871f2bbe278fc9817a9b`，签名、稳定 designated requirement 和动态依赖核对通过。安装后当前键盘输入源仍为微信输入法，飞书显示、Control 快捷键手感与 Paseo 回归必须由用户切回 Typing 东南亚后真实验收，不能提前表述为已闭环。
- 2026-08-04 用户真实复测 `0.9.10 (47)` 后确认飞书仍未显示译文，且浏览器未进入输入光标时 Control/Command 类快捷键仍有卡顿；这证明“输入源已激活”不能等同于“当前存在文本编辑焦点”。根因是全局 keyDown monitor 仍在 `activateServer` 阶段注册，Accessibility 还保留 2 秒授权 Timer，并且飞书在不同 IMK 回调中可能更换客户端代理对象，原锚点缓存按 `ObjectIdentifier` 判断会把同一编辑会话误判为新客户端。`0.9.11 (48)` 将监听生命周期收口为：输入源激活时只注册前台应用/焦点变化观察，不注册全局键盘和正文通知；后台确认存在 `AXFocused = true` 且正文可读的真实编辑元素后，才注册该元素的 Value/Selection/Destroyed 通知并启用 Command-V 全局兜底；离开编辑焦点立即撤销正文通知、Command-V monitor、锚点和翻译任务。原 2 秒授权轮询已删除，普通浏览页面、标签栏和无输入光标状态不再接收全局 keyDown；为了仍能捕获首次粘贴，只保留轻量的应用焦点通知作为资格探测，不读取正文。飞书译文锚点缓存改用 `bundleIdentifier + uniqueClientIdentifierString` 标识稳定 IMK 会话，避免同一飞书输入框因代理对象变化丢失最近可信光标位置；实时候选仍不使用缓存。完整 `bun run scripts/macos/test.ts` 通过，新增“activateServer 不得启动全局键盘监听、真实编辑焦点才可启用、离焦立即停用、无授权/正文 Timer、稳定客户端锚点”契约；系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `0375919d897f040883a27c71cf0012a4d1c1d942014f6d7d21dbb545a47e34ac`，签名、稳定 designated requirement 和动态依赖核对通过。安装后当前键盘输入源仍为微信输入法；飞书翻译、浏览器无光标快捷键和 Paseo 回归仍须用户切回 Typing 东南亚后真实验收，不能提前表述为闭环。

- 2026-08-04 `0.9.12 (49)` 按用户确认将粘贴链收口为无外部权限的剪贴板模式：删除 `TranslationAccessibility.swift`、AX 树遍历、正文通知、全局 Command-V monitor、权限命令行、权限设置页及 `ApplicationServices` 显式链接；`TranslationDraftState` 不再导入 InputMethodKit，不调用 `length()`、`selectedRange()` 或 `attributedSubstring(from:)`，异步请求只使用调度时不可变的 Rime commit/剪贴板快照，并以 `bundleIdentifier + uniqueClientIdentifierString` 稳定会话 ID 拒绝跨会话回写。Command-V 仅在宿主把按键交给当前输入法时读取 `NSPasteboard.general`，多字符 `inputText` 必须与剪贴板去除首尾空白后的正文完全一致才触发；自动请求继续等待 1000ms，译文卡固定不可替换，Control+Shift+T 改为立即翻译当前剪贴板。`bun run scripts/macos/test.ts` 完整通过，新增提交文本累积、剪贴板隔离、跨会话拒绝、无宿主正文读取和无权限声明契约；系统级 `0.9.12 (49)` 已安装，构建包与系统包主程序 SHA-256 均为 `93ea27710221c1da1b5863877153d32c022d938879bdc81a8700f4c730d6f736`，ad-hoc 签名、稳定 designated requirement 和动态依赖核对通过，包内无 `NSAccessibilityUsageDescription` 且不链接 `ApplicationServices`。安装未自动切换输入源，当前仍为微信输入法；用户须切回 Typing 东南亚后验证目标宿主是否会把 Command-V 或多字符粘贴文本转发给 InputMethodKit，未转发的宿主按当前产品边界不自动翻译。
- 2026-08-04 `0.9.13 (50)` 修复 `0.9.12` 已提交文本也无法开始翻译的问题：系统日志可见 Rime 正常 marked/insert，但没有请求启动记录，故故障位于一秒稳定期后的请求资格判断而非 Go/DeepSeek 服务；异步链现只以 `translationGeneration`、远程翻译开关、安全输入、活动控制器和组合态拒绝过期任务，不再在请求前、成功回写和错误回写阶段重复调用可能抖动的 IMK `uniqueClientIdentifierString()` 比较。提交文本与剪贴板草稿仍复用 `prepareClient` 已记录的会话标识做本地草稿隔离。三个 InputMethodKit 输入入口的 `Esc` 统一进入缓存清理：取消翻译任务并增加代次、清空翻译草稿、调用 librime `clearComposition()`、清除 marked text、隐藏候选/译文/模式状态浮层并重置锚点；无活动输入状态时清理后仍把 Esc 交还宿主。完整 `bun run scripts/macos/test.ts` 通过，Rime bridge smoke 新增 `ni` 组字后清空且 preedit/commit 为空的断言；系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `662e082d400e174a31bf0b85b75673c87493ea5abb08a0c3276125f42cfd8d1b`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过。使用系统包内同一 capability 端点发起真实 completion 返回 HTTP 200 和非空译文；安装未自动切换输入源，当前仍为微信输入法，用户切回 Typing 东南亚后仍须人工验证一秒后浮层显示和 Esc 手感。
- 2026-08-04 `0.9.14 (51)` 收口符号输入手感：以本机已安装微信输入法 `2.2.1 (637)` 的公开包内 `punctuation.json` 做只读行为核对，其半角/默认斜杠均为直接 `/`、全角为 `／`；本项目只参考这一输入行为，没有复制微信输入法代码、二进制或视觉资源。librime 的 `symbols` 预设会把 `/`、括号、反引号等多选标点保留为组合候选，因此输入控制器新增独立 `RimeInputPolicy`，三个 InputMethodKit 输入入口都在候选 UI 刷新前处理多选符号：空组合首符号不拉起候选条；拼音后的符号把当前中文与符号一次性提交；半角优先选择与实际按键完全一致的候选，全角保留 librime 当前高亮项。拼音组合中的单引号继续作为音节分隔符，提交预览未变化的逗号/句点等翻页动作不误提交。新增纯 Swift 策略、翻译草稿与真实 Rime bridge 冒烟，已验证首个 `/` 直接提交 `/` 且不建立远程翻译草稿、`nihao/` 直接提交并翻译 `你好/`、全角/分隔符/翻页边界；完整 `bun run scripts/macos/test.ts` 通过。系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `c215b6d81d88905052bfcc3d81201a47ee707b6c774214524e03d15c0da08e6d`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过；当前键盘输入源仍为微信输入法，用户切回 Typing 东南亚后须真实验收首符号、拼音后符号、全角和 `xi'an` 分隔输入。
- 2026-08-04 `0.9.15 (52)` 修复 `0.9.14` 中“正文带符号偶发不翻译”：系统日志确认混合文本请求曾正常返回 HTTP 200，但 Rime 未处理的普通空格或单字符进入 `handleUnhandledKey` 后被当作未知动作清空了翻译草稿。输入控制器现将明确会交还宿主写入的空格和单个 ASCII 可打印字符统一送入 `handleConfirmedTextInput`，只有导航、删除、回车等无法从当前事件确认最终正文的动作才使本地上下文失效。`TranslationDraftState` 始终完整缓冲已确认写入的正文、空格及前后/中间符号，只按整段是否至少包含文字或数字决定是否建立请求：单独 `/`、`。。。` 只缓冲不翻译，随后输入正文时会发送包含领先符号的完整文本，正文后的符号也会重新等待 1 秒并发送最终版本。完整 `bun run scripts/macos/test.ts` 已通过，覆盖 `你好 世界。`、`（/你好/）`、首个 `/`、`nihao/`、全角符号、`xi'an` 与候选翻页边界；系统级安装已完成，构建包与系统包版本均为 `0.9.15 (52)`，主程序 SHA-256 均为 `b70c89750bb95aaf653acbbf3aafa0239a22067b39b691f207b84bca65f7311f`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过。安装脚本未调用自动切换，真实符号混输手感仍须在宿主输入框人工验收。
- 2026-08-04 根据用户在 Paseo 输入“你好啊。”后无译文的截图与同时间真实进程日志构建 `0.9.16 (53)`：17:14:10–17:14:11 前两次中文提交均进入翻译调度，最后一个句号发生 `insertText` 却没有新调度；根因是 `prepareClient` 仍把可能逐次抖动的 `uniqueClientIdentifierString()` 当成草稿会话边界，最后一个符号到来前清空了正文，符号随后因整段不含文字而只缓冲不请求。翻译草稿现绑定当前 `IMKInputController` 的稳定生命周期标识；同一控制器收到新的 IMK 客户端代理时只更新代理引用，不再清空整句，跨控制器激活、输入源停用、鼠标切换、Esc 和明确编辑动作仍沿用原入口失效旧草稿。另修复组合态 Enter：InputMethodKit 可能通过 `inputText` 传入 `\r`/`\n`，旧代码会先误入非打印文本/粘贴分支；现在先映射为 librime `Return`，组合态交给 `luna_pinyin` 的 `express_editor` 提交当前原始输入，非组合态才交还宿主换行。完整 `bun run scripts/macos/test.ts` 通过，真实 librime bridge 新增 `ni + Return -> ni`、`nihao + space + . -> 你好。`，草稿 smoke 覆盖回车映射、符号与空格；系统级安装已完成，构建包与系统包版本均为 `0.9.16 (53)`，主程序 SHA-256 均为 `2963e7d70ee7117f0bd74f02f99df95145b7493d08cc049a021a3c860d555673`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过。使用系统包内 capability 对“你好啊。”发起真实 completion 返回 HTTP 200 和 `Hello there.`；安装脚本未自动切换，Paseo 中完整符号句与组合态 Enter 仍须用户切回 Typing 东南亚后人工验收。
- 2026-08-04 `0.9.17 (54)` 根据真实长译文截图收口译文卡高度与上方布局：正文最大显示高度由旧 `64pt` 提升至 `112pt`，正常译文卡上限为 `130pt`、错误/失效卡上限为 `144pt`，长段落先在 `420pt` 最大宽度内多行换行再截断；无候选条且长卡需要翻到光标上方时，额外让开一整行当前宿主文字，短译文仍保持紧贴光标。布局 smoke 已覆盖长卡突破旧 `82pt` 截断和相邻输入行避让，真实宿主视觉仍以用户截图为准。
- 2026-08-04 `0.9.18 (55)` 修复“替换按钮被卡片裁切后功能阻断”：译文卡不再只依赖右侧文字入口，安全可替换时整张卡片均接受鼠标点击。`TranslationDraftState` 只为本输入法明确写入宿主且首尾连续的 UTF-16 文本累计精确 `replacementRange`，剪贴板来源固定不可替换；Rime 未处理但确定会直通的普通空格/单个 ASCII 可打印字符由输入法自行 `insertText` 后记录写入前光标范围，避免宿主异步写入造成范围未知。译文展示前和点击时都会重新验证控制器会话、空选区、光标仍在原文末尾及精确范围正文未变化，再调用 `IMKTextInput.insertText(...replacementRange:)` 原位替换；任一验证失败只保留“原文已变化”只读卡，禁止覆盖用户文本。连续提交、首尾空白范围裁剪、剪贴板不可替换、长卡布局和整卡命中 smoke 均通过，完整 `bun run scripts/macos/test.ts` 已通过；系统级包已安装为 `0.9.18 (55)`，构建包与系统包主程序 SHA-256 均为 `da3cb9031fa9cdece5ba9ef9459a8c8cead7d19e8fac3739d64fbd8f790e8e48`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过。安装未自动切换，当前键盘输入源仍为微信输入法；必须切回 Typing 东南亚后真实验收整卡点击替换。
- 2026-08-04 `0.9.19 (56)` 根据真实复测修复点击替换无效和 Backspace 未正确清理：系统日志中点击测试期间没有出现宿主正文范围读取，证明把 `attributedSubstring(from:)` 成功返回作为可点击状态硬门禁会使 Paseo/Electron 等未实现该能力的客户端退化为只读卡。替换校验现以本输入法仍未失效的连续写入快照、稳定控制器会话、精确 UTF-16 范围和光标仍在原文末尾为主；宿主能返回范围正文时继续要求内容完全一致，返回 `nil` 时不再阻断完整内部事务，原文被删除、追加、移动或其它已知编辑动作清空事务后仍禁止替换。空组合态 `BackSpace`、`Delete`、`Return` 在进入 librime 前统一清空翻译事务并交还宿主，组合态删除/回车仍由 librime 编辑或提交组字，避免正文删除键被输入法吞掉；点击替换增加不含正文内容的受控成功/拒绝日志，便于下一次真实复测直接区分事件未到达和事务失效。失败优先 smoke 覆盖宿主不支持正文读取时允许完整事务、宿主返回不一致正文时拒绝、Backspace 清空后拒绝，以及三个 IMK 输入入口的空组合态编辑键边界；完整 `bun run scripts/macos/test.ts` 通过。系统级包已安装为 `0.9.19 (56)`，构建包与系统包主程序 SHA-256 均为 `1e4d108d30461aec40f7538b20e5d3133c1ef378115ccb699a2f3978ff40f4bd`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过；当前输入源为 ABC，安装未自动切换，必须切回 Typing 东南亚做真实点击与 Backspace 验收。
- 2026-08-04 `0.9.20 (57)` 根据连续真实复测将 Esc、Backspace 和译文点击收口为分层输入状态机。只读核对 Squirrel 当前公开实现后确认：键盘主链把普通键交给 librime，并依据 handled 决定是否交还宿主；非激活候选窗口在 `NSPanel.sendEvent` 层转发鼠标。该项目为 GPL-3.0，本轮只参考系统行为边界，`TranslationOverlayPanel`、鼠标状态机和翻译事务均为本项目独立实现，没有复制其代码或资源。译文浮窗新增窗口级 `sendEvent` 路由、`isFloatingPanel`、`becomesKeyOnlyIfNeeded` 和鼠标移动接收，点击不再依赖非激活面板子视图是否被 AppKit 命中；窗口消费 mouse-down/drag/mouse-up 后仍复用原有“抬起时在卡片内才替换”的单一状态机。`inputText` 现在把 `U+0008`、`U+007F` 映射为 librime `BackSpace`，把 `U+F728` 映射为 `Delete`：组合态由 librime 逐字删码，空组合态清理旧翻译事务后交还宿主删除正文。Esc 改为只清当前 librime 组合、网络请求、译文/候选/状态浮层和锚点，保留已经提交到宿主的翻译草稿；用户继续输入时仍以 Esc 前后的完整已提交正文重新等待一秒翻译，不再永久打断链条。失败优先测试覆盖非激活 panel 的窗口级 mouse-up、控制文本 Backspace/Delete 映射、真实 librime 的空闲 Backspace 不消费与组合态逐码删除，以及 Esc 清理函数禁止调用 `translationDraft.reset()`；完整 `bun run scripts/macos/test.ts` 通过。系统级包已安装为 `0.9.20 (57)`，构建包与系统包主程序 SHA-256 均为 `d2833f67d7b309de3e9ada92f8fc68c5a11594442e12c28703537ed6067f0df6`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过；安装后输入源仍为 Typing 东南亚且新进程 PID 已重新启动，真实 Paseo 点击、Backspace 与 Esc 连续输入仍待用户验收。
- 2026-08-04 重新核对 Gboard 与 Microsoft SwiftKey 官方翻译说明：成熟移动键盘的翻译主链是由键盘先进入明确的翻译模式、维护待译输入并显示翻译结果，再把译文提交到当前应用；不是让原文先作为普通正文上屏，再跨宿主回读全文并追溯替换。macOS 版后续应采用同一事务边界：中文候选提交到输入法内部 `translation draft`，宿主仅以 marked text 展示尚未确认的完整草稿，停止输入 1 秒后翻译当前完整快照；用户通过明确的“使用译文”或“上屏原文”一次性提交，Esc/Backspace 只按草稿层级编辑或取消，不再依赖任意宿主实现完整的 `attributedSubstring` 与历史正文替换。
- 2026-08-04 Gboard 方向的首版视觉与交互已经落为 `0.10.0 (58)`：Rime 候选确认文本、已知直通字符和明确导入的剪贴板正文先进入输入法内部 `translation draft`，宿主使用同一段 marked text 显示“完整已确认草稿 + 当前 preedit”，不再先上屏原文或回读宿主正文。候选条上方的紧凑翻译展示框固定显示完整语言对，等待态与加载态保持同一尺寸，停止输入 1 秒后翻译当前不可变快照，结果态提供独立的“使用译文”和“上屏原文”按钮；Backspace 在空组合态按扩展字素编辑内部草稿，Return 上屏原文，Esc 只取消当前拼音和译文展示并保留草稿，输入源切出、编辑器切换和宿主快捷键前先上屏原文，避免 marked text 丢失。旧 `attributedSubstring`、`selectedRange`、连续写入范围和追溯替换策略已从翻译主链删除；Command-V 仅在宿主实际转发时接管剪贴板为草稿，仍不注册全局监听或申请辅助功能权限。完整 `bun run scripts/macos/test.ts` 已通过，新增内部草稿累计、纯符号缓冲、Emoji Backspace、剪贴板隔离、迟到快照拒绝、等待/加载稳定尺寸和双按钮布局回归；构建包与系统包主程序 SHA-256 均为 `c1cbb8b519ab9b940e97b95d8c106e14d50aa6f5351ff25168986d2ac69d43a6`，ad-hoc 签名有效且无 `/opt/homebrew`/`ApplicationServices` 动态依赖，已安装到 `/Library/Input Methods/TypingDongnanya.app`。真实宿主中的 marked text 长句、两个按钮、Backspace、Esc、Return、切换输入源和粘贴行为仍须用户人工验收，不能仅凭自动测试表述为完成。
- 2026-08-04 已完成平台目录迁移并建立 Android 单 app 模块，applicationId 为 `com.caizhichao.typingdongnanya`，minSdk 26、compile/target SDK 36、AGP 9.3.1、Gradle Wrapper 9.5.0、JDK 17、NDK 28.2.13676358、CMake 3.31.6；AGP 9 使用内置 Kotlin，不再应用独立 Kotlin Android 插件。V1 使用原生 `InputMethodService`，由 `InputConnection.setComposingText()` 持有“完整已确认草稿 + 当前 preedit”，候选确认文本只进入内部草稿，停止输入 1 秒后发送完整不可变快照，用户明确选择译文或原文后才以 `commitText()` 上屏；组合码变化立即取消旧翻译，纯符号草稿不拉起翻译卡，结果仍由请求代次拒绝迟到回写，密码和 `IME_FLAG_NO_PERSONALIZED_LEARNING` 编辑器暂停远程翻译。
- 2026-08-04 Android 通过项目自有 JNI/CMake 薄桥直接构建现有 `vendor/librime`，没有复制 Trime GPL 代码、资源或二进制；Trime 仅作 Android IME 生命周期和依赖关系参考。静态依赖包含 librime BSD-3-Clause、OpenCC Apache-2.0、Boost Software License、yaml-cpp MIT、LevelDB BSD 与 marisa 许可证，APK 必须打包对应 LICENSE/COPYING；`shared/RimeData/opencc/` 来自当前 `vendor/librime/share/opencc` 生成结果，用于避免在 Android 目标构建阶段运行 OpenCC 主机工具，`rime-data-version.txt` 已提升为 4。
- 2026-08-04 本机 Android 命令行工具链已可用；绿盾会让 NDK 自带 Clang 读取源码密文，因此 CMake 必须继续通过 `scripts/android/apple-clang-launcher.ts` 使用系统 Apple Clang 读取明文，同时保留 NDK 的 Android target/sysroot/linker 参数，并传入 `-fno-sized-deallocation`。不得退回复制源码到临时目录、修改绿盾白名单或直接用 NDK Clang 读取本仓源码；当前产物已核对为 ARM64 Android ELF。
- 2026-08-04 Android V1 debug APK 已完成构建、Android Lint、APK v2 签名、包名/launcher/IME 入口、单一 `arm64-v8a`、Rime/OpenCC 资源、第三方许可证和密钥边界验证；SHA-256 为 `285cf6eb168d0b69d3f9130a99ac9de431dd827b3e40ad6f57ff17a32acfc430`。APK 只包含自有 HTTPS capability 地址，不含 DeepSeek Key 或 `api.deepseek.com`；同日按 Android 请求结构实测公网翻译返回 HTTP 200。平台目录迁移后的 `bun run scripts/macos/test.ts` 也已完整通过。当前 `adb devices -l` 没有已授权设备，因此尚未执行安装，也未验收首次 Rime 部署、真实候选/草稿/译文、Backspace/Esc/Enter、剪贴板按钮、安全输入框与键盘高度，不能把构建通过表述为设备验收完成。
- 2026-08-04 Android V1 debug APK 已通过独立 SSH 静态发布到腾讯服务器：`scripts/android/deploy-apk.ts` 使用 Bun `readFileSync` 后经 SSH stdin 上传，远端按 `/opt/typing-dongnanya-download/releases/<release>/<48 位随机路径>/` 保存，Nginx 仅登记 `/typing-dongnanya-download/` 前缀并通过 HTTPS 返回 `application/vnd.android.package-archive`、`attachment` 与 `private, no-store`；HTTP 同路径 308 跳转 HTTPS，错误随机路径返回 404。release 为 `20260805000027610`，公网完整下载后 SHA-256 仍为 `285cf6eb168d0b69d3f9130a99ac9de431dd827b3e40ad6f57ff17a32acfc430`，`nginx.service`、`typing-dongnanya-api.service` 与翻译 health 均保持正常；完整下载随机路径不写入仓库，只在本次发布结果中交付。
- 2026-08-05 Android `0.1.1 (2)` 修复确认译文/原文重复上屏：旧实现先调用 `finishComposingText()`，会把 composing 原文正式提交，再由 `commitText()` 追加第二份文本；现在“使用译文”与“原文上屏”都直接对仍有效的 composing 区调用 `commitText()`，由宿主原子替换。删除键改为按下立即删除一次、按住 350ms 后每 55ms 连续删除，松手、取消或键盘 View 分离时立即停止，且同一时刻只保留一个重复任务。`bun run scripts/android/test.ts` 已通过 assembleDebug、Lint、APK v2 签名、arm64 ABI、Rime/OpenCC/许可证、HTTPS capability、译文提交顺序与连续删除源码契约；新 APK SHA-256 为 `21dd415f5f2b55bb69acae637ec4a1a8ad7950a3218611b440112d340a7536ca`。同日已覆盖原有随机下载 URL 对应文件，URL 文件名因兼容原链接仍含 `v0.1.0`，但 APK 内版本为 `0.1.1 (2)`；服务器 `/opt/typing-dongnanya-download/releases/` 最终只保留这一份 APK，不累计历史 APK，稳定路径仅保存在本机 600 权限的 `~/.config/typing-dongnanya/apk-download-path`，不写入仓库。真实宿主的译文替换与长按删除仍需设备安装后人工验收。
- 2026-08-05 Android `0.1.2 (3)` 补齐粘贴翻译与删除上滑清空：键盘按钮改名为“粘贴并译”，直接通过 `ClipboardManager.primaryClip` 和 `coerceToText()` 导入完整剪贴板，不再因 MIME description 缺失静默退出，空剪贴板显示明确状态；宿主菜单已经完成粘贴时，`onUpdateSelection()` 只在无内部草稿/无 Rime 组合、非安全输入、翻译开启、文本至少 2 个 UTF-16 单元、粘贴后光标位置与剪贴板长度吻合、光标前正文完全等于当前剪贴板且 `setComposingRegion()` 成功时，才把该新增范围接回 composing 草稿并重新等待 1 秒翻译，任一条件不满足都不读取或接管宿主其它正文，因此该路径仍是宿主配合下的严格匹配能力，不能宣称所有应用通用。删除键继续按下立即删和长按连删，上滑 42dp 后按钮显示“清空”并停止连删，松手才清除当前未正式上屏的拼音/翻译草稿，不清除宿主已确认历史正文；草稿删到空时先用空文本替换 composing 区再 `finishComposingText()`，避免把最后残留字符误提交。完整 `bun run scripts/android/test.ts` 已通过，APK SHA-256 为 `fff8c9930a24527ce2524aded8978cff06c41490bd74ff3d56dee82037a8782a`；已覆盖原稳定下载 URL，服务器仍只保留一份 APK，真实应用中的宿主菜单粘贴回调、上滑阈值与清空手感仍待用户安装复验。
- 2026-08-05 Android `0.1.3 (4)` 收口翻译区域高度：旧版把翻译卡作为键盘根布局的独立 `GONE/VISIBLE` View，等待、结果和隐藏状态会反复增加或移除整块高度；现在翻译与 Rime 候选共用始终存在的固定 48dp 候选栏，有候选时显示横向候选和翻页，无候选时在同一栏显示“简体中文 → 目标语言”、等待/错误/译文及紧凑“原文/使用”按钮，隐藏态显示“译文将在这里显示”。翻译正文最多两行并尾部省略，点击正文仍可使用译文；翻译动作只改变栏内宽度，不改变输入法总高度，独立翻译卡不再插入根布局。完整 `bun run scripts/android/test.ts` 已通过，新增契约确认根布局不再单独添加翻译卡、翻译与候选都固定在 48dp 栏位、状态切换只改可见内容；APK SHA-256 为 `97583d5e71aa42d694f0ca31f216ab7d6a03d1e5e639020d352147387ecca06e`，已覆盖原稳定下载 URL且服务器仍只有一份 APK。固定高度、两行译文可读性、按钮宽度和不同屏幕尺寸仍待真机截图验收。
- 2026-08-05 macOS 候选分页键盘手感统一为与浮层箭头相同的状态边界：候选存在且不是末页时，`=` 或 `+` 调用与右箭头相同的 librime `change_page(false)`；当前不是首屏时，`-` 调用与左箭头相同的 `change_page(true)`。首屏的 `-` 和末页的 `=`/`+` 不被分页逻辑吞掉，继续保留原符号输入语义；主键区和数字键盘产生的同字符统一生效。`RimeInputPolicySmoke` 已覆盖首屏、中间页、末页与加号别名，完整 `bun run scripts/macos/test.ts` 通过；本轮只构建验证，未安装系统包，真实宿主键盘事件仍需安装后人工验收。
- 2026-08-05 Android `0.3.0 (6)` 已完成五笔与离线手写首版：五笔设置入口、`typing_wubi86` schema/词典、QWERTY 字根提示和 `wqvb → 你好` smoke 已接通；手写键区保持与四行键盘相同高度，提供键盘切换、撤销、清空、粘贴并译、删除和换行，识别候选点击后进入 composing 草稿。`bun run scripts/macos/test.ts` 与 `bun run scripts/android/test.ts` 均通过，APK 只含 `arm64-v8a` 的 librime/ONNX Runtime 必需库，模型/字符表/许可证/来源声明、HTTPS capability、上游凭据边界与 APK v2 签名门禁通过；Debug APK SHA-256 为 `8838cac410e74e6d88135a88672042291b5f908247d6a87089ee8072de776931`，大小约 37MB。当前 `adb devices -l` 无已授权设备，本轮未安装、未上传、未修改服务器；五笔字根可读性、手写识别准确率、首轮模型加载和真实触控延迟均未做真机验收。
- 2026-08-05 Android `0.3.1 (7)` 将普通 QWERTY、中文九键和手写工具栏的“粘贴并译”统一收敛为“粘贴”，并同步缩短按钮权重、把释放空间让给空格键；行为不变，点击后仍导入完整剪贴板并按最后一次内容变化重新等待 1 秒翻译。`bun run scripts/android/test.ts` 通过，Debug APK SHA-256 为 `ad3441ea18cb417e5d88ab4e4740fa1e77bbf7ffdd8cc18861476dc1c3ea34df`。上一版 `0.3.0 (6)` 及本版均未覆盖腾讯服务器稳定下载文件，因此用户从既有下载链接取得的仍是旧包，缺少九键/五笔/手写设置属于下载包未更新，不是新设置代码未生效；服务器覆盖必须再次得到用户明确确认。
- 2026-08-05 经用户明确确认，已使用 `bun run scripts/android/deploy-apk.ts` 将 Android `0.3.1 (7)` 原子覆盖到腾讯服务器原有稳定随机下载路径；未新建下载 URL、未修改或 reload Nginx、未改 Go 翻译服务。覆盖前远端唯一 APK SHA-256 为 `97583d5e71aa42d694f0ca31f216ab7d6a03d1e5e639020d352147387ecca06e`，覆盖后远端与公网完整下载 SHA-256 均为 `ad3441ea18cb417e5d88ab4e4740fa1e77bbf7ffdd8cc18861476dc1c3ea34df`；`releases` 目录仍只保留一份 APK，文件名为兼容原链接仍含 `v0.1.0`，但包内版本已核对为 `0.3.1 (7)`。公网响应保持 APK Content-Type、attachment 和 `private, no-store`，错误随机路径为 404，Nginx/翻译服务 active、health 200，最近错误日志未见新增 502/upstream 失败。
- 2026-08-05 Android `0.3.2 (8)` 将中文九键独立重排为成熟中文输入法常见的职责分区：左侧窄栏放常用标点，中间保留 1–9 的 3×3 T9 主键，右侧放删除、只清当前组合的“重输”和较大的换行键，底栏保留输入法切换、手写、粘贴、宽版 `0 / 空格` 与中英文切换；仅参考公开布局职责，不复制微信输入法代码、图标或视觉资源。数字/字母说明及 `0 / 空格` 改为两个 `TextView` 分层绘制，不再依赖字符串换行，主键区 162dp、底栏 54dp，总键区高度仍为 216dp；“重输”只调用现有组合清理入口，保留已进入 `ImeDraft` 的正文。`bun run scripts/android/test.ts` 与 Android Lint 通过，Debug APK SHA-256 为 `ba68f94699d325492b9d0a2c280c7dccca98a622d60f618e5cb982b0edd888ba`；当前无已授权 Android 设备，真实九键尺寸、标点手感、`0 / 空格` 显示与重输行为尚待真机验收，本版尚未覆盖服务器稳定下载包。
- 2026-08-05 macOS `0.10.2 (60)` 修复物理键盘加减号候选翻页：真实 librime `change_page` 往返测试确认 API 可把 `shi` 从第 0 页切到第 1 页并返回，缺口位于 InputMethodKit 带键码但没有 printable string 的回调，主键盘等号/加号、减号及数字键盘加减号此前会退化为未知 `0x...`，无法进入现有翻页策略。`RimeInputPolicy` 现在统一识别字符键名、librime 命名键和四个 macOS ANSI 物理键码；候选非末页的 `=` / `+` 前进，非首屏的 `-` 返回，首屏减号和末页加号仍保留普通符号语义。`bun run scripts/macos/test.ts` 通过，覆盖物理键码恢复、`equal/plus/KP_Add/minus/KP_Subtract` 别名与真实页码往返；构建包和系统包主程序 SHA-256 均为 `e08f22429db77d73c94f0c9f84ac76c4b72435a4b31995d71d271b52e2fbe558`，ad-hoc 签名有效且无 `/opt/homebrew`/`ApplicationServices` 动态依赖，已安装到 `/Library/Input Methods/TypingDongnanya.app`。安装未自动选择输入法，当前键盘输入源为 ABC；用户切回 Typing 东南亚后须在有多页候选的拼音（如 `shi`）中人工验收主键盘和数字键盘加减号。
