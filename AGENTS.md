# typingchao

## 项目目标

- 本项目计划开发面向东南亚语言的「边写边译」输入法，底层输入法引擎拟基于 `librime`。
- 当前仓库于 2026-07-31 初始化；macOS arm64 为首个可运行客户端，2026-08-04 起并行开发 Android arm64 V1；用户已明确产品用于商用，最终开源/闭源方式仍未确定，但商业交付包按用户要求不得直接复制、打包或依赖 GPL/LGPL 的代码、schema、词典或其它数据，只允许使用项目自有内容、已购买商业授权内容或经核实可商用的 BSD/MIT/Apache-2.0 等宽松许可证内容；客户端统一直连 DeepSeek 官方 HTTPS 翻译接口。
- 首发平台按当前开发机确定为 macOS arm64；应用以独立 `com.caizhichao.typingchao` 输入法包安装到 `/Library/Input Methods/TypingChao.app`，不替换、删除或自动切换其它输入法。
- 工程目录固定为：`platforms/macos/` 放 Swift / Objective-C++、Info.plist、macOS 资源与冒烟测试，`platforms/android/` 放 Gradle、Manifest、Kotlin、JNI/CMake 与 Android 资源，`shared/RimeData/` 放两端共用的 Rime patch/OpenCC 运行数据，`vendor/` 锁定上游子模块，`scripts/macos/` 与 `scripts/android/` 放各平台 CLI；不要把构建产物或密钥写入源码目录。
- macOS 输入源元数据必须使用 `APPL` 包类型、模块前缀的 InputMethodKit 类名、`TISInputSourceID`、`TISIntendedLanguage`、`ComponentInputModeDict`、脚本代码与输入法图标；Swift 构建固定传入 `-module-name TypingChao`，避免包内类名与 Info.plist 不一致。
- 输入源的根 ID 与模式 ID 都必须在 `Resources/*lproj/InfoPlist.strings` 提供本地化显示名；macOS“添加输入法”窗口按 `TISIntendedLanguage` 的语言分类和检索，不按产品名检索：当前值为 `zh-Hans`，测试时必须搜索“中文”或“Chinese”，再选择 Typing Chao，不能只搜索产品名。
- 注册页与菜单栏必须使用两套不同图标：`CFBundleIconFile` 和根 `tsInputMethodIconFileKey` 使用本项目原创彩色应用图标，输入模式的 menu/palette/alternate-menu 键继续使用透明 16×16 单色模板；不得再把菜单模板放大用于系统设置注册页。根来源本地化名为产品名，模式来源本地化名为“简体拼音”，产品描述使用“通用 AI 输入法”，不再把某一项功能写进品牌副标题。
- 根目录通过 `package.json` 统一暴露 `bun run build` / `bun run test`（默认 macOS）以及 `build:macos`、`test:macos`、`build:android`、`test:android`、`build:all`、`test:all`；原平台脚本仍是唯一真实执行入口。macOS 构建产物固定收口到 `platforms/macos/build/`，验证二进制与临时 Rime 数据位于 `platforms/macos/build/tests/`；完成 macOS 输入法功能开发并通过 macOS 测试后，必须紧接着执行 `bun run scripts/macos/install.ts` 覆盖安装到系统输入法目录，不能只停在本地构建包；安装脚本在新包注册成功后必须清理本项目旧命名的系统包，避免用户继续打开旧设置页，但不得删除其它输入法或自动选择新输入源；管理员授权无法完成时必须明确记录为未安装，不能表述为已交付；Android 测试 APK 仍固定为 `platforms/android/app/build/outputs/apk/debug/app-debug.apk`，当前只打包 `arm64-v8a`。
- Android Debug APK 固定流程：每次交付测试包先提升 `platforms/android/app/build.gradle.kts` 的 `versionCode` 与 `versionName`，再执行 `bun run scripts/android/test.ts`，禁止只跑 `build.ts` 后直接发布；该验证入口会重新 assembleDebug、执行 Android Lint，并核对包名/版本、Launcher 与 IME 入口、单一 `arm64-v8a` ABI、Rime/OpenCC 数据、第三方许可证、DeepSeek 官方 HTTPS 地址、本地设置 Key 边界、APK v2 签名和本轮关键源码契约，任一步失败都不得上传。验证产物固定为 `platforms/android/app/build/outputs/apk/debug/app-debug.apk`；有且仅有一台已授权设备时可执行 `bun run scripts/android/install.ts` 覆盖安装并打开设置页，但脚本不得自动启用或切换输入法。APK 静态发布仍固定执行 `bun run scripts/android/deploy-apk.ts`，只负责 APK 下载文件，不依赖翻译服务；稳定下载相对路径从本机 600 权限的 `~/.config/typingchao/apk-download-path` 读取，完整随机路径不得写入仓库。完成后更新本文件中的 APK 内版本、SHA-256、自动验证结论与真机未验收项，禁止把构建/发布通过表述为真机功能验收完成。
- 2026-08-03 本机 Bun 1.3.14 曾被 macOS `taskgated` 以无效签名终止；已对现有 Bun 可执行文件做本机 ad-hoc 重签后恢复。若再次出现命令立即以 137 退出，先查看 `~/Library/Logs/DiagnosticReports/bun*.ips` 是否为 `Taskgated Invalid Signature`，不要误判为项目构建 OOM。
- 首版 UI 使用 macOS InputMethodKit 原生 marked text 只显示拼音，候选和首选高亮由独立深色玻璃候选条显示，当前稳定句子的草稿译文由紧邻的第二层浮窗显示；没有复制 Squirrel / Weasel 代码。检索其公开实现未发现可复用的手写输入模块，因此 V1 明确不含手写输入。
- 首版设置使用输入法进程内的独立原生窗口，不额外安装设置应用：候选条尾部和输入法菜单都可打开；翻译页提供边写边译开关、目标语言、安全输入说明和“翻译当前剪贴板”快捷键，输入页提供中英、简繁、标点和半全角基础设置。设置只写本项目 `UserDefaults` 并回写当前 librime 会话，不切换 macOS 系统输入源。
- 2026-08-04 起客户端翻译链不再读取宿主编辑框正文，不使用 Accessibility、全局 Event Tap、全局键盘 monitor 或正文通知，也不声明 `NSAccessibilityUsageDescription`；Rime 拼音输入只累积本输入法明确提交的文本，宿主实际转发 Command-V 或多字符粘贴文本且与当前剪贴板一致时才翻译剪贴板内容。此模式不申请辅助功能或输入监控权限，但不能承诺飞书、浏览器等未把粘贴交给 InputMethodKit 的宿主自动触发，也不能安全点击替换宿主原文。
- 当前机器没有可用 Developer ID 签名身份，测试包仅做 ad-hoc 签名；macOS 26.5.1 的用户目录安装虽可让 `TISRegisterInputSource` 返回成功，却没有写入当前用户的 `AppleEnabledThirdPartyInputSources`，不能视为系统设置可用。安装脚本必须先清理本项目旧的用户级测试包，再经管理员授权复制到 `/Library/Input Methods/TypingChao.app`，由系统安装阶段执行 `--register-input-source`；替换磁盘包不会替换已运行进程映射的旧二进制，安装脚本仅结束本项目旧进程，用户切换到其它输入法再切回后才启动新版本。用户在系统设置手动添加前，公开 `TISEnableInputSource` 不会写入第三方来源记录，因此不能拿它冒充自动安装成功。绝不调用 `TISSelectInputSource`，不自动切换当前输入法，也禁止改写 `AppleEnabledInputSources`。

## 已确认的技术与授权边界

- `librime` 核心采用 BSD-3-Clause；引入、分发时仍须逐项核对实际依赖、输入方案和词库的许可证。
- `rime/squirrel`（macOS）与 `rime/weasel`（Windows）采用 GPL-3.0。它们只能作为系统输入法前端实现和交互逻辑的参考；在未明确采用 GPL 开源发布策略前，不得将其代码、二进制或派生实现作为闭源商业产品的基础。
- 2026-08-01 重新核对 `librime` 与当前 Squirrel / Weasel 源码：`librime` 本身只提供输入引擎和上下文 API，不提供可直接使用的 UI 壳；完整前端至少要消费组合区光标与选中范围、候选标签/注释、高亮项、页码与末页状态，并把候选选择、高亮、翻页和组字光标移动回写引擎。当前 `RimeBridge.mm` 只透出了其中一部分，下一轮不能继续在不完整快照上堆 UI。
- 2026-08-01 补充核对 macOS 开源前端：`macSKK` 为 GPL-3.0-or-later，只能作行为参考；`Gureum` 核心为 2-Clause BSD，可用于核对 InputMethodKit 生命周期和原生候选事件转发；`Easydict` 为 GPL-3.0，只参考翻译交互；`Voxt` 为 Apache-2.0，可参考其“保存原始文本快照和替换范围、结果返回后再次验证文档未变化”的事务边界。当前仍采用独立实现，不直接复制这些项目代码或视觉资源；以后若实际复用宽松许可证代码，必须记录文件来源、修改内容和 NOTICE/版权声明。
- 2026-08-03 补充核对社区复制/粘贴实现：`azooKey-Desktop` 的 AI 选中文本改写先用 `IMKTextInput.insertText(...replacementRange:)` 直接替换，兼容兜底则暂存原剪贴板、写入结果、用 `CGEvent` 模拟 Command-V，再延迟恢复剪贴板；`WritingTools` 同样采用剪贴板快照、重新激活原应用、模拟粘贴和 `changeCount` 防覆盖，并明确跨进程没有粘贴完成通知，只能使用保守延迟。社区对用户主动粘贴的检测通常依赖 Event Tap/Input Monitoring、Accessibility 的值变化通知或宿主插件；单独观察 `NSPasteboard.changeCount` 只能发现剪贴板发生复制/写入，不能证明内容已粘贴进当前输入框。当前项目若继续禁止 Accessibility、全局事件和剪贴板观察，则只能依赖宿主转发的 IMK 事件或下一次输入事件补触发，不能承诺所有应用都能在粘贴后立即翻译。
- `vendor/` 仍保留 `rime-prelude`、`rime-luna-pinyin` 与 `rime-essay` 子模块供只读格式/行为核对，但 2026-08-05 起 macOS/Android 商业构建链不再复制或引用它们。共享拼音数据固定从 Apache-2.0 的 AOSP PinyinIME `49aebad1c1cfbbcaa9288ffed5161e79e57c3679` 原始 65,105 条词典经 `scripts/rime/generate-aosp-pinyin-dictionary.ts` 确定性生成；源文件、NOTICE 与生成词典 SHA-256 分别为 `408700f28a56091fa07f3b849a0f134fbfc71e6b2ae9b3f52973a5b076f599ff`、`b5830d96fb5a7e7e7ebcc295f352846b4b998e78fdc8f9aa68e134d2e4b39986`、`c13842b707ed764a5520fe158908f38ac0cf5ae1d84bf5e3b84a2972a39ec539`。两端测试必须同时检查所需资源、来源声明、文件名和文本引用，拒绝旧 LGPL Rime 数据重新进入交付包。
- 以后新增任何 Rime 前端、词库、输入方案或第三方代码前，必须先记录其许可证、修改方式和分发影响到本文件。GPL/LGPL 项目的公开代码允许只读参考功能边界、交互行为、系统生命周期、API 调用顺序和测试口径，但禁止复制、改写、逐句翻译、机械迁移其代码、schema、词典、配置、UI 资源或形成可识别的派生实现；正式实现必须基于 librime/系统官方 API 与项目自有设计独立编写，并能说明参考事实与本项目实现的边界。
- 2026-08-05 全拼、自然码双拼与小鹤双拼已统一改接项目 `typing_pinyin` 词典并同步到 Android 设置、持久化、JNI schema 选择和键盘生命周期；加入五笔后共享 Rime 数据版本为 `7`。macOS 冒烟验证 `nihao`、自然码 `nihk`、小鹤 `nihc` 均提交“你好”，不再引用 `luna_pinyin`。
- 2026-08-05 Android `0.2.0 (5)` 已增加项目独立实现的中文九键：3×4 数字/字母分组通过 `typing_pinyin_t9` 将 `64426` 解析为“你好”，候选与词频复用 Apache-2.0 拼音数据；切换方案只重建固定高度键区并安全重挂复用按键，中英切到西文时回到 QWERTY，避免九键直接输出数字。`bun run scripts/macos/test.ts` 与 `bun run scripts/android/test.ts` 均通过，最终 Debug APK SHA-256 为 `c89f4543859bc7bf6ab81678b2e40e8d4e8a857218b5f0caa70ed8982e7bf49a`；尚未在 Android 真机验收九键候选、方案切换与手感，不得表述为真机完成。
- 2026-08-05 五笔不使用官方 LGPL `rime-wubi` 或上游链不清的极点/搜狗码表，当前锁定 `tenfyzhong/wubimb` 的 MIT 数据：仓库提交 `afba8da77234920d10ccf0c94bfc7897861f3543`、数据提交 `198c11625482c5bab34fb19128a872851f9b86ed`，原始 `wubi86.txt` 共 53,727 条且 SHA-256 为 `9fca2e8eb1b19263a09dcf54bc94de128902b4d3596ed64a305178ae910c63c8`；该仓库先建立 MIT LICENSE、后由同作者加入码表且未发现相反上游归属说明，因此可用于当前实现，但生产商用前仍建议法务复核来源链。`scripts/rime/generate-wubi86-dictionary.ts` 负责哈希/数量/字段门禁，生成词典 SHA-256 为 `b8a00a0307c33432bf8b2e25075ff85f45dcd01281e64816bc58f2ac830878c2`，macOS smoke 已验证 `wqvb{space}` 提交“你好”。
- 2026-08-05 Android 手写不依赖 Google 动态模型下载或系统隐私权限，固定打包 Apache-2.0 的 PaddleOCR `PP-OCRv6_tiny_rec_onnx` 提交 `2612ab37152ae0a677521bae4e1e3d4fb4cf7c30` 与 MIT 的 ONNX Runtime Android `1.21.1`；模型 SHA-256 为 `9ef676d6ed3c88256a2d92c640c44f25b0c40947e111b14b8be8f594091563e6`，生成的 6,905 项字符表 SHA-256 为 `a295ab9d9a1e00aa86873a06230989f90c019291d1e7eaf026693ca136437abe`。手写是独立 `KeyboardInputMode`，不是 Rime schema；画布只在抬笔后触发 420ms 停顿识别，ONNX 推理使用独立串行线程和 generation 拒绝旧结果，候选确认后才并入同一原文草稿并继续 1 秒翻译链。该 OCR 模型对真实手写的准确率、首轮加载延迟、连续书写和候选手感必须以 Android 真机验收，自动构建通过不能替代质量结论。

## 实时翻译 API 结论（2026-08-01 更新）

- 首选候选：阿里云百炼 `qwen-mt-flash`。它面向文本机器翻译，支持 92 种语言、术语干预、翻译记忆、领域提示，以及真正的增量流式输出；适合中文与泰语、越南语、印尼语、马来语、老挝语、缅甸语等东南亚语言组合。
- `qwen-mt-flash` 于 2025-11-10 在中国内地发布，于 2026-01-12 在全球地域发布；截至 2026-07-31，它属于发布约八个半月的当前翻译模型，不是刚发布的实验模型。
- 2026-07-31 16:47（Asia/Shanghai）从本机查询 OpenRouter 公共 `GET /api/v1/models`：返回 200，但没有任何模型 ID 含 `qwen-mt`；在 OpenRouter 正式上架并由其目录查询到前，不得将它作为 `qwen-mt-flash` 的测试或生产渠道。
- 购买前先在百炼的模型体验页验证翻译质量；模型页标注 `qwen-mt-flash` 支持模型体验。新开通百炼账户在北京地域、服务部署范围为中国内地时可获得 90 天新人免费额度，但具体到 `qwen-mt-flash` 的可用 token 数必须以控制台当日显示为准，并在调用前开启“免费额度用完即停”。
- 阶段性流程 POC 可先使用通用模型 `deepseek-v4-flash`：它于 2026-04-24 发布，较 Qwen-MT 更新；但它不是专用机器翻译模型，测试结果只能验证“整段提交 → 服务端 → 返回译文”的产品流程，不能替代最终语种质量、术语一致性或成本选型。
- 2026-07-31 从本机查询 OpenRouter 公共模型目录，已存在模型 ID `deepseek/deepseek-v4-flash`。可以用现有 OpenRouter 额度做流程 POC；正式的中国大陆链路、数据边界、限流和成本必须另行以 DeepSeek 直连或最终供应商直连验证，不得以 OpenRouter 结果代替。
- 用 `deepseek-v4-flash` 做纯翻译时，关闭思考模式，并在固定系统提示中要求“仅输出译文、保留段落/占位符/专名、不解释”。首轮整段提交流程可先用非流式返回；需要验证译文逐步呈现时再单独开启 SSE，不能把流式 UI 调试与翻译质量评估混在同一轮。
- `qwen-mt-flash` 和 `qwen-mt-lite` 的流式响应块只包含新生成文本；`qwen-mt-plus`、`qwen-mt-turbo` 每块包含当前完整文本，不能作为实时预览默认模型。`qwen-mt-lite` 仅支持 31 种语言，适合低延迟简单预览；`qwen-mt-plus` 只在用户确认整句后的高质量复译使用。
- 百炼翻译模型的文本接口每次只接收一条待译文本，不支持前缀续写或上下文缓存。因此「边写边译」必须将当前稳定文本快照作为新请求发送，不能把不断增加的按键输入写入同一请求。
- 百炼北京地域当前默认限制：`qwen-mt-flash` 为 60 RPM / 35,000 TPM。实时策略必须做防抖、取消旧请求、版本号拒绝过期结果，并限制每个用户的发起频率；上线前必须通过控制台申请符合活跃用户数的额度，不得按每个按键发请求。
- 备选候选：腾讯混元 `hunyuan-translation`。接口支持 SSE，输入可带 `Field`、`References` 和术语库；默认接口频率为 20 次/秒、账号默认并发 5 路，标准版当前刊例价为输入 1.2 元 / 百万 Token、输出 3.6 元 / 百万 Token。它覆盖泰语、越南语、马来语、印尼语、高棉语、缅甸语等，但官方当前语言列表未列老挝语；若老挝语是首发范围，不能替代 Qwen-MT。
- 不要把阿里 `qwen3-livetranslate-flash` 用作文本输入法翻译：其公开 API 输入为音频或视频。不要接入腾讯云旧 TMT 文本 API；该产品的 `TextTranslate` 等文本接口已在 2026-07-08 删除。
- API Key 不得嵌入 Rime 配置、Lua 脚本、输入法包或 APK；用户必须在 macOS 设置窗口或 Android 设置页手动配置，客户端从本地 UserDefaults/SharedPreferences 缓存读取后直连 DeepSeek，翻译仍需由用户明确开启并避开安全输入框。

## 开发与验证约束

- 先确定首发平台和商业授权模式，再选择系统输入法前端实现；不能因参考 Squirrel/Weasel 而复制 GPL 代码。
- 首版显示的是「草稿译文」，新的稳定输入快照到来后应整体替换上一次草稿；所有自动翻译（包含句末标点）都必须从最后一次输入活动起稳定等待 1 秒后才允许发送，过期请求、主动取消不应显示为失败。
- API 选型不能只看是否支持 SSE：每次新输入都要重新发起完整文本翻译，所以必须同时测试首字节延迟、整句完成延迟、取消后的过期回写、限流、成本和目标语言质量。
- 2026-08-02 已通过 `Tests/RimeSmoke.cc` 验证打包数据可把 `nihao{space}` 提交为 `你好`、`nage{space}` 提交为 `那个`；DeepSeek 官方 Chat Completions 的请求结构由两端客户端独立实现，输入法内最终跟随效果仍需用户配置 Key 后人工观察。
- 2026-08-01 候选窗定位统一改为优先读取 `IMKTextInput.attributes(forCharacterIndex:lineHeightRectangle:)` 的真实光标矩形，失败时才回退 `firstRect`；候选窗与译文窗共用同一个屏幕布局入口，优先显示在光标下方，空间不足时整体翻转到上方，不再使用固定偏移或屏幕中心兜底。
- 2026-07-31 默认简体中文通过 `Resources/RimeData/luna_pinyin.custom.yaml` 的 `"switches/@2/reset": 1` 固定，Rime 数据增加 `rime-data-version.txt` 版本标记，只有版本变化时重新部署，避免旧用户目录继续使用历史繁简状态。冒烟已验证 `nage -> 那个` 且结果不含繁体 `個`。
- 2026-07-31 输入法菜单图标改为透明背景的单色模板图形；旧版 64px 蓝底白圆在 macOS 菜单模板渲染中表现为白色方块，因此不能继续使用整块彩色背景。
- 2026-08-01 `0.4.0 (4)` 将草稿译文面板重构为“双层轻卡”的辅助层，直接显示本轮实际发送原文、目标语言、加载/结果/失效/具体错误状态；翻译链路按 480ms 防抖、35ms 句末延迟、600 字符上限、任务取消和请求代次拒绝迟到回写，DeepSeek 提示词仍固定为无历史单段翻译并执行最小输出清洗。
- 2026-08-01 已构建并安装测试版本 `0.4.0 (4)`；`bun run scripts/macos/test.ts` 通过，`nihao -> 你好`、`nage -> 那个`，系统包与本地构建可执行文件 SHA-256 一致，ad-hoc 签名验证通过，产物仍不依赖 `/opt/homebrew` 动态库。安装脚本已结束旧进程，用户必须切换到其它输入法再切回 TypingChao启动新版本；真实候选鼠标交互、光标定位和输入法内翻译仍待用户运行截图验收。
- 2026-08-01 `0.4.0 (4)` 不再按每次提交后的 `selectedRange()` 连续位置决定翻译原文是否重置：Rime commit 始终先进入同会话稳定缓冲，客户端文档明确匹配“完整缓冲”时建立可信范围，明确只剩新提交文本时才开启新草稿；请求开始前再次验证完整源文本，并在译文卡显示实际发送内容。“你好看真的 -> Really.”是否已闭环必须以新版本截图中的原文行和译文共同验收。
- 2026-08-01 用户截图确认 `0.4.0 (4)` 在编辑框已有“你很棒 这个商品你多少个”时，译文卡实际只发送“多少个”；根因是请求开始前按当前 Rime 缓冲长度读取客户端文本。`0.4.1 (5)` 已改为向光标前最多读取 600 字符，并从最近的换行或 `。！？!?；;` 后截取完整当前句，句末标点属于本句；翻译和点击替换共用该句的精确文档范围。当前构建与自动测试已通过，系统级安装和新截图闭环仍待完成，不能提前表述为已验收。
- 2026-08-01 边写边译已按“翻译源文本快照”和“可点击替换范围”两个职责实现：客户端不支持读取时仍可用 Rime commit 缓冲翻译，但显示“仅预览”；只有客户端、光标、原文正文和范围均验证通过时才显示并执行“替换原文”，任何一项变化都保留译文并切换为“原文位置已变化”，不能冒险覆盖用户文本。
- 2026-08-01 `0.5.0 (6)` 将候选条与译文卡改为同一套光标跟随布局：候选更新时只重定位已显示译文卡，不重建浮层；整句翻译防抖调整为 820ms，句末快速延迟调整为 180ms。构建、Rime 冒烟、ad-hoc 签名和 `/opt/homebrew` 动态依赖检查通过；系统包版本与本地可执行文件 SHA-256 一致，安装到 `/Library/Input Methods/TypingChao.app`，未自动切换当前输入法。
- 2026-08-01 `0.4.0 (4)` 已补齐候选壳所需的组合选中范围与光标、候选标签/注释、高亮项、页码和末页状态，并实现鼠标 hover 高亮、点击选词、翻页和点击组合码移动光标；所有动作均回写 librime 后使用完整快照刷新，没有复制 Squirrel / Weasel 代码或视觉资源。
- 2026-08-02 `0.6.1 (9)` 完成候选与译文浮层第一轮手感收敛：候选层不再重复绘制 preedit，按 comment 使用 40px/48px 紧凑高度，单页不预留分页空白，最小宽度为 176px；hover 只作本地视觉反馈，不再回写 librime 高亮。译文层不显示原文、草稿或仅预览按钮，新请求开始立即隐藏旧译文；不可替换状态鼠标穿透，可替换状态整卡点击后仍按客户端/光标/原文范围重新验证。InputMethodKit 控制器使用进程内单活动浮层会话，激活、停用、`hidePalettes` 和跨编辑器接管均清空组合、取消翻译、隐藏旧浮层；并使用 `IsSecureEventInputEnabled()` 在安全输入期间禁用远程翻译。`bun run scripts/macos/test.ts` 通过，`0.6.1 (9)` 已安装到系统目录，本地与系统可执行文件 SHA-256 一致；真实切换、多屏、密码框、浏览器和鼠标截图仍待人工验收，不能提前表述为完成。
- 2026-08-02 Goal 模式完成 100 项候选/译文/生命周期设计逐项审查：其中 28 并入 21，63 与 92 删除；16、17、18、24、31、35、37、38、42、47、59、62、64、65、68、71、77、78、83、93、96、98、100 收敛为以 InputMethodKit、librime 快照/keymap、轻量辅助层和安全替换事务为准的最终口径。`0.6.2 (10)` 修复 Emoji surrogate 的强制解包风险：句子边界改用 composed character sequence，并新增可执行 smoke 覆盖 Emoji、标点、空白、换行；超长且无法确认句首的当前句不再发送截断残句。候选与译文先按可见屏幕宽度约束，垂直空间不足时选择较大一侧；正常译文改为约 36pt 单行内容卡、有限宽度档位，错误/失效才显示状态行。401/402/429/5xx 用户文案不再要求终端用户检查服务器配置。三名只读 Agent 分段审查均未改代码；真实宿主视觉与多应用验收仍待执行。
- 2026-07-31 已完成本机系统级注册验证：`/Library/Input Methods/TypingChao.app` 存在，TIS 返回“已启用、可选择、当前选中”，`com.apple.inputsources` 已记录本项目的 Input Mode 与 Keyboard Input Method，输入法进程已由 `imklaunchagent` 启动。此事实证明必须按“中文/Chinese”手动添加，不应再以搜索产品名的空结果判断安装失败。
- 2026-07-31 已以只读方式核验 Squirrel 当前安装流程与本机 WeType：两者均使用 `/Library/Input Methods`；Squirrel 将系统安装阶段的注册、当前用户的启用和显式选择拆开。项目仅采用该公开的系统生命周期事实，不复制 GPL 源码；本项目测试安装不执行选择。
- `TISInputSource` 的进程内“已启用”状态不能单独作为安装成功依据；安装后先核对系统级包路径与 `--input-source-status`，再由用户在系统设置按“中文/Chinese”手动添加并核对 `com.apple.inputsources` 的第三方来源记录。公开 SDK 未提供 `TISUnregisterInputSource`，迁移旧用户级测试包时只能移除本项目自己的旧包并重新注册系统级包。
- 后续每次确认平台、许可证、模型版本、限流/价格、实测延迟、词库来源、隐私策略、输入文本提取方式或发布策略时，必须在本文件追加或更新结论，并写明核实日期；未验证的假设必须明确标注为未验证。
- 2026-08-02 `0.6.3 (11)` 补上基于 InputMethodKit `menu()` 与 librime runtime API 的最小输入法壳：菜单动态显示当前方案和中/英状态，可切换当前会话的中英、中文/西文标点、半/全角、简/繁字形，并从 librime `default.yaml` 动态读取并选择已打包方案；边写边译可开关，首版目标语言可选英语、泰语、越南语、印尼语、马来语。翻译偏好、已选方案及持久化的 Rime 视觉/标点字形选项仅写本项目 `UserDefaults`；`ascii_mode` 保持会话级，不会自动选择 macOS 系统输入源。已从本机 SDK 头文件和当前 librime 源码确认 `IMKInputController.menu()`、`get_schema_list`、`select_schema`、`set_option`、`get_option` 的职责，未复制 Squirrel/Weasel 代码。Rime 冒烟新增输入方案枚举及运行时 option 断言，用户级设置冒烟新增翻译开关/目标语言/方案/持久化边界；`bun run scripts/macos/test.ts` 通过。系统安装与菜单真实点击验收仍待完成。
- 2026-08-02 尝试通过既有 `bun run scripts/macos/install.ts` 安装 `0.6.3 (11)` 时，非交互终端在 `osascript` 管理员授权阶段无输出等待；已取消，未写入 `/Library/Input Methods/TypingChao.app`。核对后系统包仍为 `0.6.2 (10)`、SHA-256 为 `39d6337c9f051f83c99385396593fd73604c4d20956e26b5d192f2917fd9f6e3`，新构建 `0.6.3 (11)` 的 SHA-256 为 `c4a422cccf6edcac23e8811083a8702cd21c2a8e6b725768b1afc2a1588e7680`；必须在可输入管理员凭据的终端重新运行安装脚本，成功后切到其它输入法再切回，才能实际验收菜单。
- 2026-08-02 本地构建完成 `0.6.4 (12)`：Rime 的 `currentSnapshot` 及 schema/option/翻页/高亮/光标等状态动作不再读取会消费 commit 的 `get_commit`，只有按键、选词与显式 `commitComposition` 读取 commit；两个 `inputText` 未接管入口均收口旧草稿；显式提交同步更新本地 Rime 快照。稳定草稿防抖调整为 1000ms，句末仍为 180ms。翻译端点固定为 DeepSeek 官方 HTTPS 地址，API Key 由设置页缓存读取；无 Key、网络和解码错误均有可行动提示，固定提示明确原文内指令只作为待翻译数据。
- 2026-08-02 `bun run scripts/macos/test.ts` 已通过：无 Token 包端点契约、Rime `nihao -> 你好` / `nage -> 那个`、候选交互与 option、Emoji/句界/安全输入资格、设置持久化边界、Rime bridge 非消费快照与显式提交。`Info.plist` lint、ad-hoc 签名和动态依赖核对通过；`0.6.4 (12)` 本地可执行文件 SHA-256 为 `7c627aea7f663750ef2fa418e9576f8120753368fbe5aa71f89b05d23e915f27`，无 `/opt/homebrew` 动态依赖。系统包仍为 `0.6.2 (10)`，未修改；在线翻译服务和真实宿主 UI 均未因此验收。
- 2026-08-02 完成 `0.6.4 (12)` 的逐文件基础功能复审并再次构建验证：Rime bridge 仅接收 ASCII 可打印键或命名功能键，直接非 ASCII 文本和多字符 `inputText` 不再按 UTF-8 首字节伪造按键，bridge smoke 已覆盖该边界；未调用的 bridge 高亮 API 已删除，候选 hover 保持本地视觉行为。`InputOverlayAnchor` 在没有可信 marked/selected range 时直接隐藏，不会把 `NSNotFound` 交给 `firstRect`；可替换译文卡的整卡 hover 与实际点击范围一致，正常译文的绘制高度可显示最多两行；两个玻璃面板按圆角裁切，单候选面板由 176pt 收敛为 84pt，短译文卡增加 160pt / 220pt 宽度档位，减少无效留白。`bun run scripts/macos/test.ts`、Swift typecheck、Objective-C++ syntax check、`Info.plist` lint 与 ad-hoc 签名均通过；新本地 SHA-256 为 `ea25934cc672b0f7fcf916be3340b540af3ccf914db6a0885710e93cb5bbee37`，仍无 `/opt/homebrew` 动态依赖。系统包仍为未替换的 `0.6.2 (10)`（SHA-256 `39d6337c9f051f83c99385396593fd73604c4d20956e26b5d192f2917fd9f6e3`），本轮未执行管理员安装、未改服务器或远端资源；新包真实 UI、翻译接口和安全输入仍待人工验收。
- 2026-08-02 再次从 Agent TTY 发起 `bun run scripts/macos/install.ts`：macOS 管理员授权阶段在 90 秒内没有可交互输出，已主动取消；随后只读核对系统包仍为 `0.6.2 (10)`、SHA-256 `39d6337c9f051f83c99385396593fd73604c4d20956e26b5d192f2917fd9f6e3`，无残留安装进程。该执行环境无法代替用户完成系统管理员认证；必须从可见的本机 Terminal 运行安装脚本并完成授权，才可以同步新包，不能把本地 `0.6.4 (12)` 的构建通过表述为系统包已升级。
- 2026-08-02 继续收口浮层定位：部分 `IMKTextInput` 在 `attributes(forCharacterIndex:lineHeightRectangle:)` 无定位信息时会返回完整 `.zero` 矩形，不能当成真实屏幕左下角光标；`InputOverlayAnchor` 现在把该哨兵改走 `firstRect` fallback，仍然保留单一维度为零但其余坐标有效的真实 line rect。新增 `Tests/OverlayLayoutSmoke.swift` 的完整 `IMKTextInput` 替身，覆盖 attributes 优先、`.zero` fallback、缺少 marked/selected range 时不调用 `firstRect`、候选/译文 panel 屏幕内夹取；并把 smoke 接入 `bun run scripts/macos/test.ts`。完整测试、Swift typecheck、`Info.plist` lint、ad-hoc 签名和动态依赖核对均通过；本地 `0.6.4 (12)` 新 SHA-256 为 `9ba2e29ac395a5f1bb0248caa1268f914278b4628a4233a5011c6b61f9c7cfd1`。系统包仍为未替换的 `0.6.2 (10)`，真实宿主视觉与翻译接口没有据此验收。




- 2026-08-02 用户截图再次确认 0.6.7 (15) 的候选条与译文卡分别被夹到屏幕左下/左上角。根因不是间距参数，而是定位主链把“有限、非零”误当可信坐标，并在矩形不属于任何屏幕时回退 NSScreen.main 后夹取；异步翻译又在不同阶段重复查询，导致两层使用不同错误锚点。0.6.8 (16) 将浮层定位收口为 IMKTextInput.attributes(forCharacterIndex: 0, lineHeightRectangle:) 单一入口，完全移除 selectedRange、markedRange、librime caret 和 firstRect 对 UI 坐标的参与；无效或离屏矩形直接隐藏，不再回退主屏角落。翻译请求与 UI 锚点解耦：调度时捕获同客户端锚点，网络照常执行，返回时优先刷新当前锚点、失败则使用提交时锚点，两者都无效才记录受控日志。OverlayLayout smoke 已覆盖固定 index 0、firstRect 零调用、范围缺失、零值/NaN/Infinity/负尺寸/离屏拒绝和候选/译文屏幕内夹取；真实 ChatGPT/TextEdit/浏览器/副屏定位仍必须以安装后截图验收。
- 2026-08-02 用户截图确认 0.6.8 系统包的输入源菜单图标仍显示为白色圆角方块。已核实 V3 PDF 虽标称模板图形，但实际包含覆盖 1–15pt 范围的不透明青绿色圆角背景；macOS 在选中菜单项时按 alpha 蒙版统一染白，背景与内部箭头一起形成实心白块，问题不是单纯的 TIS 缓存。0.6.9 (17) 删除 V3 源资源并改用 TypingChaoMenuIconV4.pdf：透明 16×16 页面只绘制单色双向翻译箭头，根、menu、palette、alternate-menu 四个图标键统一更名引用；新增 scripts/macos/generate-menu-icon.ts 作为确定性生成入口和 Tests/MenuIconSmoke.swift 像素级门禁，强制四角透明、稀疏 alpha mask、单色与有效包围盒。bun run scripts/macos/test.ts 全部通过，本地构建主程序 SHA-256 为 `6e32cf79d375f0f7be190180fd0a67479ea0aa43657dc543f3753a2d13f16689`；系统安装因管理员授权窗口未完成而已取消，系统包仍为 0.6.8 (16)，不能称为真实菜单验收通过。
- 2026-08-02 继续核对 0.6.9 图标未变化问题：系统包与构建包均为 0.6.9 (17)，主程序和 `TypingChaoMenuIconV4.pdf` 的 SHA-256 分别一致，系统包四个图标键均引用 V4，当前输入法进程也从系统 V4 包重新启动；TIS 当前的 `kTISPropertyIconImageURL` 已明确指向系统包内 V4，因此已排除旧包未安装、旧输入法进程和旧 Info.plist。剩余最可能是负责绘制输入源菜单的 `TextInputMenuAgent` 持有按输入源 ID 缓存的旧图像，已只重启该用户级进程并确认 PID 更新、TIS 仍返回 V4 URL；真实菜单是否刷新必须以用户重新展开后的截图确认，不能提前称为闭环。`scripts/macos/install.ts` 已补充安装后重启 `TextInputMenuAgent`，不重启 SystemUIServer、不切换输入法。
- 2026-08-03 用户截图确认正常译文辅助层采用「只显示译文正文 + 右侧轻量替换入口」的紧凑单层方向可继续使用。`0.6.10 (18)` 将所有自动翻译统一为最后一次输入活动后稳定等待 1000ms，句末标点不再走 180ms 快速请求；Rime 组合码仍在变化时只取消旧任务和旧译文、不清空已提交草稿，组合结束后重新计时。请求开始前若当前文档原文与调度快照不同，会按新原文重新计满 1 秒；返回后再同时校验 generation、当前客户端、组合态和当前句正文，旧回复不得覆盖或展示为最终文本。译文卡宽度改为 160/220/320/420/560/720/960pt 有限档位并封顶 960pt，避免长句直接扩到整屏；整卡替换改在鼠标抬起时提交并支持拖出取消。`bun run scripts/macos/test.ts`、Go 测试、Swift 语法、plist、签名和动态依赖核对均通过，最终构建可执行文件 SHA-256 为 `9dee7c06ee0638dafb92ec0fd542236703c1312a46f0e34439ef5c3384b7312e`；最终系统包覆盖仍在等待 macOS 管理员授权，不能把当前系统包的同版本旧二进制写成已同步。
- 2026-08-03 `0.6.11 (19)` 针对用户新截图收口四项问题：注册页改用独立彩色应用图标并将模式副标题改为“简体拼音 · 边写边译”；候选条尾部与输入法菜单新增设置入口，原生设置窗口可直接修改目标语言及首版基础输入选项；无组合态空格由宿主直接插入时只重算 1000ms 稳定期，不再清空已经提交的整句草稿；长译文由最多约两行放宽到最多约四行后再缩略。完整 macOS smoke 与 Go 测试已通过，系统包已安装为 `0.6.11 (19)`，构建包和系统包主程序 SHA-256 均为 `ebe8b4d2f6a15a37dc08b1485c72a0d98098591abfe2c1f3c054601673e15bd4`，两类图标资源哈希一致、ad-hoc 签名有效、无 Homebrew 动态依赖和上游 Key；安装未自动切换输入法，当前 TIS 状态为已启用、可选择、当前未选中。真实系统注册页、候选设置按钮、设置窗口交互与空格后网络请求仍需用户切回输入法后人工验收。
- 2026-08-03 `0.6.12 (20)` 重构宿主文本变更触发链：旧实现遇到 Command 快捷键、多字符 `inputText`、未被 Rime 接管的删除/直输时会直接清空草稿，且宿主完成粘贴、剪切、撤销、重做后没有回读入口，因此文本已变更也不会翻译。新实现保留 Command 事件给宿主，只对 `V/X/Z/K`、删除键、未处理可打印字符/空格/换行建立编辑前局部文档探针；宿主落字后以文档长度与重叠正文确认真实变化，再从当前客户端重建整句与替换范围，并从该次最终变化重新等待 1000ms。`recognizedEvents` 同时接收左右鼠标按下/松开，参考 Gureum 公开的 InputMethodKit 事件范围但未复制其代码，使右键菜单粘贴可在鼠标松开后短时观察；单纯复制、移动光标或选区不发请求。请求开始与返回仍校验 generation、当前客户端、组合态和最终句正文。新增 smoke 覆盖键盘粘贴、等长替换、撤销后重建、光标移动不误触发和命令分类；完整 `bun run scripts/macos/test.ts` 已通过。构建包版本为 `0.6.12 (20)`，主程序 SHA-256 为 `4c687ef2e82c9d5346696d7ddc8580f0fe38db8529258a633076dc29e50c2127`；系统包覆盖仍等待管理员授权完成，未完成前 `/Library/Input Methods` 仍是 `0.6.11 (19)`，不能称为已安装验收。
- 2026-08-03 若 Swift/Clang 报告 SDK 头文件在 PCH 建立后尺寸变化，应使用本轮独立的 `CLANG_MODULE_CACHE_PATH` / `SWIFT_MODULE_CACHE_PATH` 重跑现有构建测试脚本；该错误来自本机旧模块缓存，不应删除 SDK 文件或误判为业务代码编译失败。
- 2026-08-03 用户明确调整复制语义后发布 `0.6.13 (21)`：所有已识别的宿主正文变化仍按最终文档快照重新等待 1000ms；`Command-C` 另外视为明确翻译动作，有选区时翻译选中文字且不开放点击替换，无选区时重新读取光标前当前整句。这样复制会触发翻译，而粘贴、剪切、撤销、删除仍必须等宿主真正改完正文后再发送，不能把操作名代替最终文本校验。`bun run scripts/macos/test.ts` 全部通过，系统包已安装为 `0.6.13 (21)`；构建包与 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `ce050069a23a8a323b6c3786f83ee2d5c9d24ee96d381ec63cd2a84d41470a7d`，两者签名验证通过且无 `/opt/homebrew` 动态依赖。安装没有自动切换输入法，TIS 状态为已启用、可选择、当前未选中；真实宿主中的复制、粘贴及连续编辑仍需用户切回 TypingChao后人工验收。
- 2026-08-03 用户在 Codex/ChatGPT 类宿主中连续复制同一文本仍无翻译 UI，确认 `0.6.13 (21)` 的缺陷是把复制触发绑定在 `IMKInputController.handle` 的 `Command-C` 事件；宿主应用会直接消费复制命令，输入法未必收到该按键，因此不是 UI 缓存问题。`0.6.14 (22)` 改为只在 TypingChao输入源激活期间每 120ms 检查 `NSPasteboard.changeCount`，代次未变化时不读取内容；代次变化且当前不是安全输入时才读取最终字符串，并以只读翻译源重新等待 1000ms。相同字符串连续复制会因新的 changeCount 各触发一次，复制文本不开放点击替换；输入源切出或控制器销毁时立即停止监听。独立 NSPasteboard 回归已验证同一文本连续写入两次产生两个可消费代次，完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.14 (22)`，构建包和系统包主程序 SHA-256 均为 `d335b613456d50adf658c4756acba39ba272245d98397c090d11d7c1a9a71d4a`，签名有效且无 `/opt/homebrew` 动态依赖；安装后当前输入源仍是未选中，必须人工切回 TypingChao启动新进程后再做真实宿主验收。
- 2026-08-03 用户截图确认 `0.6.14 (22)` 已显示译文 UI，但复制长句后仍只得到手动输入末尾“你好”的译文 `Hello.`。根因是剪贴板监听已经触发成功，`synchronizeCopiedText` 也保存了复制正文，但请求开始前和返回后共用的 `resolvedSnapshot` 无条件重新读取当前 IMK 文档，把复制源覆盖成输入框当前句。`0.6.15 (23)` 新增明确的 `TranslationSourceKind`：文档源继续在发送前重建最终整句，复制源固定使用对应剪贴板快照且保持只读；复制后继续手动输入时先清空复制草稿并切回文档源，避免两类正文串接。新增 smoke 直接以“输入框为你好、剪贴板为今天天气挺好”验证两次解析仍保持复制文本，并验证后续手动输入恢复文档源；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.15 (23)`，构建包与系统包主程序 SHA-256 均为 `5d99253aed5b1beb5c3d23c94c2012a01f8bc960a11aaa385db3478558b31d8a`，签名有效且无 `/opt/homebrew` 动态依赖；安装后当前输入源仍未选中，必须人工切回后再做真实复制验收。
- 2026-08-03 用户以空输入框直接粘贴“你好啊。 你好啊。 你好啊。”仍无 UI，确认前五轮回归只验证了“IMK 已收到快捷键/剪贴板回调后的状态”，没有覆盖宿主完全吞掉 `Command-V`、输入法尚未收到任何键盘事件的真实路径；旧 `activateServer` 又主动清空 `lastClient`，因此文档观察器没有客户端可读，现有 smoke 会误报通过。`0.6.16 (24)` 改为从 `IMKInputController` 初始化参数及 `client()` 立即绑定该输入会话，激活期间每 120ms 同时观察剪贴板代次和有限文档快照；即使没有任何 IMK 按键回调，只要宿主正文真实变化就重新等待 1000ms。若变化严格匹配当前剪贴板插入结果，则固定实际插入范围并翻译完整粘贴文本，多句内容不再缩成最后一句；连续粘贴相同剪贴板也按每次文档变化逐次触发。新增失败优先回归直接模拟“无 IMK 回调 → 粘贴三段你好啊 → 完整源文本 → 同剪贴板再次粘贴 → 不重复消费静止文档”，并保留等长替换、撤销、光标移动、复制源隔离等既有用例；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.16 (24)`，构建包与系统包主程序 SHA-256 均为 `6f4166547fadaa5fa153ed10187d5b3c01295ad88f28fb4efbac3751e05f8143`，签名有效且无 `/opt/homebrew` 动态依赖；安装未自动切换输入法，TIS 为已启用、可选择、当前未选中，仍须用户切回后做真实宿主验收。
- 2026-08-03 用户在没有候选 UI 时复制/粘贴仍无译文，统一日志已确认 `0.6.16 (24)` 的会话内 Timer 实际仍每 120ms 调用 IMK 文档读取，因此“UI 未显示就完全没有监听”不是完整根因。进一步对照 Apple InputMethodKit 生命周期、Gureum 的初始化客户端持有方式、Squirrel 的进程级 ApplicationDelegate 状态管理及 Easydict 的全局事件/AX 方案后，确认剪贴板生命周期不能继续归属按输入会话创建的 `IMKInputController`；本轮未引入 Easydict 式全局键盘事件或 Accessibility 权限，只把无需权限的 `NSPasteboard.changeCount` 监听上移到输入法主进程。`0.6.17 (25)` 从 `main.swift` 启动唯一进程级 `TranslationClipboardMonitor`，复制发生时即使没有活跃控制器也保留 5 秒未消费快照；随后有效文本会话建立且存在真实光标锚点时再交付，已交付代次只消费一次，相同文本下一次复制仍按新代次触发。会话内 Timer 现在只观察宿主文档变化，不再重复读取剪贴板；安全输入、翻译关闭或其它输入源当前激活时不读取正文。
- 2026-08-03 同轮发现旧 `--input-source-status` 的“当前选中”结论不可信：从 TIS 来源列表读取 Typing 模式的 `kTISPropertyInputSourceIsSelected` 曾返回 `true`，但 `TISCopyCurrentKeyboardInputSource()` 实际为微信输入法 `com.tencent.inputmethod.wetype.pinyin`。状态与进程级监听资格现统一改用真实当前键盘输入源 ID 精确比较，并新增 `InputSourceRegistrationSmoke` 防止再次用列表缓存冒充当前来源。完整 `bun run scripts/macos/test.ts` 通过；系统包已安装为 `0.6.17 (25)`，构建包与系统包主程序 SHA-256 均为 `619c1b9ce7e5855ae18193e0b24ebc6886a9004c0b2c8f50d728470a86db1639`，签名有效且无 `/opt/homebrew` 动态依赖。安装后真实当前键盘输入源仍是微信输入法，TypingChao明确为当前未选中且进程未运行，必须由用户手动切换到 TypingChao后再验收，禁止自动选择。
- 2026-08-03 用户截图确认 `0.6.17 (25)` 的正常译文卡会被长英文撑到约 `720px`，呈现为横跨编辑区的单行灰色工具条；根因是布局允许 `960px` 最大宽度并包含 `560/720/960` 宽度档位，同时输入法独立进程没有固定深色外观。`0.6.18 (26)` 将译文卡最大宽度收至 `420px`，仅保留 `160/200/280/360/420` 紧凑档位，长译文在最多约三行内换行，正文降为 13pt 常规字重，并固定深色 HUD 外观、轻边框和紧凑替换入口；翻译触发、文档观察和服务链路未改。新增 `TranslationOverlaySmoke` 验证长句宽度上限、多行高度、短句紧凑尺寸和窄屏约束，完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.18 (26)`，构建包与系统包主程序 SHA-256 均为 `2efbd6509e6c445e05dd52c94ab2562e3087772fe2fad9264f3f4c3c66f81eda`，签名有效且无 `/opt/homebrew` 动态依赖；安装未自动切换输入法，真实宿主视觉仍须用户切回 TypingChao后截图验收。
- 2026-08-03 用户截图确认 `0.6.18 (26)` 在宿主已回车提交并清空输入框后仍显示旧译文卡。失败优先回归确认两个根因：已验证的文档源在当前句读取失败时仍会退回旧快照，使迟到响应重新显示已删除原文；鼠标发送路径在 mouse-up 后才建立正文基线，可能错过点击发送同步清空。`0.6.19 (27)` 统一收口提交生命周期：无组合态 Return、数字键盘 Enter 与 Command-Return 在交还宿主前立即撤销旧译文；鼠标按下前保存文档基线并覆盖点击发送、右键粘贴等后续编辑；120ms 文档轮询持续验证已显示译文的客户端、原文范围和光标事务；验证过的原文一旦从宿主消失，发送前和返回后都禁止退回旧快照。新增“提交清空 → 文档变化 → 旧快照必须失效”的失败回归，并补回车有无组合态策略断言，完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.19 (27)`，构建包与系统包主程序 SHA-256 均为 `c7d8eb31dbb5675ec8d772b6d9eb8ff36c7e3724def0b321acf561b86533e7b4`，签名有效且无 `/opt/homebrew` 动态依赖；安装后真实当前键盘输入源为 TypingChao且新进程已启动，回车提交、点击发送和迟到响应仍须用户在真实宿主中最终验收。
- 2026-08-03 用户再次反馈 `0.6.19 (27)` 复制粘贴不触发 UI；本机统一日志核实该轮粘贴并非未请求：21:08:05 发起 HTTPS 请求，21:08:06 返回 HTTP 200。代码复查确认真正竞态有两处：按键短时观察通常先于 120ms 文档轮询消费同一次粘贴，却只调用当前句同步，导致多句粘贴缩成最后一句；文档轮询先发现旧译文范围失效时会 `reset` 后直接返回，已经消费到的粘贴变化不会再次出现。此前进程级 `TranslationClipboardMonitor` 只观察“复制”而不是宿主实际“粘贴”，还会提前创建独立翻译状态，违背“目标输入法激活后仅按正文变化工作”的当前边界。`0.6.20 (28)` 已删除该额外监听及其 pasteboard 代次/待消费状态，`Command-V` 短时观察和无 IMK 回调的定时轮询统一构造 `TranslationDocumentChange` 并进入 `synchronizeDocumentChange`；匹配当前剪贴板插入时固定完整 `documentRange`，否则再读取当前句；旧译文失效与当前文档变化同时发生时先处理变化，不再丢事件。新增统一多句静默/重复粘贴回归，并增加不记录正文的“文档变化来源与字符数、请求开始、响应收到、UI 显示”阶段日志；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.6.20 (28)`，构建包与系统包主程序 SHA-256 均为 `6e9aca189df15527c2d5e5d7ee0b69a521cec1c107445c76a055ca90b4ca3c1a`，签名有效且无 `/opt/homebrew` 动态依赖，当前新进程已启动；真实复制粘贴四阶段日志仍待用户本轮重试后核对。
- 2026-08-03 用户在 `0.6.20 (28)` 当前输入源明确为 TypingChao时完成真实多句粘贴，但统一日志没有文档变化、请求开始或网络请求，证明本轮断点发生在文档监控资格而非 API/UI。代码复查确认 `hidePalettes()` 仅表示系统隐藏输入法面板，却错误清空了 `activeOverlayController`；粘贴被宿主直接消费、没有后续 IMK 按键回调时，120ms 文档监控会持续被资格 guard 拦截。`0.6.21 (29)` 保留隐藏面板时的 UI/异步状态收口，但只在真正 `deactivateServer` 时释放输入会话所有权；新增生命周期回归，禁止 `hidePalettes` 再清空当前控制器。完整 `bun run scripts/macos/test.ts` 通过，系统包已安装为 `0.6.21 (29)`，构建包与系统包主程序 SHA-256 均为 `37824f25254b21e399647561451d09394fec73c9520355e5ac4c1ac99219d3b3`，签名有效且无 `/opt/homebrew` 动态依赖，当前新进程 PID `18205` 已启动；真实静默粘贴仍须用户本轮最终验收。
- 2026-08-03 `0.7.0 (30)` 将宿主直接粘贴补齐为同一输入法进程内的 Accessibility 观察链：仅在 TypingChao为当前输入源时，对当前前台应用注册 `AXFocusedUIElementChanged`、`AXValueChanged`、`AXSelectedTextChanged` 与元素销毁通知，并以 180ms 有限探针兼容通知不可靠的宿主；明确排除 `AXSecureTextField`，只读取光标前有限正文且日志只记录来源和字符数。粘贴必须同时满足“剪贴板正文出现在旧选区、最终光标位置匹配、文档长度变化匹配、局部正文一致”，并容忍宿主把选区与正文变化拆成两次 AX 通知；请求开始、响应返回、浮层持续显示和点击替换前均重新核对同一元素、原文范围与最终光标，继续复用 1 秒防抖、请求代次和迟到响应拒绝。新增辅助功能权限状态/请求 CLI、安装期权限引导、Info.plist 用途说明及静默粘贴、重复粘贴、覆盖选区、首尾空白、分段通知和非粘贴回归；完整 `bun run scripts/macos/test.ts` 通过。系统包已安装为 `0.7.0 (30)`，构建包与系统包主程序 SHA-256 均为 `a04d65540eed89ad6f219dc0764c65c21a97a0d17e0b34259fa06b10cf196124`，签名有效、无 `/opt/homebrew` 动态依赖，辅助功能权限为 `true`，当前新进程 PID `55137` 已启动；真实宿主中“不补键粘贴后等待约 2 秒”的 UI 与四阶段日志仍须用户本轮最终验收。
- 2026-08-04 为排除继续按现象猜测，已固定只读对照的输入法前端版本：Gureum `501693a59e8d8d49b92e443c9d9e0bd9adcea26e`（BSD-2-Clause）在控制器初始化时保存 `inputClient`；Squirrel `8418c95c3245e1e0bda578ac054025c5bfaccb7f`（GPL-3.0）在初始化、激活和按键入口持续补全弱引用客户端；azooKey Desktop `3ae5a4651c329d48fee9b9ec7ac1bcd60b940a12`（MIT）长期使用初始化客户端，并在宿主无法直接回写时使用剪贴板与 CGEvent；macSKK `398c1c763e9e8d27b6042022495013a5a8bafc25`（GPL-3.0-or-later）同样在初始化阶段捕获客户端。当前项目只采用“客户端生命周期不能依赖后续按键回调”的行为结论，未复制 GPL 代码、视觉资源或二进制。
- 2026-08-04 对 `0.8.0 (35)` 的真实 InputMethodKit 进程日志核实：`sh.paseo.desktop` 粘贴时，TCC 对 `kTCCServiceListenEvent` 和 `kTCCServiceAccessibility` 均返回拒绝，因此 Command-V 未进入进程级监听，API 与翻译 UI 均不会启动；安装脚本从终端运行 `--request-accessibility` 得到的结果属于错误的进程归属，不能代表输入法宿主进程。`0.8.1 (36)` 改为输入监控授权成功后才注册全局键盘监听，启动、切换输入源和安装阶段均不主动打开系统隐私页；首次启用只展示输入法内“权限”引导，逐项说明输入监控与辅助功能用途、未授权影响和真实状态，只有用户点击“去开启”后才请求权限并进入对应系统设置。完整 `bun run scripts/macos/test.ts` 已通过；当前只完成本地构建，系统级安装与真实 Command-V 验收仍待后续执行。
- 2026-08-04 候选条设置入口与半/全角交互按成熟前端重新收口：azooKey Desktop 当前版本由 `AppDelegate` 持有并复用唯一 `ConfigWindow`，macSKK 通过统一 AppKit/SwiftUI Settings 生命周期打开唯一设置窗口；本项目采用同样的“应用级窗口所有权”行为结论，新增 `TypingChaoApplicationDelegate` 管理唯一设置窗口，不复制其代码或视觉资源。候选条尾部删除手绘“⌄ + ⚙︎”双字符，改为一个真实 `NSButton` 与 SF Symbols `gearshape`，具备独立点击区、悬停、工具提示和辅助功能描述；点击先隐藏候选层，再由应用级入口打开或前置 700×500 的翻译/输入/权限三页设置窗口。Rime Prelude 默认 `default.yaml` 只加载 `numbered_mode_switch`，未默认启用 `Shift+Space`；本项目 `default.custom.yaml` 明确追加 `Shift+Space -> full_shape` 与 `Control+Period -> ascii_punct`，并把空格键作为 Rime 命名键 `space` 传递，修复此前 Shift+Space 被普通可打印空格分支绕过的问题。半/全角和中/西文标点继续作为两个独立 Rime option，设置页分卡说明，切换后用独立短时状态层显示“半角字符/全角字符/中文标点/西文标点”；Rime 数据版本提升到 `3`。`0.9.0 (37)` 完整 `bun run scripts/macos/test.ts` 已通过，包括真实 Shift-Space option 切换、状态文案、单齿轮与唯一设置窗口源码契约；系统包与构建包主程序 SHA-256 均为 `b04346e5a0774afa95e3ebc8f9160da1d5b4e398db1d729cfde9d6cf340ab45f`，签名有效、无 `/opt/homebrew` 动态依赖，系统级安装已完成但真实设置按钮点击和视觉仍待用户切回输入法验收。
- 2026-08-04 `0.9.1 (38)` 对 `0.9.0` 的候选条、设置、权限、模式提示和粘贴链做了全功能 P0/P1 收口：候选尾部用独立几何布局为页码、上一页、下一页、`8pt + 1pt 分隔线 + 7pt` 分组间距和单个 28×28 齿轮按钮分别预留宽度，新增 `CandidateBarLayoutSmoke` 防止页码侵入候选或分页与设置再次合并；设置窗口改用标准 `.normal` 层级，失去当前控制器时仍保存 Rime 选项，已授权权限隐藏无效“去开启”按钮，首次引导只在窗口即将真实展示时标记为已读。Command-V 兜底只在 IMK 正文已验证实际插入剪贴板内容时发送，验证失败不再降级为只读剪贴板翻译；Accessibility 请求开始、响应回写、点击替换前的精确正文探针和替换后的 AX 基线刷新均改为后台串行队列异步执行，避免 `0.18s` 跨进程超时阻塞输入法主线程。Shift-Space 与 Control-Period 产生的半/全角和中西标点变化会同步持久化，真实 Rime Bridge 冒烟已覆盖两组快捷键；非组字模式提示同时避让已有译文卡。完整 `bun run scripts/macos/test.ts` 无 Swift 警告并通过，系统包与构建包主程序 SHA-256 均为 `2d77c940c72ec476c10c31515c5a0125c856d26446774ca53c443f0a9784fb0d`，签名有效、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；系统级安装已完成，当前系统输入源仍为微信输入法且本项目进程未启动，必须由用户切回 TypingChao后验收真实候选视觉、齿轮点击、设置窗口再次打开、TCC 授权返回刷新和真实宿主粘贴，不能以几何/源码测试表述为视觉已通过。
- 2026-08-04 `0.9.2 (39)` 纠正 `0.8.1` 把“输入监控 + 辅助功能”同时当成必需条件的权限模型：当前 Command-V 观察使用 `NSEvent.addGlobalMonitorForEvents`，而粘贴正文核对、返回前复验和点击替换本身都依赖 Accessibility，因此统一以当前输入法进程的 `AXIsProcessTrusted()` 作为单一授权状态，不再用 `CGPreflightListenEventAccess()` 产生“系统已授权但设置页仍显示缺失”的假状态。设置页会在初始化、每次显示、窗口重新获得焦点及可见期间每秒刷新，授权后隐藏“去开启”；进程级 Command-V 监听和 AX 文档观察分别在 1 秒、2 秒内重新检测授权，事件监听创建失败会明确记录 `available: false`。粘贴链自检确认三条入口均只在宿主正文真实变化后调度：IMK/文档轮询核对前后光标与实际插入范围，AX 核对同一进程、同一焦点元素、文档长度、最终光标和实际正文，Command-V 兜底在 450ms 后核对光标前完整剪贴板文本；多句粘贴、相同剪贴板连续粘贴、覆盖选区、首尾空白、AX 分段通知、单纯复制/移动光标不误判均有 smoke 覆盖。所有自动路径继续统一等待正文稳定 1000ms，请求开始和返回都按 generation、当前客户端、最终原文与替换范围拒绝迟到结果。完整 `bun run scripts/macos/test.ts` 已通过，系统包与构建包主程序 SHA-256 均为 `93c08a5260293d70dc578b178303f75db0f02cbcdd652585a53799194727cb10`，签名有效、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；系统包已安装，安装包 CLI 当前返回辅助功能 `true`，但当前输入源仍为微信输入法，真实 InputMethodKit 进程权限、设置页状态和宿主粘贴 UI 必须由用户切回 TypingChao后最终验收，不能把 CLI 结果单独当成真实进程验收。
- 2026-08-04 用户截图最终确认系统“辅助功能”列表中 `TypingChao` 开关已开启，但 `0.9.2 (39)` 设置页仍显示“未开启”；当时 TIS 已明确选中本输入法，实际进程 PID `33086` 也从 `/Library/Input Methods/TypingChao.app` 启动，因此排除未初始化、旧进程映射和单纯窗口刷新问题。根因是测试包长期使用默认 ad-hoc 签名，其 designated requirement 为每次构建变化的 `cdhash`，系统设置保留的同名旧授权记录不能代表新二进制身份。`0.9.3 (40)` 的本地开发构建改为显式稳定要求 `designated => identifier "com.caizhichao.typingchao.inputmethod.TypingChao"`，并在 `scripts/macos/test.ts` 强制校验不得退回 `cdhash`；该弱要求只允许用于当前机器迭代测试，正式分发必须改用真实 Developer ID/受信代码签名，不得把 identifier-only 要求作为生产安全方案。已重新安装系统包，并用 `tccutil reset Accessibility com.caizhichao.typingchao.inputmethod.TypingChao` 仅清理本项目旧辅助功能记录，用户需要在当前稳定身份下重新授权一次，后续同一开发要求的代码更新不应再因 CDHash 改变重复丢权。完整 `bun run scripts/macos/test.ts` 通过，系统包与构建包主程序 SHA-256 均为 `d6b5e18d4b65c4ccbbc853f65a47c14f738ff2d4a9afe37582f622573dad5d19`，签名与 designated requirement 验证通过、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；当前系统输入源为微信输入法且本项目进程未运行，必须切回 TypingChao、点击一次“去开启”并完成真实进程授权后再验收权限状态和粘贴翻译。
- 2026-08-04 用户在权限重新授权后截图确认复制粘贴已经触发翻译，但粘贴 `你好。你好。你好。` 最终只显示 `Hello.`。`0.9.4 (41)` 定位为客户端粘贴竞态：旧 Command-V 兜底固定等待 450ms，350ms 文档轮询可能先按当前末句调度并占用请求代次，完整粘贴兜底随后又因已有任务提前退出，导致完整三句被末句快照永久覆盖；这不是 Go 代理截断。现改为每 45ms 核对一次真实文档、最多 10 次的有限探针，一旦锁定光标前完整剪贴板正文，就取消先到的末句任务、同步文档基线并按完整文本重新进入 1000ms 稳定期；只有已调度同一完整范围时才去重。DeepSeek 提示词同时删除“不要重复任何句子”的歧义，明确保持句子数量、顺序、重复次数、标点与换行，原文重复几次就翻译几次；相同公网 API 连续三次实测均返回 `Hello. Hello. Hello.`，耗时分别约 608ms、659ms、819ms，因此正常体感仍包含用户要求的 1000ms 防抖与约 0.6–0.8 秒网络模型时间。服务端只做鉴权、限流和透明转发，本轮无需部署；完整 `bun run scripts/macos/test.ts` 已通过，系统包与构建包主程序 SHA-256 均为 `d5054f7b5ea22fe6c3ae648424ddfeff3cba2d0274ca72f0f90bc27673737afe`，版本、稳定 designated requirement、签名及动态依赖核对通过。系统级 `0.9.4 (41)` 已安装，但真实宿主中的完整多句粘贴和最终 UI 仍须用户切回 TypingChao后验收，不能提前表述为最终闭环。
- 2026-08-04 用户反馈 `0.9.4 (41)` 启用后连普通打字也明显卡顿。逐项审计确认客户端同时存在五条过重路径：主线程每 350ms 读取 IMK 文档、后台每 400ms 读取 AX 文档、每次按键的 `prepareClient` 都重复执行 Accessibility 启动/前台焦点刷新、未接管宿主按键最多执行 13 次主线程正文探针、鼠标操作最多执行 50 次主线程正文探针；IMK/AX 都是跨进程读取，AX 单次消息超时上限为 180ms，这些任务叠加会周期性挤占输入法事件循环。重新核对当前 Squirrel 与 Gureum 源码后，两者的核心按键链均以 IMK 事件驱动为主，只保留和具体动作绑定的一次性 Timer，不做常驻宿主正文轮询；项目 `0.9.5 (42)` 因此删除两套常驻正文 Timer，`prepareClient` 不再重启 AX 监听，鼠标只收口旧 UI，宿主直输改为连续输入可取消的 120ms 后单次读取。AX 粘贴监听只响应 `AXObserver` 的焦点、正文和选区通知，所有正文读取继续在 utility 串行队列执行，并把通知合并为最多一个正在执行和一个待执行探针；2 秒权限检查命中同一前台应用时直接返回，不再重复读取 AX 焦点。`scripts/macos/test.ts` 已增加输入延迟契约，禁止恢复按键级监听启动、IMK/AX 正文 Timer 或循环主线程探针；完整测试通过。系统级 `0.9.5 (42)` 已安装，构建包与系统包主程序 SHA-256 均为 `92759bf3042baff463710db52aabe1abf3a2285e6fd68d434ecef6d31a4e18fd`，稳定 designated requirement、签名和动态依赖核对通过；当前系统仍选中微信输入法，真实连续打字手感及键盘/右键粘贴仍须用户切回 TypingChao后验收。
- 2026-08-04 用户确认 `0.9.5 (42)` 普通打字卡顿明显改善，但连续多次分别粘贴时译文只覆盖最后一次。根因是请求代次取消本身正确，错误在于每次 Command-V 或 AX 粘贴确认都会把草稿范围重置成最新 `insertedRange`，因此最终代次虽然最新，却只携带最后一段正文。`0.9.6 (43)` 将同一客户端、同一焦点元素、正文真实相邻且合并后不超过 600 字符的连续粘贴合并为一个完整可验证范围；中间出现手动编辑、范围不相邻、焦点变化或正文验证失败时立即开启新范围，不跨上下文误合并。AX 与 Command-V 可能乱序确认同一次粘贴，因此迟到入口若发现其插入范围已经包含在当前完整范围中，只复用当前快照，不得把 AX 已合并的正文缩回最后一段。新增连续三次粘贴累积、非相邻编辑断开、AX 三次累积和 AX/快捷键乱序四类失败回归，完整 `bun run scripts/macos/test.ts` 通过。系统级 `0.9.6 (43)` 已安装，构建包与系统包主程序 SHA-256 均为 `77e581679a06be2963852288211b41126d29438cea6b7c24aac9a73e7e4109a9`，稳定 designated requirement、签名与动态依赖核对通过；当前系统仍选中微信输入法，真实连续三次粘贴必须由用户切回 TypingChao后最终验收。
- 2026-08-04 用户截图确认连续粘贴三次“你好。”后删除一次，编辑框实际剩余“你好。你好。”，但 `0.9.6 (43)` 译文仍只有 `Hello.`。根因不是请求取消，而是自动翻译仍可能沿用 Command-V/IMK 触发时的最后片段或最后一句，发送前没有统一优先读取 Accessibility 当前焦点编辑框完整正文。`0.9.7 (44)` 将编辑事件收缩为纯触发：宿主粘贴、删除、撤销和直输统一做一次可取消延迟读取，删除 Command-V 的 10 次主线程循环探测；停止编辑 1000ms 后以及响应回写前都优先在 utility 串行队列重新读取当前编辑框完整正文，Accessibility 不可用时才回退 IMK，超过 600 字符则不做部分翻译。点击替换只验证客户端与完整原文范围，单纯移动光标不再使替换失效；设置说明同步改为“翻译当前编辑框完整内容”。新增 IMK 与 AX 两组“粘贴三次再删除一次”失败回归，完整 `bun run scripts/macos/test.ts` 已通过。系统级 `0.9.7 (44)` 已安装，构建包与系统包主程序 SHA-256 均为 `93e30dc6d0b7391526f34550d454d56515ad3ec47a2805d69f37eb870cf5bec1`，稳定 designated requirement、签名与动态依赖核对通过；安装脚本在本项目现有系统包归当前用户且可写时直接原位更新，首次安装或受保护包才请求管理员授权。当前键盘输入源为 ABC，输入法进程已退出，必须由用户切回 TypingChao后做真实宿主最终复测。
- 2026-08-04 用户真实复测确认 `0.9.7 (44)` 粘贴后完全不显示译文。系统现场已核实版本、当前输入源和 Accessibility 授权均正常，公网翻译接口以“你好。你好。”返回 HTTP 200 与 `Hello. Hello.`；统一日志显示粘贴后只发生 IMK `Get length`，没有进入翻译请求。根因是宿主编辑链虽然标称“停止编辑一秒后读取”，实际仍在事件后 120ms 先读取一次正文，宿主尚未暴露粘贴结果时读取为空并直接终止，后续 1000ms 防抖根本不会启动。`0.9.8 (45)` 删除这次前置读取：粘贴、删除、撤销和宿主直输只重置同一个 1000ms 稳定任务，满一秒后才首次读取当前编辑框完整正文，读取成功后不再追加第二段防抖而是立即请求；显式翻译快捷键同样使用已解析快照立即请求。完整 `bun run scripts/macos/test.ts` 通过，系统包与构建包主程序 SHA-256 均为 `6b9b509c6921feed854716d50bec882a95afaa4d0f1051674917d49641dee3d8`，签名、稳定 designated requirement 和动态依赖核对通过；系统级安装已完成，当前键盘输入源为微信输入法，真实粘贴仍须用户切回 TypingChao最终验收。
- 2026-08-04 用户在 Electron 宿主 Paseo（`sh.paseo.desktop`）复测 `0.9.8 (45)` 仍无翻译；真实日志显示粘贴事件和稳定期后的 IMK `Get length` 已发生，但没有发出 CFNetwork 翻译请求。现场核实标准 `kAXFocusedUIElementAttribute` 返回 `-25212`，不能取得正文；按 Electron 官方辅助功能入口对当前宿主进程设置 `AXManualAccessibility = true` 后，Accessibility 树可遍历到唯一且 `AXFocused = true` 的深层 `AXTextArea`，其 `AXValue`、`AXSelectedTextRange` 和字符数均可读取，根因确定为旧代码只查顶层焦点而漏掉真实编辑框。`0.9.9 (46)` 在稳定请求阶段的 utility 串行队列中复用已观察焦点元素，失效时才限深 80、限 5000 元素遍历一次宿主 AX 树；禁止在按键主线程遍历、禁止恢复常驻正文轮询。完整 `bun run scripts/macos/test.ts` 通过，Electron 焦点兜底 smoke 覆盖 `AXManualAccessibility`、focused 元素和树遍历契约；系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `ac8d76a438cdbb6f71e9a598ed2300cb0f311c41084c124cb7362a75f74fe764`，签名、稳定 designated requirement 和动态依赖核对通过。安装后当前仍为微信输入法且旧进程已退出，必须由用户切回 TypingChao并在 Paseo 粘贴后静置 1 秒做真实宿主验收；若仍失败，只读检查新 PID 日志、AX 搜索结果和是否产生 CFNetwork 请求，不再修改防抖时序。
- 2026-08-04 用户真实确认 `0.9.9 (46)` 在 Paseo 的粘贴整段翻译已经生效，但飞书输入框仍未显示译文，并反馈 Control 类宿主快捷键出现卡顿。现场核实飞书主进程为 `com.electron.lark`，顶层 AX 焦点同样返回 `-25212`，同时存在空的 `WatermarkWidget` 覆盖窗和名为“飞书”的 `AXStandardWindow`；飞书测试期间客户端翻译请求已返回 HTTP 200，响应后再次执行 IMK 锚点查询，因此本轮同时处理编辑窗搜索、无组合态锚点和快捷键误探针。`0.9.10 (47)` 将 AX 深层搜索改为主窗口/标准窗口优先、可见子树优先，全部候选根共享最多 5000 元素与 80 层深度预算，不因窗口数量扩大扫描；候选层仍只接受实时坐标，译文层仅在同一 IMK 客户端内最多 6 秒复用最近可信锚点，使宿主提交后暂时不返回组合区矩形时仍能显示结果。普通 Control 快捷键在进入 librime 前直接交还宿主，只有既有 `Control+Period` 标点切换和 `Control+Shift+T` 手动翻译保留；进程级 Command-V 回调仍在启动期配置，但全局 keyDown monitor 只在本输入源实际激活期间注册，停用立即移除，并删除原 1 秒授权轮询。完整 `bun run scripts/macos/test.ts` 通过，新增 Control 快捷键策略、同客户端短时锚点复用、跨客户端/过期拒绝及标准窗口共享搜索预算契约；系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `96826c8ca300ebce81c13c01ed8fbbddc846ce316daa871f2bbe278fc9817a9b`，签名、稳定 designated requirement 和动态依赖核对通过。安装后当前键盘输入源仍为微信输入法，飞书显示、Control 快捷键手感与 Paseo 回归必须由用户切回 TypingChao后真实验收，不能提前表述为已闭环。
- 2026-08-04 用户真实复测 `0.9.10 (47)` 后确认飞书仍未显示译文，且浏览器未进入输入光标时 Control/Command 类快捷键仍有卡顿；这证明“输入源已激活”不能等同于“当前存在文本编辑焦点”。根因是全局 keyDown monitor 仍在 `activateServer` 阶段注册，Accessibility 还保留 2 秒授权 Timer，并且飞书在不同 IMK 回调中可能更换客户端代理对象，原锚点缓存按 `ObjectIdentifier` 判断会把同一编辑会话误判为新客户端。`0.9.11 (48)` 将监听生命周期收口为：输入源激活时只注册前台应用/焦点变化观察，不注册全局键盘和正文通知；后台确认存在 `AXFocused = true` 且正文可读的真实编辑元素后，才注册该元素的 Value/Selection/Destroyed 通知并启用 Command-V 全局兜底；离开编辑焦点立即撤销正文通知、Command-V monitor、锚点和翻译任务。原 2 秒授权轮询已删除，普通浏览页面、标签栏和无输入光标状态不再接收全局 keyDown；为了仍能捕获首次粘贴，只保留轻量的应用焦点通知作为资格探测，不读取正文。飞书译文锚点缓存改用 `bundleIdentifier + uniqueClientIdentifierString` 标识稳定 IMK 会话，避免同一飞书输入框因代理对象变化丢失最近可信光标位置；实时候选仍不使用缓存。完整 `bun run scripts/macos/test.ts` 通过，新增“activateServer 不得启动全局键盘监听、真实编辑焦点才可启用、离焦立即停用、无授权/正文 Timer、稳定客户端锚点”契约；系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `0375919d897f040883a27c71cf0012a4d1c1d942014f6d7d21dbb545a47e34ac`，签名、稳定 designated requirement 和动态依赖核对通过。安装后当前键盘输入源仍为微信输入法；飞书翻译、浏览器无光标快捷键和 Paseo 回归仍须用户切回 TypingChao后真实验收，不能提前表述为闭环。

- 2026-08-04 `0.9.12 (49)` 按用户确认将粘贴链收口为无外部权限的剪贴板模式：删除 `TranslationAccessibility.swift`、AX 树遍历、正文通知、全局 Command-V monitor、权限命令行、权限设置页及 `ApplicationServices` 显式链接；`TranslationDraftState` 不再导入 InputMethodKit，不调用 `length()`、`selectedRange()` 或 `attributedSubstring(from:)`，异步请求只使用调度时不可变的 Rime commit/剪贴板快照，并以 `bundleIdentifier + uniqueClientIdentifierString` 稳定会话 ID 拒绝跨会话回写。Command-V 仅在宿主把按键交给当前输入法时读取 `NSPasteboard.general`，多字符 `inputText` 必须与剪贴板去除首尾空白后的正文完全一致才触发；自动请求继续等待 1000ms，译文卡固定不可替换，Control+Shift+T 改为立即翻译当前剪贴板。`bun run scripts/macos/test.ts` 完整通过，新增提交文本累积、剪贴板隔离、跨会话拒绝、无宿主正文读取和无权限声明契约；系统级 `0.9.12 (49)` 已安装，构建包与系统包主程序 SHA-256 均为 `93ea27710221c1da1b5863877153d32c022d938879bdc81a8700f4c730d6f736`，ad-hoc 签名、稳定 designated requirement 和动态依赖核对通过，包内无 `NSAccessibilityUsageDescription` 且不链接 `ApplicationServices`。安装未自动切换输入源，当前仍为微信输入法；用户须切回 TypingChao后验证目标宿主是否会把 Command-V 或多字符粘贴文本转发给 InputMethodKit，未转发的宿主按当前产品边界不自动翻译。
- 2026-08-04 `0.9.14 (51)` 收口符号输入手感：以本机已安装微信输入法 `2.2.1 (637)` 的公开包内 `punctuation.json` 做只读行为核对，其半角/默认斜杠均为直接 `/`、全角为 `／`；本项目只参考这一输入行为，没有复制微信输入法代码、二进制或视觉资源。librime 的 `symbols` 预设会把 `/`、括号、反引号等多选标点保留为组合候选，因此输入控制器新增独立 `RimeInputPolicy`，三个 InputMethodKit 输入入口都在候选 UI 刷新前处理多选符号：空组合首符号不拉起候选条；拼音后的符号把当前中文与符号一次性提交；半角优先选择与实际按键完全一致的候选，全角保留 librime 当前高亮项。拼音组合中的单引号继续作为音节分隔符，提交预览未变化的逗号/句点等翻页动作不误提交。新增纯 Swift 策略、翻译草稿与真实 Rime bridge 冒烟，已验证首个 `/` 直接提交 `/` 且不建立远程翻译草稿、`nihao/` 直接提交并翻译 `你好/`、全角/分隔符/翻页边界；完整 `bun run scripts/macos/test.ts` 通过。系统级安装已完成，构建包与系统包主程序 SHA-256 均为 `c215b6d81d88905052bfcc3d81201a47ee707b6c774214524e03d15c0da08e6d`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过；当前键盘输入源仍为微信输入法，用户切回 TypingChao后须真实验收首符号、拼音后符号、全角和 `xi'an` 分隔输入。
- 2026-08-04 `0.9.15 (52)` 修复 `0.9.14` 中“正文带符号偶发不翻译”：系统日志确认混合文本请求曾正常返回 HTTP 200，但 Rime 未处理的普通空格或单字符进入 `handleUnhandledKey` 后被当作未知动作清空了翻译草稿。输入控制器现将明确会交还宿主写入的空格和单个 ASCII 可打印字符统一送入 `handleConfirmedTextInput`，只有导航、删除、回车等无法从当前事件确认最终正文的动作才使本地上下文失效。`TranslationDraftState` 始终完整缓冲已确认写入的正文、空格及前后/中间符号，只按整段是否至少包含文字或数字决定是否建立请求：单独 `/`、`。。。` 只缓冲不翻译，随后输入正文时会发送包含领先符号的完整文本，正文后的符号也会重新等待 1 秒并发送最终版本。完整 `bun run scripts/macos/test.ts` 已通过，覆盖 `你好 世界。`、`（/你好/）`、首个 `/`、`nihao/`、全角符号、`xi'an` 与候选翻页边界；系统级安装已完成，构建包与系统包版本均为 `0.9.15 (52)`，主程序 SHA-256 均为 `b70c89750bb95aaf653acbbf3aafa0239a22067b39b691f207b84bca65f7311f`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过。安装脚本未调用自动切换，真实符号混输手感仍须在宿主输入框人工验收。
- 2026-08-04 `0.9.17 (54)` 根据真实长译文截图收口译文卡高度与上方布局：正文最大显示高度由旧 `64pt` 提升至 `112pt`，正常译文卡上限为 `130pt`、错误/失效卡上限为 `144pt`，长段落先在 `420pt` 最大宽度内多行换行再截断；无候选条且长卡需要翻到光标上方时，额外让开一整行当前宿主文字，短译文仍保持紧贴光标。布局 smoke 已覆盖长卡突破旧 `82pt` 截断和相邻输入行避让，真实宿主视觉仍以用户截图为准。
- 2026-08-04 `0.9.18 (55)` 修复“替换按钮被卡片裁切后功能阻断”：译文卡不再只依赖右侧文字入口，安全可替换时整张卡片均接受鼠标点击。`TranslationDraftState` 只为本输入法明确写入宿主且首尾连续的 UTF-16 文本累计精确 `replacementRange`，剪贴板来源固定不可替换；Rime 未处理但确定会直通的普通空格/单个 ASCII 可打印字符由输入法自行 `insertText` 后记录写入前光标范围，避免宿主异步写入造成范围未知。译文展示前和点击时都会重新验证控制器会话、空选区、光标仍在原文末尾及精确范围正文未变化，再调用 `IMKTextInput.insertText(...replacementRange:)` 原位替换；任一验证失败只保留“原文已变化”只读卡，禁止覆盖用户文本。连续提交、首尾空白范围裁剪、剪贴板不可替换、长卡布局和整卡命中 smoke 均通过，完整 `bun run scripts/macos/test.ts` 已通过；系统级包已安装为 `0.9.18 (55)`，构建包与系统包主程序 SHA-256 均为 `da3cb9031fa9cdece5ba9ef9459a8c8cead7d19e8fac3739d64fbd8f790e8e48`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过。安装未自动切换，当前键盘输入源仍为微信输入法；必须切回 TypingChao后真实验收整卡点击替换。
- 2026-08-04 `0.9.19 (56)` 根据真实复测修复点击替换无效和 Backspace 未正确清理：系统日志中点击测试期间没有出现宿主正文范围读取，证明把 `attributedSubstring(from:)` 成功返回作为可点击状态硬门禁会使 Paseo/Electron 等未实现该能力的客户端退化为只读卡。替换校验现以本输入法仍未失效的连续写入快照、稳定控制器会话、精确 UTF-16 范围和光标仍在原文末尾为主；宿主能返回范围正文时继续要求内容完全一致，返回 `nil` 时不再阻断完整内部事务，原文被删除、追加、移动或其它已知编辑动作清空事务后仍禁止替换。空组合态 `BackSpace`、`Delete`、`Return` 在进入 librime 前统一清空翻译事务并交还宿主，组合态删除/回车仍由 librime 编辑或提交组字，避免正文删除键被输入法吞掉；点击替换增加不含正文内容的受控成功/拒绝日志，便于下一次真实复测直接区分事件未到达和事务失效。失败优先 smoke 覆盖宿主不支持正文读取时允许完整事务、宿主返回不一致正文时拒绝、Backspace 清空后拒绝，以及三个 IMK 输入入口的空组合态编辑键边界；完整 `bun run scripts/macos/test.ts` 通过。系统级包已安装为 `0.9.19 (56)`，构建包与系统包主程序 SHA-256 均为 `1e4d108d30461aec40f7538b20e5d3133c1ef378115ccb699a2f3978ff40f4bd`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过；当前输入源为 ABC，安装未自动切换，必须切回 TypingChao做真实点击与 Backspace 验收。
- 2026-08-04 `0.9.20 (57)` 根据连续真实复测将 Esc、Backspace 和译文点击收口为分层输入状态机。只读核对 Squirrel 当前公开实现后确认：键盘主链把普通键交给 librime，并依据 handled 决定是否交还宿主；非激活候选窗口在 `NSPanel.sendEvent` 层转发鼠标。该项目为 GPL-3.0，本轮只参考系统行为边界，`TranslationOverlayPanel`、鼠标状态机和翻译事务均为本项目独立实现，没有复制其代码或资源。译文浮窗新增窗口级 `sendEvent` 路由、`isFloatingPanel`、`becomesKeyOnlyIfNeeded` 和鼠标移动接收，点击不再依赖非激活面板子视图是否被 AppKit 命中；窗口消费 mouse-down/drag/mouse-up 后仍复用原有“抬起时在卡片内才替换”的单一状态机。`inputText` 现在把 `U+0008`、`U+007F` 映射为 librime `BackSpace`，把 `U+F728` 映射为 `Delete`：组合态由 librime 逐字删码，空组合态清理旧翻译事务后交还宿主删除正文。Esc 改为只清当前 librime 组合、网络请求、译文/候选/状态浮层和锚点，保留已经提交到宿主的翻译草稿；用户继续输入时仍以 Esc 前后的完整已提交正文重新等待一秒翻译，不再永久打断链条。失败优先测试覆盖非激活 panel 的窗口级 mouse-up、控制文本 Backspace/Delete 映射、真实 librime 的空闲 Backspace 不消费与组合态逐码删除，以及 Esc 清理函数禁止调用 `translationDraft.reset()`；完整 `bun run scripts/macos/test.ts` 通过。系统级包已安装为 `0.9.20 (57)`，构建包与系统包主程序 SHA-256 均为 `d2833f67d7b309de3e9ada92f8fc68c5a11594442e12c28703537ed6067f0df6`，ad-hoc 签名、稳定 designated requirement、无 `/opt/homebrew`/`ApplicationServices` 动态依赖和无 `NSAccessibilityUsageDescription` 均核对通过；安装后输入源仍为 TypingChao且新进程 PID 已重新启动，真实 Paseo 点击、Backspace 与 Esc 连续输入仍待用户验收。
- 2026-08-04 重新核对 Gboard 与 Microsoft SwiftKey 官方翻译说明：成熟移动键盘的翻译主链是由键盘先进入明确的翻译模式、维护待译输入并显示翻译结果，再把译文提交到当前应用；不是让原文先作为普通正文上屏，再跨宿主回读全文并追溯替换。macOS 版后续应采用同一事务边界：中文候选提交到输入法内部 `translation draft`，宿主仅以 marked text 展示尚未确认的完整草稿，停止输入 1 秒后翻译当前完整快照；用户通过明确的“使用译文”或“上屏原文”一次性提交，Esc/Backspace 只按草稿层级编辑或取消，不再依赖任意宿主实现完整的 `attributedSubstring` 与历史正文替换。
- 2026-08-04 Gboard 方向的首版视觉与交互已经落为 `0.10.0 (58)`：Rime 候选确认文本、已知直通字符和明确导入的剪贴板正文先进入输入法内部 `translation draft`，宿主使用同一段 marked text 显示“完整已确认草稿 + 当前 preedit”，不再先上屏原文或回读宿主正文。候选条上方的紧凑翻译展示框固定显示完整语言对，等待态与加载态保持同一尺寸，停止输入 1 秒后翻译当前不可变快照，结果态提供独立的“使用译文”和“上屏原文”按钮；Backspace 在空组合态按扩展字素编辑内部草稿，Return 上屏原文，Esc 只取消当前拼音和译文展示并保留草稿，输入源切出、编辑器切换和宿主快捷键前先上屏原文，避免 marked text 丢失。旧 `attributedSubstring`、`selectedRange`、连续写入范围和追溯替换策略已从翻译主链删除；Command-V 仅在宿主实际转发时接管剪贴板为草稿，仍不注册全局监听或申请辅助功能权限。完整 `bun run scripts/macos/test.ts` 已通过，新增内部草稿累计、纯符号缓冲、Emoji Backspace、剪贴板隔离、迟到快照拒绝、等待/加载稳定尺寸和双按钮布局回归；构建包与系统包主程序 SHA-256 均为 `c1cbb8b519ab9b940e97b95d8c106e14d50aa6f5351ff25168986d2ac69d43a6`，ad-hoc 签名有效且无 `/opt/homebrew`/`ApplicationServices` 动态依赖，已安装到 `/Library/Input Methods/TypingChao.app`。真实宿主中的 marked text 长句、两个按钮、Backspace、Esc、Return、切换输入源和粘贴行为仍须用户人工验收，不能仅凭自动测试表述为完成。
- 2026-08-04 已完成平台目录迁移并建立 Android 单 app 模块，applicationId 为 `com.caizhichao.typingchao`，minSdk 26、compile/target SDK 36、AGP 9.3.1、Gradle Wrapper 9.5.0、JDK 17、NDK 28.2.13676358、CMake 3.31.6；AGP 9 使用内置 Kotlin，不再应用独立 Kotlin Android 插件。V1 使用原生 `InputMethodService`，由 `InputConnection.setComposingText()` 持有“完整已确认草稿 + 当前 preedit”，候选确认文本只进入内部草稿，停止输入 1 秒后发送完整不可变快照，用户明确选择译文或原文后才以 `commitText()` 上屏；组合码变化立即取消旧翻译，纯符号草稿不拉起翻译卡，结果仍由请求代次拒绝迟到回写，密码和 `IME_FLAG_NO_PERSONALIZED_LEARNING` 编辑器暂停远程翻译。
- 2026-08-04 Android 通过项目自有 JNI/CMake 薄桥直接构建现有 `vendor/librime`，没有复制 Trime GPL 代码、资源或二进制；Trime 仅作 Android IME 生命周期和依赖关系参考。静态依赖包含 librime BSD-3-Clause、OpenCC Apache-2.0、Boost Software License、yaml-cpp MIT、LevelDB BSD 与 marisa 许可证，APK 必须打包对应 LICENSE/COPYING；`shared/RimeData/opencc/` 来自当前 `vendor/librime/share/opencc` 生成结果，用于避免在 Android 目标构建阶段运行 OpenCC 主机工具，`rime-data-version.txt` 已提升为 4。
- 2026-08-04 本机 Android 命令行工具链已可用；绿盾会让 NDK 自带 Clang 读取源码密文，因此 CMake 必须继续通过 `scripts/android/apple-clang-launcher.ts` 使用系统 Apple Clang 读取明文，同时保留 NDK 的 Android target/sysroot/linker 参数，并传入 `-fno-sized-deallocation`。不得退回复制源码到临时目录、修改绿盾白名单或直接用 NDK Clang 读取本仓源码；当前产物已核对为 ARM64 Android ELF。
- 2026-08-05 Android `0.1.2 (3)` 补齐粘贴翻译与删除上滑清空：键盘按钮改名为“粘贴并译”，直接通过 `ClipboardManager.primaryClip` 和 `coerceToText()` 导入完整剪贴板，不再因 MIME description 缺失静默退出，空剪贴板显示明确状态；宿主菜单已经完成粘贴时，`onUpdateSelection()` 只在无内部草稿/无 Rime 组合、非安全输入、翻译开启、文本至少 2 个 UTF-16 单元、粘贴后光标位置与剪贴板长度吻合、光标前正文完全等于当前剪贴板且 `setComposingRegion()` 成功时，才把该新增范围接回 composing 草稿并重新等待 1 秒翻译，任一条件不满足都不读取或接管宿主其它正文，因此该路径仍是宿主配合下的严格匹配能力，不能宣称所有应用通用。删除键继续按下立即删和长按连删，上滑 42dp 后按钮显示“清空”并停止连删，松手才清除当前未正式上屏的拼音/翻译草稿，不清除宿主已确认历史正文；草稿删到空时先用空文本替换 composing 区再 `finishComposingText()`，避免把最后残留字符误提交。完整 `bun run scripts/android/test.ts` 已通过，APK SHA-256 为 `fff8c9930a24527ce2524aded8978cff06c41490bd74ff3d56dee82037a8782a`；已覆盖原稳定下载 URL，服务器仍只保留一份 APK，真实应用中的宿主菜单粘贴回调、上滑阈值与清空手感仍待用户安装复验。
- 2026-08-05 Android `0.1.3 (4)` 收口翻译区域高度：旧版把翻译卡作为键盘根布局的独立 `GONE/VISIBLE` View，等待、结果和隐藏状态会反复增加或移除整块高度；现在翻译与 Rime 候选共用始终存在的固定 48dp 候选栏，有候选时显示横向候选和翻页，无候选时在同一栏显示“简体中文 → 目标语言”、等待/错误/译文及紧凑“原文/使用”按钮，隐藏态显示“译文将在这里显示”。翻译正文最多两行并尾部省略，点击正文仍可使用译文；翻译动作只改变栏内宽度，不改变输入法总高度，独立翻译卡不再插入根布局。完整 `bun run scripts/android/test.ts` 已通过，新增契约确认根布局不再单独添加翻译卡、翻译与候选都固定在 48dp 栏位、状态切换只改可见内容；APK SHA-256 为 `97583d5e71aa42d694f0ca31f216ab7d6a03d1e5e639020d352147387ecca06e`，已覆盖原稳定下载 URL且服务器仍只有一份 APK。固定高度、两行译文可读性、按钮宽度和不同屏幕尺寸仍待真机截图验收。
- 2026-08-05 macOS 候选分页键盘手感统一为与浮层箭头相同的状态边界：候选存在且不是末页时，`=` 或 `+` 调用与右箭头相同的 librime `change_page(false)`；当前不是首屏时，`-` 调用与左箭头相同的 `change_page(true)`。首屏的 `-` 和末页的 `=`/`+` 不被分页逻辑吞掉，继续保留原符号输入语义；主键区和数字键盘产生的同字符统一生效。`RimeInputPolicySmoke` 已覆盖首屏、中间页、末页与加号别名，完整 `bun run scripts/macos/test.ts` 通过；本轮只构建验证，未安装系统包，真实宿主键盘事件仍需安装后人工验收。
- 2026-08-05 Android `0.3.1 (7)` 将普通 QWERTY、中文九键和手写工具栏的“粘贴并译”统一收敛为“粘贴”，并同步缩短按钮权重、把释放空间让给空格键；行为不变，点击后仍导入完整剪贴板并按最后一次内容变化重新等待 1 秒翻译。`bun run scripts/android/test.ts` 通过，Debug APK SHA-256 为 `ad3441ea18cb417e5d88ab4e4740fa1e77bbf7ffdd8cc18861476dc1c3ea34df`。上一版 `0.3.0 (6)` 及本版均未覆盖腾讯服务器稳定下载文件，因此用户从既有下载链接取得的仍是旧包，缺少九键/五笔/手写设置属于下载包未更新，不是新设置代码未生效；服务器覆盖必须再次得到用户明确确认。
- 2026-08-05 经用户明确确认，已使用 `bun run scripts/android/deploy-apk.ts` 将 Android `0.3.1 (7)` 原子覆盖到腾讯服务器原有稳定随机下载路径；未新建下载 URL、未修改或 reload Nginx、未改 Go 翻译服务。覆盖前远端唯一 APK SHA-256 为 `97583d5e71aa42d694f0ca31f216ab7d6a03d1e5e639020d352147387ecca06e`，覆盖后远端与公网完整下载 SHA-256 均为 `ad3441ea18cb417e5d88ab4e4740fa1e77bbf7ffdd8cc18861476dc1c3ea34df`；`releases` 目录仍只保留一份 APK，文件名为兼容原链接仍含 `v0.1.0`，但包内版本已核对为 `0.3.1 (7)`。公网响应保持 APK Content-Type、attachment 和 `private, no-store`，错误随机路径为 404，Nginx/翻译服务 active、health 200，最近错误日志未见新增 502/upstream 失败。
- 2026-08-05 Android `0.3.2 (8)` 将中文九键独立重排为成熟中文输入法常见的职责分区：左侧窄栏放常用标点，中间保留 1–9 的 3×3 T9 主键，右侧放删除、只清当前组合的“重输”和较大的换行键，底栏保留输入法切换、手写、粘贴、宽版 `0 / 空格` 与中英文切换；仅参考公开布局职责，不复制微信输入法代码、图标或视觉资源。数字/字母说明及 `0 / 空格` 改为两个 `TextView` 分层绘制，不再依赖字符串换行，主键区 162dp、底栏 54dp，总键区高度仍为 216dp；“重输”只调用现有组合清理入口，保留已进入 `ImeDraft` 的正文。`bun run scripts/android/test.ts` 与 Android Lint 通过，Debug APK SHA-256 为 `ba68f94699d325492b9d0a2c280c7dccca98a622d60f618e5cb982b0edd888ba`；当前无已授权 Android 设备，真实九键尺寸、标点手感、`0 / 空格` 显示与重输行为尚待真机验收，本版尚未覆盖服务器稳定下载包。
- 2026-08-05 macOS `0.10.2 (60)` 修复物理键盘加减号候选翻页：真实 librime `change_page` 往返测试确认 API 可把 `shi` 从第 0 页切到第 1 页并返回，缺口位于 InputMethodKit 带键码但没有 printable string 的回调，主键盘等号/加号、减号及数字键盘加减号此前会退化为未知 `0x...`，无法进入现有翻页策略。`RimeInputPolicy` 现在统一识别字符键名、librime 命名键和四个 macOS ANSI 物理键码；候选非末页的 `=` / `+` 前进，非首屏的 `-` 返回，首屏减号和末页加号仍保留普通符号语义。`bun run scripts/macos/test.ts` 通过，覆盖物理键码恢复、`equal/plus/KP_Add/minus/KP_Subtract` 别名与真实页码往返；构建包和系统包主程序 SHA-256 均为 `e08f22429db77d73c94f0c9f84ac76c4b72435a4b31995d71d271b52e2fbe558`，ad-hoc 签名有效且无 `/opt/homebrew`/`ApplicationServices` 动态依赖，已安装到 `/Library/Input Methods/TypingChao.app`。安装未自动选择输入法，当前键盘输入源为 ABC；用户切回 Typing Chao 后须在有多页候选的拼音（如 `shi`）中人工验收主键盘和数字键盘加减号。
- 2026-08-07 macOS `0.10.2 (60)` 新增长按左右 Command 打开的一次性 AI 输入框：只有 Command 单独持续按住 550ms 才触发，Command-C/V 等普通快捷键、叠加其它修饰键或安全输入状态均不会打开；面板支持输入单条要求、Enter 发送、错误提示、查看结果、上屏结果和 Esc/关闭，不保存或携带多轮历史。请求复用设置页手动缓存的 DeepSeek Key 并直连官方 Chat Completions，关闭面板、切换会话或重复发送会取消旧任务并拒绝迟到回写。`bun run scripts/macos/test.ts` 通过，新增 Command 物理键、非激活可输入面板与单轮契约冒烟；构建包和 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `5c422e9c075899f7a2ea4c65955f54c07e941823b4e8712aaa278f11bb30043c`，签名有效且已安装。用户必须先切换到其它输入法再切回 Typing Chao 启动新进程；真实宿主中的长按触发、中文提示输入、请求结果和上屏行为仍待人工验收。
- 2026-08-07 用户可见品牌名已统一为 `Typing Chao`：macOS `0.10.3 (61)` 的 `CFBundleDisplayName`、`CFBundleName`、三套 `InfoPlist.strings`、输入法菜单和设置窗口均使用带空格名称，TIS `--input-source-status` 返回“输入法：Typing Chao”，源码与系统包均不存在旧品牌字样；完整 `bun run scripts/macos/test.ts` 通过，构建包与 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `c0bbdea5d017084224209e52880078d3ba7a83663a683d154330e877e5ff0ce9`，ad-hoc 签名有效且已安装。内部可执行文件、模块名、类名、Bundle ID 与安装目录继续使用无空格技术标识 `TypingChao`，不能改成带空格名称；安装不会自动切换输入源，已打开的系统设置页面仍需关闭后重新打开以刷新自身缓存。
- 2026-08-07 Android 用户可见品牌名已统一为 `Typing Chao`，版本提升为 `0.3.3 (9)`；`bun run scripts/android/test.ts` 与 Android Lint 通过，Debug APK SHA-256 为 `f673ad6964ab33ad6d0ff6898b8afc5cace040fbc34fc9d1a8179b67769e2aa8`。本版尚未安装 Android 真机，也未覆盖腾讯服务器稳定下载包，不能表述为真机或线上发布完成。
- 2026-08-07 macOS `0.10.4 (62)` 将设置侧栏产品描述由单一“边写边译输入法”改为两行“AI 输入 · 边写边译”和动态读取包信息的“版本 0.10.4 (62)”，以后提升 `CFBundleShortVersionString` / `CFBundleVersion` 后无需再手工同步界面版本；标题继续显示 `Typing Chao`。完整 `bun run scripts/macos/test.ts` 通过，构建包与 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `0d555e48e73eaaa24571ac2f127ad59360411e74763a46a459bffbc30f80a12e`，ad-hoc 签名有效且已安装。用户需先切换到其它输入法再切回 Typing Chao，使新进程加载新版设置界面；真实侧栏换行与版本显示仍待用户打开设置页验收。
- 2026-08-07 macOS `0.10.5 (63)` 将设置侧栏产品描述收敛为“通用 AI 输入法”，输入源模式名由“简体拼音 · 边写边译”改为“简体拼音”，边写边译只保留为设置和菜单中的独立功能项。长按 Command 依赖宿主是否把 modifier-only `flagsChanged` 事件转发给 InputMethodKit，不能作为稳定唯一入口；当前保留长按兼容触发，并新增约 350ms 双击 Command、可靠的 `Control + Shift + A` AI 输入快捷键和输入法菜单“AI 输入…”入口，不引入全局键盘 monitor、Event Tap、Accessibility 或按键状态轮询。完整 `bun run scripts/macos/test.ts` 通过，构建包与 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `31c90431bd40e8ddfa164078f443457d7219f360e04d816586364f05285215a2`，ad-hoc 签名有效且已安装；安装后 TIS 状态为已启用、可选择、当前选中，用户需在文本编辑框中人工验收双击 Command、`Control + Shift + A`、菜单入口和宿主可选的长按兼容触发。
- 2026-08-07 macOS `0.10.6 (64)` 根据宿主实际事件表现移除 Command 长按、双击 Command 和 `flagsChanged` AI 手势，统一改为只识别 InputMethodKit 可稳定转发的 `Option + Return` 普通按键事件；AI 浮层提示、输入法菜单快捷键和冒烟测试同步更新，未引入全局键盘 monitor、Event Tap、Accessibility 或按键状态轮询。完整 `bun run scripts/macos/test.ts` 返回 `0`，AI input overlay smoke 已通过；构建包与 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `147f61efd08111906244712da6edf4c4bb823df286d8199204a8b3a2acd0503c`，ad-hoc 签名有效，已安装到系统输入法目录，TIS 状态为已启用、可选择、当前选中。Option + Return 的真实宿主触发、AI 请求结果和上屏行为仍待用户人工验收，安装后需切换到其它输入法再切回 Typing Chao 以启动新进程。
- 2026-08-07 macOS `0.10.7 (65)` 在首次 `Option + Return` 读取不到可信光标锚点时，下一轮主线程立即重试后再展示 AI 输入浮层，避免依赖键盘自动重复；单次 AI 请求和无全局监听边界不变。默认 `bun run build` / `build:macos` 现在使用 Swift `-Onone` 并复用模块缓存，优化构建仅保留给显式 `bun run build:macos:release`；完整 `bun run test:macos` 返回 `0`，随后默认构建耗时约 1 分 58 秒。`0.10.7 (65)` 已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `c0c367d17a9ef385af734bbdb9522baf9e2168b42c215c278776c392ee3daa5d`，ad-hoc 签名有效，TIS 状态为已启用、可选择、当前选中；首次单按真实宿主表现仍待用户人工验收，安装后需切换到其它输入法再切回 Typing Chao。
- 2026-08-07 macOS `0.10.8 (66)` 修复 AI 面板被首个按键的光标锚点缺失阻断的问题：用户主动触发时有可信光标则贴近光标，无可信光标则居中展示，不能再等待按键重复。翻译设置页新增“AI 输入快捷键”下拉框，默认 `Option + Return`，可改为 `Option + Shift + Return`；输入法菜单快捷键同步读取该设置，设置冒烟覆盖默认值与持久化。完整 `bun run test:macos` 返回 `0`；`0.10.8 (66)` 已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `bcfa46fe3f4cd78b07cbe6c75c674b1cad3b6d5b4b3c0ae1ee7713c73626acac`，ad-hoc 签名有效，TIS 状态为已启用、可选择、当前选中。真实宿主单按与设置窗口视觉效果仍待用户人工验收，安装后需切换到其它输入法再切回 Typing Chao。
- 2026-08-07 用户实测 `0.10.8 (66)` 单按仍未打开、长按后才可见；`0.10.9 (67)` 将 AI 面板展示统一延后到当前 InputMethodKit 按键事件结束后 80ms，避免同一事件收尾阶段隐藏首帧浮层，并保留有光标贴近光标、无光标居中展示的定位规则。完整 `bun run test:macos` 返回 `0`；`0.10.9 (67)` 已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `c4e8fc7444815064df67aaf38fb737560d2acb0ebee51f94cde635f82129d605`，ad-hoc 签名有效，TIS 已启用且可选择。安装时当前系统输入源为 ABC，项目未自动切换；单按真实宿主行为仍待用户在切回 Typing Chao 后验收。
- 2026-08-07 本机 InputMethodKit SDK `IMKInputController.h` 已核实键盘输入的 key binding、带键码文本和直接 NSEvent 三种模式必须三选一；此前控制器同时实现三种入口，用户实测单按 `Option + Return` 不稳定而长按后才出现。`0.11.0 (68)` 统一为仅实现 `inputText(_:key:modifiers:client:)` 的带键码键盘入口，删除直接 NSEvent 和 key binding 文本入口，并移除 80ms 展示补偿；AI、Rime、Command-V、Esc 和直通文本均由该入口处理。完整 `bun run test:macos` 返回 `0`；`0.11.0 (68)` 已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `a6a0f4d9bb927b18f2eedc69ed9bb6bb3f732938e2886ccecd751d63ff9d7e7f`，ad-hoc 签名有效，TIS 已启用、可选择、当前选中；真实单按与宿主鼠标默认行为仍待用户切换输入法后人工验收。
- 2026-08-07 用户继续实测 `0.11.0 (68)` 单按 `Option + Return` 仍无可见效果；根因不是首次按键未到达，而是 AI 面板内的 `NSTextField` 获得键盘焦点后会为同一输入法建立新的 IMK 会话，进程级单活动控制器逻辑把该内部会话误判为外部编辑器接管，随即由 `deactivateServer`、`hidePalettes` 或 `resetForExternalActivation` 关闭刚显示的面板；长按产生的重复事件又在新控制器里打开第二个面板，因此才表现为“长按有效”。macOS `0.11.1 (69)` 增加进程级 AI 面板所有者：面板仍为 key window 时保留原宿主客户端和面板生命周期，内部 IMK 会话不得清理原面板，重复快捷键只消费而不再创建第二个面板；面板失去交互焦点、主动关闭或真实会话结束后仍按原入口收口，不引入延时、全局键盘 monitor、Event Tap、Accessibility 或轮询。完整 `bun run test:macos` 返回 `0`；构建包与 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `2e46493c8b640d59ce75377906843765853f940e961b8b764c4d94c94e015c9d`，ad-hoc 签名有效，已安装且新进程已启动，TIS 状态为已启用、可选择、当前选中；真实宿主单按打开、中文提示输入、关闭后焦点恢复和结果上屏仍待用户人工验收。
- 2026-08-07 用户实测 `0.11.1 (69)` 已无频繁闪烁但首次单按 `Option + Return` 仍不会立即打开，说明上一轮只修复了 AI 面板内部 IMK 会话误关闭，尚未修复首次组合键事件来源。本机 macOS 26.5 SDK 的 `IMKInputController.h` 明确要求 key binding、带键码文本和直接 NSEvent 三种事件模式三选一；macOS `0.11.1 (70)` 因此改为只实现 `recognizedEvents` 与 `handle(_:client:)` 的直接 NSEvent 模式，在首个 `.keyDown` 内判断 `Option + Return`，普通 Rime、Command-V、Control-Shift-T、Esc、鼠标收口和宿主直通也统一走该入口，不再保留 `inputText(_:key:modifiers:client:)`，且继续禁止延时、按键重复兜底、全局 monitor、Event Tap、Accessibility 和轮询。完整 `bun run test:macos` 返回 `0`；构建包与 `/Library/Input Methods/TypingChao.app` 主程序 SHA-256 均为 `e785588f1903f6bc63fe74798d01041fb30189a6c147adea569df626c121e540`，ad-hoc 签名有效，已安装且 TIS 状态为已启用、可选择；安装时当前输入源为 ABC，需切回 Typing Chao 启动新进程，真实宿主首次单按、中文输入、关闭焦点恢复和结果上屏仍待用户人工验收。
- 2026-08-07 用户实测 `0.11.1 (70)` 仅在重新切换到 Typing Chao 后首次 `Option + Return` 可用，之后失效；根因是 AI 面板中的 `NSTextField` 建立内部 IMK 会话时，原宿主控制器收到 `deactivateServer`，旧逻辑虽保留面板却仍调用 `super.deactivateServer`，导致原输入法会话实际停用，只有重新选择输入源才会恢复。`0.11.1 (71)` 在 AI 面板可见且由当前控制器持有时直接忽略该内部停用，不再更改 `isServerActive` 或调用 `super`；真实输入源切出仍按原路径清理。macOS 构建与测试脚本同时优先使用本机 `MacOSX15.4.sdk`：默认 `MacOSX.sdk` 已指向 26.5，但当前 CLT Swift 内部构建号无法加载其模块；15.4 SDK 已实际通过 macOS 13 target 的 typecheck 和完整测试。`bun run test:macos` 返回 `0`；`0.11.1 (71)` 已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包 SHA-256 均为 `31d36f87a6d716a06d7d82e8862f7324e1230d2ff68ce50791f863287c24f915`，ad-hoc 签名有效，TIS 为已启用、可选择、当前选中。安装脚本已结束旧进程，用户仍需先切换到其它输入法再切回 Typing Chao；连续多次单按、关闭后焦点恢复和结果上屏仍待真实宿主人工验收。
- 2026-08-07 用户继续实测 `0.11.1 (71)`：打开/关闭设置后只有第一次 `Option + Return` 生效，随后再次按无反应。结合 InputMethodKit 社区维护经验与本项目控制器流，根因收敛为 AI 面板使用可编辑 `NSTextField` 并调用 `makeKeyAndOrderFront`：它会抢走宿主焦点、创建嵌套 IMK 会话，使后续快捷键路由落在错误会话，先前仅在 `deactivateServer` 中保活原控制器无法从根源消除该焦点切换。`0.11.1 (73)` 将 AI 面板改为不允许成为 key window 的纯展示 `NSPanel`，不再创建独立文本焦点；原宿主 IMK 会话持续接收按键，面板内提示词复用现有 librime 的拼音组合、候选选择、翻页、退格、Esc、Enter 发送和 Command-V 粘贴，但中间文本只写面板草稿、绝不写入宿主。没有加入延时、长按/重复按键兜底、全局 monitor、Event Tap、Accessibility 或轮询。完整 `bun run test:macos` 返回 `0`；`0.11.1 (73)` 已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `4126b962fff1082d5e5d35a5b4f132d8fcf1d7e2ba1b1a5ee40559113b633b51`，ad-hoc 签名验证通过。安装时当前输入源为 ABC，未自动切换；用户须切到其它输入法再切回 Typing Chao 后，在普通文本框连续至少三次单按 `Option + Return`、关闭再打开并输入中文提示进行真实验收，未验收前不得称为最终修复。
- 2026-08-07 用户反馈 `0.11.1 (73)` 的 `Option + Return` 已完全无效果，要求先参考社区现成方案。已重新核对社区维护经验与 Apple InputMethodKit 事件模式：带 Option 的组合键应由 `recognizedEvents` + 原始 `handle(_:client:)` 接收，不能靠普通 `inputText` 文本回调或长按重复事件；当前实现已沿用该模式，继续更换回调方式没有证据能解决现场问题。为停止猜测，`0.11.1 (74)` 只加入可删除的 `TypingChaoAITrace` 受控日志，记录 controller 标识、activate/deactivate/hide/close、每次 Option-Return 是否到达、是否为 repeat、AI 面板可见性以及展示 guard 拒绝原因；日志不读取宿主正文、剪贴板内容或 Key。完整 `bun run test:macos` 返回 `0`，已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `e7dc786e64c518645b6fd5343027e340109fc32a5eb3dd6e933ddef3502b5651`，ad-hoc 签名有效。用户须切到其它输入法再切回 Typing Chao 后按一次 Option-Return 复现，随后由本机 `log show` 读取该轮日志；未取得日志前不得继续用新快捷键或时序补丁猜测根因，定位完成后删除本轮诊断日志。
- 2026-08-07 统一日志未返回 `TypingChaoAITrace`，因此 `0.11.1 (75)` 将同一份无正文、无剪贴板、无 Key 的生命周期/Option-Return 诊断追加到本机临时文件 `/tmp/typingchao-ai-trace.log`，避免 InputMethodKit 进程的 NSLog 管道不可见时继续失明排查；安装脚本后清空该临时文件，等待用户单次复现再只读读取。完整 `bun run test:macos` 返回 `0`，输入源安装时已启用、可选择且当前选中 Typing Chao；构建包与系统包主程序 SHA-256 均为 `7ba9373cd9fbc2bc2bdef5a7da760875f6fe3b94ab7c9aa6c3d7436fb5fc16fa`，ad-hoc 签名有效。此日志只为本轮定位，根因确认后必须删除写入逻辑和临时文件。
- 2026-08-07 用户确认数字键盘的 Option-Enter 能打开 AI 输入，而主键盘 Option-Return 无效；这直接说明当前 IMK 原始事件链可处理 keypad Return，但主 Return 被宿主文本系统提前消费，不能再把主键表现归因于面板生命周期。按用户要求，macOS `0.11.1 (76)` 将默认 AI 输入快捷键及设置/菜单显示改为 `Option + Space`（备选为 `Option + Shift + Space`）；旧保存的 Option-Return 枚举值不再匹配，启动后自动回退到新的默认值。完整 `bun run test:macos` 返回 `0`，AI 快捷键、设置持久化、菜单和无 key window 面板 smoke 均通过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `e16048ccf650d2de634202e8e83316262bfa673801a9302702b85ecf3f45223c`，ad-hoc 签名有效，安装时输入源为当前选中 Typing Chao。新包仍须切到其它输入法再切回启动；真实 Option-Space 单按、连续打开与面板输入待用户验收。
- 2026-08-07 `0.11.1 (77)` 停止继续尝试宿主可能提前消费的 Option 组合键：候选条尾部新增明确的“AI”点击入口，单击后复用同一单轮 AI 面板；输入法菜单保留无快捷键的“AI 输入…”入口。设置页的 AI 快捷键选项、UserDefaults 对应项和临时 `TypingChaoAITrace` / `/tmp/typingchao-ai-trace.log` 诊断均已删除；未引入辅助功能、输入监控、Event Tap、全局键盘监听或独立后台进程。`bun run test:macos` 返回 0，覆盖候选布局、AI 面板不抢焦点、Rime、许可证、签名和静态边界；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `5518a114282d1ca2739e82d41e86d6f9929e0807556082cfcba14b762d039f31`，ad-hoc 签名有效。安装时当前键盘输入源为 ABC，未自动切换；用户须切到其它输入法再切回 Typing Chao 后，在普通文本框输入拼音使候选条出现，单击尾部“AI”并验证面板输入、发送和上屏。
- 2026-08-08 `0.11.1 (78)` 新增输入法内部 `=ai` 快速命令：完整输入前暂存前缀，命中后吞掉命令并复用现有单轮 AI 面板；被其它字符、修饰键或鼠标打断时按原顺序回放缓存键，避免静默吞字。候选条尾部的 AI 点击入口继续保留，设置页增加 `=ai` 使用提示；没有新增全局键盘监听、辅助功能、输入监控或独立后台进程。`bun run test:macos` 返回 0，新增 AI 命令状态机 smoke 与原有 Rime、候选布局、AI 面板、许可证和签名门禁均通过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `40923c6aa791eac24056042533f2c14ff2e907b2b60f0894d2c72f105b10501f`，ad-hoc 签名有效。安装时 Typing Chao 为当前选中输入源，但用户仍须先切到其它输入法再切回，以启动新版本；真实宿主中 `=ai` 触发、普通 `=ai` 文本需求和 AI 面板上屏仍待人工验收。
- 2026-08-08 `0.11.1 (79)` 收口 `=ai` 快速命令的中文输入态边界：中文模式下完整输入 `=ai` 才打开单轮 AI，英文模式不触发，避免英文输入时无法输入普通字面量；未完成前缀被其它字符、修饰键或鼠标打断时按原顺序回放。候选条 AI 点击入口和设置页 `=ai` 提示继续保留；新增命令状态机与 smoke，未引入全局键盘监听、辅助功能、输入监控或独立后台进程。`bun run test:macos` 返回 0；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `6abf1422961fc87a94ade1593df50330b3806ef4c1cf8000299388d8bb58199d`，ad-hoc 签名有效。安装时 Typing Chao 为当前选中输入源，但用户仍须先切到其它输入法再切回，以启动新版本；真实宿主中 `=ai` 触发、英文模式字面量输入和 AI 面板上屏仍待人工验收。
- 2026-08-08 用户确认 `0.11.1 (79)` 的前缀缓存会让单独输入 `=` 暂时不可见，不符合正常输入预期。`0.11.1 (80)` 将 `=`、`=a`、`=ai` 直接显示为宿主 marked text；只有完整匹配时才把候选条首位切换为高亮 AI 星标候选，必须按回车或点击该候选后才移除命令文本并打开现有单轮 AI 面板。退格可逐字撤销，输入其它字符、修饰键、鼠标接管或会话结束时按普通文本提交当前可见前缀；英文模式、候选条尾部 AI 点击入口和菜单入口保持原行为。完整 `bun run test:macos` 返回 0；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `269f2c499a47a5c3c52fea9a738f05bbe37cf5610368e1811493f7545cdcf71d`，ad-hoc 签名有效。安装时 Typing Chao 为当前选中输入源，用户仍须先切换到其它输入法再切回；真实宿主中 `=` 即时显示、`=ai` 首位候选和回车确认仍待人工验收。
- 2026-08-08 用户认为 `=ai` 后仅出现一个候选不自然，并要求把等号本身作为可扩展命令候选入口。`0.11.1 (81)` 因此将触发流程收缩为两次按键：中文空组合态输入 `=` 后立即显示等号 marked text，同时候选条首位显示带数字 `1` 的 AI 星标候选；继续按 `1` 或点击首位候选才移除等号并打开现有单轮 AI 面板。退格取消等号，其它按键、鼠标或会话切换仍先把等号按普通文本提交；英文模式、候选条尾部 AI 点击入口和菜单入口不变，后续可继续在同一等号候选列表追加其它命令。完整 `bun run test:macos` 返回 0；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `8296e15629f48ad87cf55dcea339664041d9abb6b9050c3064243b85f19a1256`，ad-hoc 签名有效。安装时 Typing Chao 为当前选中输入源，用户仍须先切换到其它输入法再切回；真实宿主中等号候选显示、数字 `1` 确认和普通等号输入仍待人工验收。
- 2026-08-08 `0.11.1 (82)` 将等号命令候选的键盘确认补齐为普通首选候选语义：显示 `1 ✦ AI` 后，数字 `1`、回车或鼠标点击首位候选均打开同一个单轮 AI 面板；其它按键仍先提交普通等号，不改变英文模式和候选条尾部 AI 入口。设置提示同步说明数字 `1` 与回车都可确认。完整 `bun run test:macos` 返回 0；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `672486507a343e5320755f98ff443902df4ae7ec4de9c9a99c9d9270053c0664`，ad-hoc 签名有效。安装时 Typing Chao 为当前选中输入源，用户仍须先切换到其它输入法再切回；真实宿主回车确认仍待人工验收。
- 2026-08-08 用户反馈 Shift 在输入法中未正确实现。已核对当前 InputMethodKit 原始事件链与 librime 配置：单独 Shift 在本项目中按 `Shift_L/Shift_R: noop` 设计，不切换中英文；`Shift+Space` 由 Rime `key_binder` 负责切换半角/全角。根因是 macOS 左/右 Shift 键码 `56/60` 在 `InputMethodController.keyName(for:)` 中没有映射，未映射时会被当作未知键并可能提前提交翻译草稿；`0.11.5 (86)` 已补为 `Shift_L/Shift_R`，单独 Shift 走明确空操作，Shift+Space 继续传入 `Shift` 修饰键。新增 Shift 源码契约检查，修正本地 Codex Responses 翻译冒烟断言为中文输入期待英文结果；`TYPINGCHAO_LOCAL_AI_KEY=key-wuxian bun run test:macos` 返回 `0`，已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `623f6d7d8ec0546c9030cc2b51b2c17fd01b2ca0576b4de9b3108e6849bc781b`，ad-hoc 签名有效。安装后未自动切换输入法，用户需切到其它输入法再切回 Typing Chao；单独 Shift 与 Shift+Space 的真实宿主手感仍待人工确认。
- 2026-08-08 用户进一步反馈 `Shift + =`（加号）仍无法输入。复现核对发现 librime 会把物理等号键配合 `Shift` 当作未修饰 `=` 并直接提交，前一轮只补键码映射不足以恢复实际字符。`0.11.5 (86)` 现已在 macOS 原始事件入口识别“仅 Shift + 标点”，读取 `event.characters` 的真实字符（如 `+`、`!`、`_`），去掉 Shift 修饰后重新交给 Rime；AI 输入面板也复用该路径，保留全角/半角配置。Rime bridge smoke 新增实际 `+` 提交断言，完整 `TYPINGCHAO_LOCAL_AI_KEY=key-wuxian bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `4af519ce99ee4bf220a3f565a4cf2bbb570d07195b22d3bcf6ad9e4b8741381d`，ad-hoc 签名有效。仍需切到其它输入法再切回 Typing Chao 后，在真实宿主测试 `Shift + =`；单独 Shift 仍按 noop 设计，Shift+Space 仍为半角/全角切换。
- 2026-08-08 用户继续反馈 `Shift + C` 无法输入大写。复现核对发现 librime 对 `C + Shift` 会继续按小写拼音 `c` 组字，不能依赖修饰键直接得到大写字符。`0.11.5 (86)` 已将 Shift 实际字符识别范围从标点扩展到字母：读取 `event.characters` 的 `C` 后去掉 Shift 修饰重新处理；普通输入未被 Rime 接管时交回宿主，翻译草稿和 AI 输入面板则追加真实大写字符。完整 `TYPINGCHAO_LOCAL_AI_KEY=key-wuxian bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `522f41b86a967b83746bdbf5d18756b77ab50fa5d00c3e7def578df459c26bf7`，ad-hoc 签名有效。仍需切到其它输入法再切回 Typing Chao 后，在普通文本框测试 `Shift + C`；单独 Shift 与 Shift+Space 的既有语义不变。
- 2026-08-08 用户反馈 AI 输入面板不能输入数字，并确认该面板应是通用输入框，只跳过外层 `=` AI 命令监听。`0.11.5 (86)` 已在 AI 面板空组合态将数字、符号、空格和大写字母直接追加为普通提示词；中文小写字母仍复用 Rime 组字，已有中文组合态的数字候选选择和空格确认不改变，面板可见时 `=` 不会再次触发外层 AI 命令。新增 AI 面板数字/普通文本源码契约检查，完整 `TYPINGCHAO_LOCAL_AI_KEY=key-wuxian bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `f4b22eeb1efa1f3d959495d7cf2c7cf6a0b0e57a8fb17d84e626078735777a3f`，ad-hoc 签名有效。仍需切到其它输入法再切回 Typing Chao 后，在 AI 面板测试数字、`=`、空格和中文输入。
- 2026-08-08 用户反馈 AI 面板底部的“关闭 / 发送”两个文字按钮不需要，并反馈回车后提示未配置 Key，但用户已在设置中填写。已核对当前机器：持久化服务选择为 `codex-responses`，当前输入法默认设置未读取到对应 Codex Key；若 Key 配在 DeepSeek，必须先把面板/设置页服务切回 DeepSeek，两个服务 Key 不共用。`0.11.5 (86)` 已移除 AI 面板底部“关闭 / 发送”按钮，继续保留 Esc 关闭、Enter 发送和有结果后的上屏入口；Key 错误提示改为按当前服务显示“Key 无法使用”，HTTP 401/403 显示“Key 错误或无权限”，设置页 Key 占位文案也修正为实际服务名。完整 `TYPINGCHAO_LOCAL_AI_KEY=key-wuxian bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `c9dcad9f42d6cefc99cccacb4f61d97b5155a5e4da126c439fa4540b9329cdab`，ad-hoc 签名有效。真实 AI 面板视觉与用户 Key 读取仍需切换到其它输入法再切回 Typing Chao 后人工确认。
- 2026-08-08 macOS `0.11.6 (87)` 设置页 API Key 改用保留安全输入的 `PasteableSecureTextField`：支持设置窗口内 `Command-V`、显式“粘贴”按钮和当前选区替换，粘贴内容只写当前服务对应的本机 `UserDefaults`，不进入 Keychain、输入法包或日志；空白首尾会在写入前清理。完整 `bun run test:macos` 在同一源代码版本返回 `0`，版本号变更后重新构建并安装到 `/Library/Input Methods/TypingChao.app`，本地与系统包主程序 SHA-256 同为 `e32713f18dafbeee589eec247c1ddcadb730a874ef40803a05e1f495744d26e3`，ad-hoc 签名有效。仍需切到其它输入法再切回 Typing Chao 后，在设置页点击“粘贴”或按 `Command-V` 人工确认长 Key 写入与保存。
- 2026-08-08 macOS `0.11.7 (88)` 将 AI 输入从单轮请求改为当前面板会话连续对话：提交后只清空当前待输入框，不清空上方聊天记录；已完成的用户/AI消息继续显示，下一次 DeepSeek Chat Completions 或 Codex Responses 请求由客户端重新携带本地历史，仍关闭服务端 `store`，不新增 SDK、Keychain 或远程会话存储。UI 继续使用 Swift/AppKit，TypeScript 只负责构建与测试脚本；`TYPINGCHAO_LOCAL_AI_KEY=key-wuxian bun run test:macos` 返回 `0`，本地 `8317` Responses 冒烟已覆盖 AI 输入、连续上下文和翻译；随后重新构建并安装到 `/Library/Input Methods/TypingChao.app`，本地与系统包主程序 SHA-256 同为 `46cf04a016852f8938f89fdea25ddfe2f56dd9081aa2992587300fd36f75b97f`，ad-hoc 签名有效。仍需切到其它输入法再切回 Typing Chao 后，人工确认第二轮提问能看到上一轮上下文。
- 2026-08-08 macOS `0.11.8 (89)` 调整 AI 面板空间利用：面板扩大到 `520×500`，聊天区扩大到 `380` 高，单条或未超过可视区的对话保持顶部完整显示，不再每次刷新都强制滚到底部；只有历史消息确实超过可视区时才滚动到最新内容。完整 `bun run test:macos` 返回 `0`，随后重新构建并安装到 `/Library/Input Methods/TypingChao.app`，本地与系统包主程序 SHA-256 同为 `7006c5cb24adc61450d11f4084647ef027413d06d4b04ed9aed8b47f03a4095c`，ad-hoc 签名有效。仍需切到其它输入法再切回 Typing Chao 后，人工确认一条消息不会把输入区提前挤出可视范围，多轮消息超出面板后才出现滚动。
- 2026-08-08 macOS `0.11.9 (90)` 补上“选中文本后进入 AI 操作”的最小入口：只读取用户当前明确的 IMK 选区，不读取宿主全文、不申请 Accessibility；选中文本后按 `=` 仍先显示与普通 `=` 相同的首位 AI 候选，按 `1` 或回车进入 AI 面板并自动预填选中文本；结果提交前校验选区未变化，未变化时替换原选区，变化时退回当前插入点，直接从候选条/菜单打开 AI 也会预填当前选区。新增选区上下文 smoke 和构建源清单，完整 `bun run test:macos` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `cafbd71127b3d58dec33d0f604d3c07ae68fc498e04fdcc9c0659612d0408525`，ad-hoc 签名有效，版本为 `0.11.9 (90)`。仍需切到其它输入法再切回 Typing Chao 后，在 TextEdit/浏览器等真实宿主中人工确认“选中文本 → `=` → `1`/回车 → 面板预填 → 结果替换”链路；未做该人工操作前不能表述为真实宿主验收完成。
- 2026-08-08 macOS `0.11.10 (91)` 修正选区 AI 触发时的替换行为：选中文本后按 `=` 不再写入或清空 marked text，只显示 `1 AI` 候选并保留宿主原选区；确认候选后继续以原选区预填 AI，结果提交仍按选区未变化规则替换或回退插入。完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `72825625fb3bd301e7b6498a1385c1d582a77195327f13a49b73fb46b9822b07`，ad-hoc 签名有效。仍需切到其它输入法再切回 Typing Chao 后，在真实宿主确认按 `=` 时原选中文字持续高亮且候选可正常弹出。
- 2026-08-08 macOS `0.11.11 (92)` 修正等号和候选回车边界：无选区时 `=` 交还宿主按普通字符输入；只有存在明确选区时 `=` 才进入 AI 候选入口并保持原选区不变。AI 候选或 AI 面板状态下的 `commitComposition` 现在直接收口并阻止回车继续提交到宿主，避免一次回车既弹出 AI 又在编辑框插入换行。完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `62a8397e424aae51d256d58af088856b1b3917fe6a576fa8bced1f4b89d7d191`，ad-hoc 签名有效。仍需切到 Typing Chao 后在真实宿主人工确认普通 `=`、选区 `=` 候选和回车只触发 AI 不插入换行。
- 2026-08-08 macOS `0.11.12 (93)` 收口等号匹配入口：`=` 始终交还 Rime/宿主普通输入链路，不再被 AI 快捷逻辑吞掉；选中文本后的 AI 入口继续通过输入法菜单或候选条 AI 按钮打开并预填明确选区。AI 候选/面板状态下的回车由输入法内部收口，不再同时向宿主编辑框插入换行；Rime bridge smoke 新增 `=` 直接提交断言。完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `fbec2e10410da1d6911b32b2c8d4176982a4dbde3c6c94af0700888e377df9c3`，ad-hoc 签名有效，动态依赖未引用 `/opt/homebrew`。仍需切到其它输入法再切回 Typing Chao 后，在真实 TextEdit/浏览器等宿主人工确认普通 `=`、选区 AI 入口和回车不插入换行。
- 2026-08-08 macOS `0.11.13 (94)` 修正等号在组字期间被候选翻页逻辑吞掉的问题：主键盘等号不再作为候选翻页键，组字中按 `=` 会先收口当前拼音，再把字面等号追加到同一输入草稿；加号/减号翻页仍保留。`RimeInputPolicy` smoke 新增等号在非末页也必须直通的断言，完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `dd1cf31eee63381c9851c5a6bf3cba8212d3be874a3927fe962de7b8458a6025`，ad-hoc 签名有效，动态依赖未引用 `/opt/homebrew`。仍需切到其它输入法再切回 Typing Chao 后，人工确认空组合态和正在输入拼音时按 `=` 均可看到等号。
- 2026-08-08 macOS `0.11.14 (95)` 恢复并收口等号 AI 入口：空闲状态按 `=` 重新显示可见等号 marked text 和首位 AI 候选；选中文本时继续保持原选区并显示 AI 候选；未确认 AI 而按其它普通键时，等号会先按原字符提交，不再静默丢失；AI 候选/面板回车仍由输入法内部收口，不向宿主插入换行。候选翻页的等号/加号既有规则未改回退，`RimeInputPolicy` smoke 继续覆盖该契约。完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `1f6a71a379f71e0bf107a685e024a386bc37a1078a2f396ec129d0ce8f7a0a45`，ad-hoc 签名有效，动态依赖未引用 `/opt/homebrew`。仍需切到其它输入法再切回 Typing Chao 后，人工确认按 `=` 能显示 AI 候选、按 `1`/回车进入面板，以及普通后续字符不会丢失等号。
- 2026-08-08 macOS `0.11.15 (96)` 修正等号 AI 候选确认时的回车双路径：候选 1/回车打开 AI 面板后，`handle`、`commitComposition` 和 `inputText` 三个可能入口都会消费同一次回车，防止宿主把当前表单内容（例如 `asf`）一并提交；AI 面板已正常打开后的回车仍只提交 AI 提示词，不交给宿主。新增 Return 收口契约检查，完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `bea720b9161131b66a60569a1e6cb9ab0be9778b45ddf23e7e332b19f9faaf67`，ad-hoc 签名有效，动态依赖未引用 `/opt/homebrew`。仍需切到其它输入法再切回 Typing Chao 后，在带有 `asf` 等文本的真实宿主中确认按 `=` 后回车只打开 AI，不提交宿主内容。
- 2026-08-09 macOS `0.11.16 (97)` 修复 AI 面板提交时的宿主重复输入：AI 面板可见期间，`inputText` 不再把同一轮 Return 回传的已确认提示词交给 `super.inputText`，而是统一在输入法内部消费；因此面板中的 `aasdf` 不会再因一个回车同时提交到宿主。完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `9896fe40baea6e4ae46103645e764785e5caa859e22328b2405850bac698b9c1`，版本为 `0.11.16 (97)`，ad-hoc 签名验证通过。仍需切到其它输入法再切回 Typing Chao 后，在真实宿主中人工确认 AI 面板输入 `aasdf` 后按一次回车只提交 AI、不再让宿主发送同一文本。
- 2026-08-09 macOS `0.11.17 (98)` 修正组字期间的等号候选操作：有候选时主键盘 `=` / `+` 始终走候选向后翻页，末页也消费按键并保持当前候选，不再把 `=` 直接写成普通符号；非末页仍按 librime 正常切换候选页。新增末页等号/加号策略 smoke，完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `6c86bda2a3d109cbd4d2188da9f1f552a1766cb5e010e90ce875385627b8b491`，版本为 `0.11.17 (98)`，ad-hoc 签名验证通过。当前输入源仍是 ABC，需切回 Typing Chao 后人工确认输入 `adsf` 时按 `=` 能切换候选而不是插入等号。
- 2026-08-09 macOS `0.11.18 (99)` 收缩等号处理：仅保留空闲状态按主键盘 `=` 显示 AI 候选入口；组字期间、候选页和其它场景的 `=` / `+` 不再由输入法翻页或吞键，交回 librime 默认输入链路。`RimeInputPolicy` smoke 更新并通过，完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `67da4e4aca6d25fc3541da3ad474dec0b1d5429a60494b2acfe57e20ff37389c`，版本为 `0.11.18 (99)`，ad-hoc 签名验证通过。用户需切换到其它输入法再切回 Typing Chao 后，确认空闲态 `=` 仍显示截图中的 AI 候选，正在输入拼音时 `=` 恢复普通字符行为。
- 2026-08-09 macOS `0.11.19 (100)` 修正截图所示 AI 等号候选状态：AI 候选已显示时再次按主键盘 `=` 只清理本次等号命令和候选浮层，不再把第二个等号提交到宿主或当前输入草稿；选中文本仍保留，不会误删宿主内容。完整 `bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `7a14e8522b06978c39be75297f6a75516cd8ca800936a272bb155585a2459ac8`，ad-hoc 签名验证通过。用户需切换到其它输入法再切回 Typing Chao 后确认截图状态再次按 `=` 会直接清理 AI 候选，不会插入等号。
- 2026-08-09 macOS `0.11.20 (101)` 清理等号 AI 旧前缀逻辑：AI 入口只接受空闲态一次、无修饰的单个 `=`；不再拼接后续字符、不再把未确认等号提交到宿主，组字、翻译草稿和其它按键继续走原输入链路。重复 `=`、普通后续字符、鼠标和会话切换只清理当前 AI 候选状态；选中文本仍保留。`bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `6afd2386715ae1b9f31c184e7eac7f68bfdb9c03f7c4abc9664994f0c31a3f36`，ad-hoc 签名验证通过。用户需切换到其它输入法再切回 Typing Chao 后确认只有单独按一次 `=` 才出现 `1 AI` 候选。
- 2026-08-09 macOS `0.11.21 (102)` 补齐等号的 InputMethodKit `inputText` 入口：空闲态单独收到一个 `=` 时也统一进入 AI 候选处理，不再从 `inputText` 漏回宿主；组字、非单字符、重复等号及其它按键不触发 AI。等号处理逻辑保持单一入口，不拼接旧前缀，也不提交未确认等号。`bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `0fef8a94c6f6de4a55ee50bedd2e5927dd6c2175df6a6b65cfafce25db6dc608`，ad-hoc 签名验证通过。用户需切换到其它输入法再切回 Typing Chao 后确认单独按一次 `=` 才显示 `1 AI` 候选。
- 2026-08-09 macOS `0.11.22 (103)` 修复单个等号被 InputMethodKit 双入口重复处理的问题：同一次物理 `=` 的 `keyDown` 与后续 `inputText` 回调现在只消费一次，不会先显示 AI 候选又被第二条回调立即清理；独立 `inputText("=")` 仍可单独触发，组字和其它字符不触发。`bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `09d055561dbfcc3d638c83f6bb882f5c33734352ae6613af11ec507cb6663192`，ad-hoc 签名验证通过。用户需切换到其它输入法再切回 Typing Chao 后确认按一次 `=` 后候选不会立即消失。
- 2026-08-09 macOS `0.11.23 (104)` 移除单等号触发时的 marked text 残留：按一次独立 `=` 只消费为 AI 候选触发器，不再把 `=` 显示或写入输入框；选中文本保持原选区，组字和其它输入继续走默认链路。`bun run test:macos` 返回 `0`；已重新安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `1ed44be0def860f15632285372bed13bb693d2ac764a4a8507cdf8162495834e`，ad-hoc 签名验证通过。用户需切换到其它输入法再切回 Typing Chao 后确认单独按一次 `=` 只出现 `1 AI` 候选，不留下等号字符。
- 2026-08-09 macOS `0.11.24 (105)` 将单等号重复回调抑制改为来源无关的同一入口状态：`keyDown` 先到或 `inputText` 先到时，同一次物理 `=` 的第二条 IMK 回调只消费、不清理已显示的 `1 AI` 候选；真正再次按 `=` 仍清理候选，且不把等号写入宿主。`bun run test:macos` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `0e22d49bb93f97d6782ae8b06267afcf00f1086ad7d585686f7a3c8389183312`，`codesign --verify --deep --strict` 通过。当前输入源仍为 ABC、Typing Chao 未自动切换，必须切换到 Typing Chao 后人工确认单独一次 `=` 只显示 `1 AI` 候选，选中候选后才打开 AI 面板。
- 2026-08-10 macOS `0.11.25 (106)` 收缩等号 AI 行为：等号恢复走普通 Rime 输入链路，选中文本按 `=` 会正常被等号替换并可见；单个无修饰 `=` 在普通输入完成后附加显示 `1 AI` 候选，不再清空 marked text、隐藏等号或保留旧的选中文本带入逻辑；重复回调只抑制同一次物理按键，真正再次输入等号仍按普通等号处理并关闭候选。`bun run test:macos` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `f3b4bc573d3dfc51d00e2a6cea2923c966422df54b0681e5c708e1bf17fb7705`，`codesign --verify --deep --strict` 通过。当前输入源为 Typing Chao 且已选中，仍需用户在真实编辑器中确认输入框同时显示 `=` 和 `1 AI` 候选。
- 2026-08-10 macOS `0.11.26 (107)` 修正等号 AI 候选条偶发跳到旧位置：截图根因是 `showAIInputCommandCandidate` 默认允许 `InputOverlayAnchorCache` 复用最近锚点，等号刚提交且 IMK 暂时没有当前光标矩形时会把候选条放到历史位置；候选条现在强制 `allowsCachedAnchor: false`，无实时光标时隐藏而不错误复用旧坐标，译文层的短时缓存规则不变。`bun run test:macos` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `9008daa6a394027f4150443d61d72bf382444864b0bfa085f00d088d050e9957`，`codesign --verify --deep --strict` 通过。截图所示 Paseo/真实宿主定位仍需用户切回新版本后人工确认，自动测试只覆盖实时锚点拒绝旧缓存契约。
- 2026-08-10 macOS `0.11.27 (108)` 修正 Paseo 中等号输入后 `1 AI` 候选不显示的问题：`0.11.26` 禁止候选条复用旧锚点后，等号提交瞬间 IMK 当前坐标仍可能暂时为空，候选条因此被隐藏；现在在等号普通输入前捕获一次实时光标锚点，提交后优先使用新的实时锚点，暂时为空时仅回退到本次按键前捕获的同一位置，不读取长期历史缓存。`bun run test:macos` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `07a1598d7d4d1c08d437584b8843d0c1702f7243a914f334f4c53ef1b72f95c5`，`codesign --verify --deep --strict` 通过。当前输入源为 Typing Chao 且已选中，Paseo 真实宿主仍需用户切回新进程后确认候选条出现在等号输入框附近。
- 2026-08-10 macOS `0.11.28 (109)` 修正等号候选锚点优先级：上一版虽然捕获了等号输入前的正确光标，但显示时仍优先使用等号提交后的错误 IMK 坐标，导致 Paseo 继续把 `1 AI` 放到左上角；现在同一轮候选显示优先使用输入前实时锚点，只有没有输入前锚点时才尝试当前实时坐标，仍不使用长期历史缓存。`bun run test:macos` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `1fffa1581732b5b118fcac0d1ef9fc0f7d60749f7a0de7e5102925e5881e4b95`，`codesign --verify --deep --strict` 通过。当前运行进程已核对为新系统包，仍需用户在 Paseo 中重新切换输入法后确认候选条是否跟随右下角等号。
- 2026-08-10 macOS `0.11.32 (113)` 修正 Paseo 候选条定位：优先使用 `firstRect(forCharacterRange:)` 返回的屏幕坐标，只有该坐标无效时才回退 `attributes(index: 0)`，避免把宿主窗口内坐标误当屏幕坐标而将候选条放到窗口底部。完整 `bun run test:macos` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `8348abd9a03d7613edd694f00332e8315a70114dfb73c650e5f216342daf226e`，ad-hoc 签名验证通过。仍需在 Paseo 中切换到其它输入法再切回 Typing Chao 后人工确认候选条贴近实际光标。
- 2026-08-10 macOS `0.11.33 (114)` 修正 Paseo 候选条仍落在左下的问题：Paseo 的 IMK 矩形在窗口尺寸范围内时按窗口内左上坐标转换为 AppKit 屏幕坐标，窗口几何最多每 500ms 更新一次，其他宿主继续使用原有屏幕坐标链路；新增窗口坐标转换 smoke。缓存清理后完整 `bun run test:macos` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `b3152b676f302d76b4bc422ce835aab8f8bdec5f6526817d8f8b6a03b4f44fbb`，ad-hoc 签名验证通过。仍需在 Paseo 中切换到其它输入法再切回 Typing Chao 后人工确认候选条贴近当前输入框。
- 2026-08-10 macOS `0.11.35 (116)` 修正候选条重复转换 `firstRect` 屏幕坐标的问题：`firstRect(forCharacterRange:)` 现在始终按屏幕坐标使用，只有 `attributes(index: 0)` 的宿主窗口内回退矩形才进行窗口坐标转换，避免候选条被二次翻转到左侧或底部。完整 `bun run test:macos` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `91281686fd23091ea64ec24fc5adbf922d24711547410a3975a57acefa4c6c1b`，ad-hoc 签名验证通过。仍需在 Paseo 与飞书中切换到其它输入法再切回 Typing Chao 后人工确认候选条分别贴近实际输入框。
- 2026-08-11 macOS `0.11.38 (119)` 根据 Paseo 真实回调日志修正候选条定位：Paseo 的 `attributes(index: 0)` 返回窗口内有效矩形 `{{747,188},{1,19}}`，`firstRect(forCharacterRange:)` 返回约 `1.6e-314` 的非零垃圾矩形，旧逻辑误判为有效后被夹到左下角；现对锚点增加实际几何量门禁，拒绝这类垃圾值并回退到已验证的窗口坐标转换。新增 tiny firstRect 回退 smoke；完整 `bun run test:macos` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `93afde9cc1fa6fddba8a47c12037c8c40d2cc30098627c3481b968045cfed7b5`，ad-hoc 签名验证通过。真实 Paseo/飞书切换后的截图复核仍待完成。
- 2026-08-11 macOS `0.11.39 (120)` 根据用户截图修正 0.11.38 的二次坐标翻转：Paseo 的候选条已从左下移动到页面上方，但仍远离底部输入框，说明有效 `attributes` 矩形不应再按宿主窗口左上坐标翻转；现移除 Paseo/飞书专用窗口几何转换，`firstRect` 与 `attributes(index: 0)` 统一按 IMK 屏幕坐标使用，并保留极小垃圾矩形回退门禁。更新 screen-space smoke；完整 `bun run test:macos` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `2f896fcd286c236e53055cb4da636bfde3a532c17143c76479ccbee52f10c81b`，ad-hoc 签名验证通过。真实 Paseo/飞书切换后的最终截图仍待完成。
- 2026-08-11 用户确认 macOS `0.11.39 (120)` 在 Paseo 中候选条定位已恢复有效，前一轮页面上方错位问题闭环；最终人工验收结论以用户截图和本轮反馈为准，仍未宣称飞书已单独验收。
- 2026-08-14 macOS `0.11.41 (122)` 为 AI 服务配置增加按服务隔离的 Base URL：DeepSeek 默认 `https://api.deepseek.com` 并固定追加 `chat/completions`，Codex Responses 默认 `http://127.0.0.1:8317/v1` 并固定追加 `responses`；设置页切换服务时同步恢复该服务保存的 Key/Base URL，未配置地址时以对应默认值作为占位，清除后恢复默认。自定义地址只接受带主机名、无查询参数的 HTTP/HTTPS Base URL，并继续只写本项目 UserDefaults。完整 `bun run scripts/macos/test.ts` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `007fda09b9cf05255c99a1c8616c48cb69fc97ba3311454ced796fa743f27991`。真实设置页切换、占位显示及自定义服务请求仍需人工验收。
- 2026-08-15 macOS `0.11.42 (123)` 为 AI 服务设置页补上按服务隔离的模型列表拉取和选择：Base URL 继续按服务保存并默认显示各自占位，模型通过当前服务的 `GET /models` 拉取后在设置页下拉选择，未拉取时使用各自默认模型名作为回退；切换服务时同步恢复该服务保存的 Key/Base URL/模型。完整 `bun run scripts/macos/test.ts` 返回 `0`，已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `4908613b6572e1412707afce52c19faaa0b541a325f1df780004b07ea313b1a4`。真实服务的模型列表接口和设置页切换仍需按实际账号人工验收。
- 2026-08-15 macOS `0.11.43 (124)` 修复 AI 快速入口回写前残留等号：单独输入 `=` 后确认 AI 候选不再提前调用 `insertText("=")` 或清空 marked text，AI 面板与请求期间继续由原 marked 等号持有宿主替换位置；结果确认时先用 `insertText(..., replacementRange: NSNotFound)` 原位替换 marked text，再重置命令状态并关闭面板，取消面板时仍按原行为提交普通等号。`scripts/macos/test.ts` 新增主链顺序门禁，完整 `bun run scripts/macos/test.ts` 返回 `0`；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `f98d5827d4b6a9f75003aa2e02ace682ea8d5e5a5c756de9b079a10bdc93cc24`，ad-hoc 签名验证通过。真实宿主中蓝色 marked 状态持续和 AI 结果无等号残留仍需用户切换输入法后人工验收。
- 2026-08-15 macOS `0.11.44 (125)` 在不调整 Esc 和关闭语义的前提下，为快速 `=` 入口增加 AI 结果 marked 预览：现有非流式请求返回完整结果后，面板继续显示结果，同时对原客户端调用 `setMarkedText` 把蓝色 marked 区域从 `=` 更新为结果正文；该文本仍未正式提交，只有现有结果确认入口才执行 `insertText` 上屏，Esc 仍沿用原关闭/取消链。选区和菜单入口不创建该宿主预览。完整 `bun run scripts/macos/test.ts` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `d4d46a847c5bffa8670cdfcb593a25ecc5f6ed41233ef0d951cfcec0a61ffe18`，ad-hoc 签名验证通过。真实宿主 marked 结果预览、长文本布局和输入手感仍需用户切换输入法后人工验收。
- 2026-08-15 macOS `0.11.45 (126)` 将 AI 结果态的“上屏结果”鼠标按钮收敛为纯键盘入口：保留现有 `Command-Return` 确认并退出行为，结果可提交时在面板右下显示独立 `⌘`、`↩` 轻量键帽和“上屏并退出”提示，按钮及其 action/configure 代码已删除；加载、错误和空态不显示该提示，Esc 和普通 Enter 语义未改。完整 `bun run scripts/macos/test.ts` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `8c4385967365ac89670d3973bf8eb00e615ff2cad3c3d2d96cb8f6eb090d6d5f`，ad-hoc 签名验证通过。真实面板中的键帽样式、间距和快捷键手感仍需用户切换输入法后人工验收。
- 2026-08-15 macOS `0.11.46 (127)` 为 Codex Responses 的 AI 问答请求默认声明 `tools: [{"type":"web_search"}]`，不增加设置开关；模型按问题需要决定是否实际搜索。该工具只进入多轮 AI 问答请求，翻译请求保持原协议，避免普通翻译产生不必要的联网搜索调用。完整 `bun run scripts/macos/test.ts` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `eb1cb40009d54297a78a7a1ab23e7a41939e10542149de6b034e116b62b60cd2`，ad-hoc 签名验证通过。随后使用当前已保存的 `vip-gpt-5.6-luna` 和本机 `8317` Base URL 完成真实联网冒烟，响应项依次包含 `reasoning:completed`、`web_search_call:completed`、`message:completed`，最终文本为 `WEB_SEARCH_SMOKE_OK`；再以构建产物运行 `gpt-5.6-luna` 的 AI 输入、会话和翻译网络冒烟也全部通过。
- 2026-08-15 macOS `0.11.47 (128)` 修复 Codex Responses 已声明 `web_search` 但天气问题仍先按模型记忆回答“没有搜索工具”的问题：根因是 `tools` 只提供可用能力，默认提示词没有要求搜索任务必须调用。Codex AI 问答提示词现明确规定，用户要求搜索/查询/查证，或问题涉及当前、今天、最新、实时、近期、天气、新闻、价格、汇率、赛事、政策、人物职务等可能变化的信息时，必须先实际调用 `web_search` 再回答，不得因模型记忆中已有答案而跳过，也不得声称没有搜索工具；该规则只对 Codex Responses 追加，DeepSeek 提示词不受影响。完整 `bun run scripts/macos/test.ts` 返回 `0`；使用当前 `vip-gpt-5.6-luna` 直接询问“厦门今天天气怎么样？”且不在用户问题中指定工具，响应包含 `web_search_call:completed` 后再返回天气结果。已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 均为 `180da2aaffac7cc573be1d32036d08fe093baf50ba53c5f3e6d4a5eda16c6944`，ad-hoc 签名验证通过。
- 2026-08-15 macOS `0.12.0 (129)` 将设置窗口与 AI 问答浮层的视觉层统一改为项目自有 React `19.2.8` + Tailwind CSS `4.3.3` 静态页面，由 Vite `8.2.1` 构建后随输入法包进入 `Resources/WebUI`，运行时只依赖系统 WebKit，不携带 Node/Bun 运行时；Swift 继续负责 InputMethodKit 生命周期、Rime 组字、UserDefaults、API Key、网络请求和迟到结果收口，设置页初始化只获知 Key 是否已配置，AI WebView 不取得键盘第一响应者而由隐藏原生按键视图保持原 IMK 会话。React、React DOM、Tailwind、Vite 及 TypeScript 工具链许可证已随包写入 `Resources/ThirdPartyLicenses`，没有复制 Fcitx 等 GPL 实现。完整 `bun run test:macos` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已安装到 `/Library/Input Methods/TypingChao.app`，本地与系统二进制 SHA-256 均为 `75215848ad8d8bcbf060d4c028f37e92068d3b5fb80b8bfb4722990ec297c5dd`，ad-hoc 签名验证通过且无 `/opt/homebrew`、`/usr/local` 动态依赖。设置页输入、AI 面板键盘/鼠标、长对话滚动和真实明暗视觉仍需用户切换输入法后人工验收，不能以自动测试替代。
- 2026-08-15 用户切换到 macOS `0.12.0 (129)` 后确认设置页与 AI 问答浮层均为黑色空白。独立 WKWebView 探针核实包内 `index.html` 与 Tailwind CSS 已加载，但 Vite 生成的外部 `type="module"` 脚本在 `file://` 页面没有执行，React `#root` 保持为空；`0.12.1 (130)` 在 `scripts/macos/build-web-ui.ts` 构建后内联 CSS/JS，并把普通脚本放到 `#root` 节点之后执行，测试契约同时拒绝外部脚本/样式引用和错误执行顺序。完整 `bun run test:macos` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已覆盖安装到 `/Library/Input Methods/TypingChao.app`，本地与系统二进制 SHA-256 均为 `9da4f0622959c607ac7d8518ad4b6b6b179f35d586bb5254c33758cc11d81b84`，签名有效且无 `/opt/homebrew`、`/usr/local` 动态依赖。安装包内设置页和 AI 页均已通过 WKWebView 探针收到 `webViewReady` 且生成非空 DOM；真实输入法窗口视觉与交互仍需用户确认，不能以探针替代人工验收。
- 2026-08-15 用户确认 React 设置页基本显示正常，并要求候选条也统一为 Tailwind、移除候选尾部 AI 图标，同时设置页在内容未超出时不应显示滚动条。`0.12.2 (131)` 将 `CandidateOverlay` 改为仅管理 NSPanel、真实 Rime 宽度/分页快照和 Swift 动作桥接，候选文字、高亮、hover、分页与设置按钮改由 `WebUI/src/CandidateApp.tsx` 和 Tailwind 渲染；候选点击/翻页/设置继续经白名单消息回到 Swift，AI 仍保留单个 `=` 后的首位触发候选和输入法菜单入口，尾部重复 AI 按钮已删除。设置页通过固定高度/`min-height: 0`、收缩垂直间距及仅 `overflow-y: auto` 收口：700pt 实际 WKWebView 探针结果为 `scrollHeight = clientHeight = 700`，超出后才允许垂直滚动。完整 `bun run test:macos` 返回 `0`，本地 Codex Responses 网络冒烟因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过；已覆盖安装到 `/Library/Input Methods/TypingChao.app`，本地与系统二进制 SHA-256 均为 `18d95f558f515be8a4ee72254ba9641aa5fee3233e05856761855dbd43440282`，ad-hoc 签名有效且无 `/opt/homebrew`、`/usr/local` 动态依赖。安装包内候选 WKWebView 已验证渲染 5 项、无尾部 AI 按钮，设置页无实际内容溢出；真实输入法候选鼠标/分页与视觉仍待用户切换后人工确认。
- 2026-08-15 用户反馈单独输入 `=` 时 React 候选里的首位 AI 触发项将“AI”挤到第二行。`0.12.3 (132)` 为该项增加专用 `candidate-item-ai-trigger` 三列 Grid（序号、星标、文本），保证 `1 ✦ AI` 单行排列；普通候选布局不变，候选尾部 AI 图标仍不恢复。TypeScript 检查、Web UI 构建、WKWebView 探针与完整 `bun run test:macos` 均通过（本地 Codex Responses 网络冒烟按既有无 `TYPINGCHAO_LOCAL_AI_KEY` 规则跳过）。已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `e00b8ec77d78e735af7c481277dc72f6956bdb9f14aa50c9b433857e2837d498`，ad-hoc 签名及 designated requirement 有效、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；安装未自动切换输入法，真实 `=` 候选视觉仍待用户切换输入法后截图确认。
- 2026-08-15 用户要求 AI 问答不再由 Swift 代理请求，改由 React 直接使用 Vercel AI SDK。`0.12.4 (133)` 新增 Apache-2.0 的 `ai 7.0.66` 与 `@ai-sdk/openai 4.0.42`：AI 面板在已打开的 WKWebView 内存中临时接收当前服务的 Base URL、模型和 Key，Codex Responses 通过 `streamText + createOpenAI(...).responses(...)` 直连 `/v1/responses` 并声明 `web_search`，DeepSeek 通过 `.chat(...)` 直连；会话、流式文本、取消、60 秒超时与可行动错误状态均在 React 管理。Swift 已删除 `requestAIInput`、AI 请求任务、会话消息和流片段回传，只保留 IMK/Rime 输入同步、运行配置、面板尺寸、安全输入入口、最终结果 marked 预览与宿主上屏；页面关闭即取消请求并清空运行配置，不把 Key 写入源码、构建产物或网页持久化存储。完整 `bun run test:macos` 通过（Swift 与 React AI SDK 的真实网络冒烟都因未设置 `TYPINGCHAO_LOCAL_AI_KEY` 按规则跳过）；本机 CLIProxy 对 `file://` Origin 的 `/v1/responses` OPTIONS 返回 `204` 且允许 `authorization/content-type`、`Access-Control-Allow-Origin: *`，`/v1/models` 的 401 响应同样带 CORS 头。已安装到 `/Library/Input Methods/TypingChao.app`，构建包与系统包主程序 SHA-256 同为 `8e991fa45debdf17a28d9b01f1dce81cd9b75a6eac20c304f88cd6faff3c11e2`，ad-hoc 签名与 designated requirement 有效、无 `/opt/homebrew` 或 `/usr/local` 动态依赖；需先切换输入法重启新进程，再以真实 Key 验收流式回答、当日价格联网搜索、取消与 Command-Return 上屏。

- 2026-08-21 paseo 与 WebUI 回归：本次在 InputMethodController.swift 单文件统一修复两类回归，避免再用宿主二次分发或全局兜底。paseo 场景（轻量 IMKTextInput，依赖 inputText→super 直通）在预编辑态 Shift+6/^ 等符号被吞，表现为“paseo 不行 VSCode 行”，重启 paseo 后恢复；统一改为 inputText/handleUnhandledKey 先 commitComposition 再按 TranslationPolicy.passThroughText 确认输入直通（排除 = 交 AI 链），特殊扩展 rq/sj 触发后任意普通键 clearSpecialInputExpansion 并 return false 回主链，不再常驻吞键。AI 白板场景为双重根因：1）AIInputApp.tsx 将 nativeBridge {messageType,messageData}+webViewReady 误改成 {type,payload}+aiInputAction 导致 TypingChaoWebView isPageReady 永 false 与消息丢弃；2）ai-elements Conversation(SticksToBottom) flex:1 overflow-hidden 在 ai-shell-body 塌陷白板。尝试修复未果后已回滚 WebUI 到 origin/main 原生版（AIInputApp.tsx 448 行 + AIInputSDK.ts + styles.css chat-transcript 布局），删除 components/ai-elements 与 shiki/streamdown 增量；产物体积回落 656KB/41KB，bun x tsc --noEmit + build-web-ui 内联 + build + install 均通过且 /Library 与 build 二进制 SHA-256 一致（9c577b51）。后续 AI Elements 二次接入仅允许官方 MessageResponse(CodeBlock/shiki)+streamdown 表格增量，保留 chat-transcript，禁用 Conversation/StickToBottom 全高容器，file:// 下 shiki 动态分包需 build-web-ui.ts 的 assets 相对路径 patch，且 WKWebView 保持 acceptsKeyboardFocus:false + AIInputKeyCaptureView 单一输入链，typingChaoViewName/webViewReady 门控必检。已同步追加到 memories/repo/typing-dongnanya/macos-input-method.md。
- 2026-08-21 Swift 原生 AI 输入彻底复刻 ai-elements（白板彻底修复，`ea93558f` 已安装）：`AIInputOverlay.swift` 移除 `TypingChaoWebView(aiInput)`，替换为 `AIInputOverlayNativeView: header(✦AI输入)+NSScrollView(NSStackView) transcript + promptContainer(NSTextView composition绿色下划线+▏isPromptEnabled)`；`panelSize 520×500 / empty 520×164`、`borderless+nonactivatingPanel floating`、`becomesKeyOnlyIfNeeded=false/canBecomeKey=true`、`canJoinAllSpaces+fullScreenAuxiliary` 保持；`orderFrontRegardless+makeKey+makeFirstResponder(1×1 AIInputKeyCaptureView)` 单一输入链 `IMK → handleAIInputKey`。`AIInputModels.swift` 对齐 TS 合约 `AIStreamEvent/AIToolCall/AISource/AIConversationMessage/AIInputRuntimeConfiguration/AIInputState/AIPendingState/AILocalShell*`；`AIInputService.swift: @unchecked Sendable streamWithEvents` 复刻 `POST /v1/responses {model,input,instructions,tools:[web_search,shell local],stream:true,store:false,max_output_tokens:1024} SSE` 与 `chat/completions` 双链，`shell Process(/bin/zsh -lc) maxSteps8/PerCmd30s/8k`；`AIInputMarkdownView.swift` 用 `NSAttributedString` 复刻 `MessageResponse(streamdown)+CodeBlock(shiki)+Sources/Reasoning折叠+Tool/Loader/Shimmer`（GFM等宽制表、` ```code``` ` 圆角段落+复制按钮+inline code/link/标题/列表）。`build.ts` 已编入 `AIInputModels/Service/MarkdownView`；`test.ts` 放宽 `processRimeKey(`、补 `SpecialInputExpansion.swift`、新增 `AIInputOverlaySmoke: NativeView+Service+MarkdownView`、`deactivateServer isActiveAIInputController guard`。多角度自检：`applyPrompt` 按 `hasContent` 分支占位且不在空态重建 transcript；`applyState` 的 `promptContainer border` 提前到折叠 early-return 前；移除 `applyPrompt→applyState` 每次按键全栈重建，解决 164px `scrollView.isHidden` 高度塌陷；`layoutCodeCopyButtons` 按 `boundingRect` 估算。`bun run test:macos` 15 smoke 全过，`build→install` 校验 `ba67e297132c494d` 一致（`06f76ac` 已提交，15 smoke: Rime/Input/Overlay/AIOverlay 等）。`shell tool` 回退由 `AIInputService.executeLocalShell/runSingleShell` 在进程内 `Process` 保留。本轮 11 工厂已全量接入 `applyState`：`ChainOfThought(reasoning→isStreaming)+Context(token估算)+Queue(inputAvailable/inputStreaming)+Task(工具计数)+Tool(逐个)+Sources+WebPreview(first url)+InlineCitation(前3)+Confirmation(shell streaming)+空态 Artifact+Plan+Suggestions+Checkpoint(有tool且有text时分隔)`，多角度 31 项自检全过，`Conversation(SticksToBottom)` 禁用、白板/单一链/shell/渲染/生命周期全闭环。
